# gen_plan_evaluation_1 — test_idea

> Phase: `invention_loop` · round 1 · `gen_plan`
> Run: `run_ITc1Qruy7fap` — Text To Logic Pipeline
>
> Full, verbatim transcript of this agent task — every system/user prompt, assistant response, thinking block, tool call and tool result — in the order they occurred. Nothing truncated.

## Task: `gen_plan_evaluation_1` (terminal_claude_agent, claude-sonnet-4-6)

### [1] CONFIG · 2026-06-07 04:54:03 UTC

```
model: claude-sonnet-4-6 | effort: low | permission: bypassPermissions | cwd: /ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_plan/gen_plan_evaluation_1
```

### [2] TOOL CALL — Write · 2026-06-07 04:55:28 UTC

````
File: /ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_plan/gen_plan_evaluation_1/.terminal_claude_agent_struct_out.json

{
  "title": "Dual-Signal SBFL vs. Baselines: End-to-End Statistical Evaluation on FOLIO",
  "summary": "Self-contained Python pipeline that extracts Prolog KBs from FOLIO validation examples, runs dual-signal SBFL repair and four baselines, then computes bootstrap CIs, Cohen's h, McNemar tests, oracle calibration Spearman rho, hallucination proxy rates, and LLM call efficiency — outputting eval_out.json and matplotlib figures.",
  "runpod_compute_profile": "cpu_heavy",
  "metrics_descriptions": "## Metrics\n\n### Primary: Multi-Hop Deduction Accuracy\n- **Definition**: For each FOLIO example, run the final Prolog KB against the gold conclusion query. A correct answer = Prolog returns the same truth value (True/False/Unknown) as the FOLIO gold label. Accuracy = correct / total examples.\n- **Stratification**: Compute separately for examples requiring 1-hop, 2-hop, and 3-hop reasoning chains (approximate hop depth by counting conjuncts in the gold FOL conclusion). Report all three. Hypothesis predicts 3-hop improvement > 2-hop improvement.\n- **Bootstrap CI**: 10,000 resamples with replacement on per-example binary correctness vectors. Report [2.5%, 97.5%] percentile interval for each method. CI excludes zero for the dual-SBFL minus one-shot difference = statistical significance criterion.\n- **Cohen's h**: Effect size for two proportions: h = 2*arcsin(sqrt(p1)) - 2*arcsin(sqrt(p2)). Threshold h ≥ 0.2 = small meaningful effect per hypothesis success criteria.\n- **McNemar Test**: Paired test on per-example correct/incorrect vectors between dual-SBFL and each baseline (one-shot, self-refine, flat-SBFL, CoT). p < 0.05 = significant. Use scipy.stats.contingency.mcnemar.\n\n### Oracle Calibration: Spearman ρ\n- **Definition**: On the first 20 FOLIO examples (calibration subset), generate both LLM-oracle queries and use gold FOLIO labels as human-oracle. For each document compute Ochiai scores from LLM-oracle pass/fail, compute Ochiai scores from human-oracle pass/fail, then rank predicates by each. Spearman ρ = rank correlation between the two ranked lists, averaged across 20 documents.\n- **Threshold**: ρ ≥ 0.6 validates the localization oracle. ρ < 0.6 is a pre-registered invalidation criterion — report it prominently.\n- **Implementation note**: Human oracle uses FOLIO gold label directly as the single ground-truth query answer; LLM oracle generates 10-15 yes/no queries per document and runs them against the KB.\n\n### Hallucination Proxy Rate\n- **Definition**: For each KB predicate, check if it can be grounded to a span in the source document using simple lexical overlap (≥2 content words from predicate name/arguments appear in a 50-word window in the source text). Hallucination rate = fraction of predicates with no grounding span.\n- **Proxy caveat**: True hallucination detection requires human annotation; this lexical proxy is reported with that caveat. Success criterion is ≥20% reduction vs. one-shot baseline.\n- **Computation**: Run on all 150 examples for each method. Report mean ± std.\n\n### LLM Call Efficiency\n- **Definition**: Mean and std of LLM API calls consumed per document per method. Track calls at extraction, oracle generation, repair rounds. Dual-SBFL should use fewer calls than self-refine at matched accuracy.\n- **Also track**: Cumulative USD cost per method (log each call's cost from OpenRouter response metadata).\n\n### Secondary: Atomic Fact Precision/Recall\n- **Definition**: Treat FOLIO gold FOL premises as a set of atomic predicates. For each KB, extract the predicate names and arities. Precision = KB predicates that appear in gold / total KB predicates. Recall = gold predicates covered by KB / total gold predicates.\n- **Note**: Exact predicate matching is approximate since LLM may use different naming; use fuzzy string match (token overlap ≥ 0.5) as fallback. Report both strict and fuzzy variants.\n\n## Methods Evaluated\n1. **dual-SBFL**: Full pipeline — KB extraction + LLM oracle + stratified SLD trace + Ochiai + sub-goal harvesting + top-5 repair × 3 rounds\n2. **one-shot**: Single LLM call to extract KB, no repair\n3. **self-refine**: KB extraction + up to 3 rounds of LLM re-generation with global pass-rate feedback only\n4. **flat-SBFL**: Same as dual-SBFL but with flat binary coverage (no stratification into direct/indirect fault, no sub-goal harvesting) — ablation\n5. **CoT**: Chain-of-thought prompting — LLM reasons step-by-step and answers directly without Prolog KB\n\n## Output Schema (eval_out.json)\n```json\n{\n  \"method_results\": {\n    \"<method>\": {\n      \"accuracy\": float,\n      \"accuracy_1hop\": float,\n      \"accuracy_2hop\": float,\n      \"accuracy_3hop\": float,\n      \"hallucination_rate\": float,\n      \"mean_llm_calls\": float,\n      \"total_cost_usd\": float,\n      \"atomic_precision_strict\": float,\n      \"atomic_recall_strict\": float,\n      \"atomic_precision_fuzzy\": float,\n      \"atomic_recall_fuzzy\": float\n    }\n  },\n  \"bootstrap_cis\": {\n    \"<method_vs_baseline>\": {\"lower\": float, \"upper\": float, \"n_resamples\": 10000}\n  },\n  \"effect_sizes\": {\n    \"<method_vs_baseline>\": {\"cohens_h\": float}\n  },\n  \"mcnemar_pvalues\": {\n    \"<method_vs_baseline>\": float\n  },\n  \"oracle_calibration_rho\": float,\n  \"oracle_calibration_pvalue\": float,\n  \"oracle_calibration_valid\": bool,\n  \"n_examples\": int,\n  \"n_calibration_examples\": 20\n}\n```\n\n## Implementation Details for Executor\n\n### Environment Setup\n```\nuv pip install pyswip datasets openai scipy numpy matplotlib pandas scikit-learn\n# SWI-Prolog must be installed: apt-get install swi-prolog\n```\n\n### FOLIO Dataset Loading\n```python\nfrom datasets import load_dataset\nds = load_dataset('yale-nlp/FOLIO', split='validation')\nexamples = list(ds)[:150]  # cap at 150\n```\nEach FOLIO example has: `premises` (list of NL sentences), `conclusion` (NL string), `label` (entailed/not-entailed/unknown).\n\n### LLM Calls via OpenRouter\nUse `claude-3-haiku-20240307` via OpenRouter (cheapest capable model). Fallback to `meta-llama/llama-3.1-8b-instruct` if Haiku unavailable. Track cost with a global counter. HARD STOP at $8 cumulative.\n\n### Prolog KB Extraction Prompt\nGive the LLM the premises text. Ask it to output a valid SWI-Prolog KB as a code block: facts and rules only, no queries. Parse out the ```prolog ... ``` block. Validate with pyswip.Prolog().assertz() — catch exceptions and flag as extraction failure.\n\n### Stratified SLD Trace Implementation\nUse a pyswip meta-interpreter or SWI-Prolog's built-in `trace/0` + `catch/3`. For each oracle query:\n1. Run `call(Query)` wrapped in a meta-interpreter that hooks on each resolution step\n2. Record for each predicate: (accessed=True/False, unified=True/False, subgoal_failed=True/False)\n3. On failure, extract the deepest sub-goal that was called but never succeeded\n\nAlternative simpler approach if meta-interpreter is complex: use SWI-Prolog's `listing/1` + manual coverage tracking via asserting hooks. Concretely:\n- Use `assert(coverage_hit(Pred, unified))` inside a wrapper that tries `call(Goal)` and catches failures\n- After each query, collect all `coverage_hit/2` facts\n\n### Ochiai Score Computation\nFor each predicate P across N oracle queries:\n- `ef` = queries where P was accessed AND query failed\n- `ep` = queries where P was accessed AND query passed  \n- `nf` = queries where P NOT accessed AND query failed\n- `ochiai(P) = ef / sqrt((ef + nf) * (ef + ep))` (0 if denominator=0)\n\nFor stratified variant: only count `ef`/`ep` for DIRECT fault (unification failed), not indirect.\n\n### Sub-Goal Harvesting\nCollect all deepest-unsatisfied sub-goals from failing queries. Count frequency. Sub-goals appearing in ≥2 failing queries and never appearing as a fact/rule head in the KB = missing predicate candidates.\n\n### Repair Prompt\nFor each top-5 suspicious item, send: the suspicious predicate/sub-goal name, its current definition (if any), the surrounding KB context (±5 predicates), the relevant source text span. Ask LLM to output corrected or new Prolog clauses.\n\n### Hop Depth Estimation\nFor FOLIO examples, count the number of distinct premise sentences referenced in the gold FOL conclusion's proof chain. Approximate: conclusions with 1 conjunct = 1-hop, 2 conjuncts = 2-hop, 3+ = 3-hop. Alternatively parse gold_conclusion FOL formula depth.\n\n### Figures to Generate (matplotlib)\n1. Bar chart: accuracy by method with CI error bars\n2. Bar chart: hallucination rate by method\n3. Scatter plot: accuracy vs. LLM calls per method\n4. Line plot: accuracy by hop depth for dual-SBFL vs. one-shot\n5. Box plot: Spearman ρ distribution across calibration documents\n\nSave as PNG in ./figures/. Also embed as base64 in eval_out.json under `figures` key.\n\n### Failure Handling\n- If pyswip/SWI-Prolog not available: fall back to a Python pure-Prolog interpreter (pylog or kanren)\n- If LLM returns invalid Prolog: skip repair round, count as 0-repair attempt, continue\n- If FOLIO label is 'Unknown': treat as 3-valued logic — Prolog returns no proof = unknown\n- If budget $8 hit: stop processing new examples, evaluate on completed subset, note N_completed in output\n\n### Gradual Scaling\nStart with 10 examples to verify pipeline works end-to-end (< 15 min). Then scale to 50, then 150. Log progress after every 10 examples. Save partial results to eval_out_partial.json every 10 examples so work is not lost.",
  "metrics_justification": "These metrics directly test each component of the hypothesis:\n\n**Multi-hop deduction accuracy** is the primary hypothesis claim — that dual-signal SBFL improves Prolog-based reasoning accuracy. Using the FOLIO gold labels as ground truth provides human-annotated evaluation that is completely decoupled from the LLM oracle used for repair localization, directly resolving the oracle circularity problem stated in the hypothesis. Bootstrap CIs and McNemar tests provide the '95% CI excludes zero' criterion specified in the success criteria. Cohen's h ≥ 0.2 is the stated effect size threshold.\n\n**Hop-depth stratification** tests the specific directional prediction that '3-hop improvement exceeds 2-hop improvement' — a falsifiable sub-claim that distinguishes this from a generic accuracy improvement. If the pattern reverses, it provides nuanced disconfirmation.\n\n**Spearman ρ on oracle calibration** directly tests Assumption 1 (LLM oracle provides sufficient relative ranking signal) and implements the pre-registered invalidation criterion (ρ < 0.6 = abandon method). Measuring this on the first 20 examples before the main experiment mirrors the Phase 0 design in the investigation approach.\n\n**Hallucination proxy rate** tests the claim that repair reduces unsupported predicates by ≥20%. The lexical grounding proxy is imperfect but sufficient for a computational experiment — the plan notes it as a proxy, which matches the scope of this evaluation artifact (full human annotation would require a DATASET artifact).\n\n**Flat-SBFL ablation** tests whether stratified coverage is necessary (Assumption 3) by comparing against the non-stratified variant. If flat-SBFL matches dual-SBFL, stratification adds no value — a partial disconfirmation scenario.\n\n**LLM call efficiency** tests the 'fewer calls than self-refine at matched accuracy' claim, which is a practical efficiency argument central to the motivation.\n\n**Atomic fact precision/recall** provides a fine-grained signal about whether the KB structure (predicate schema) is a bottleneck even when deduction accuracy improves — this corresponds to the partial confirmation scenario in the hypothesis ('SBFL reduces hallucination without improving multi-hop accuracy').\n\nTogether these metrics cover all four success/disconfirmation/partial-confirmation scenarios specified in the hypothesis, making this a complete and rigorous evaluation."
}
````

### [3] TOOL RESULT — Write · 2026-06-07 04:55:28 UTC

````
{"type": "create", "filePath": "/ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_plan/gen_plan_evaluation_1/.terminal_claude_agent_struct_out.json", "content": "{\n  \"title\": \"Dual-Signal SBFL vs. Baselines: End-to-End Statistical Evaluation on FOLIO\",\n  \"summary\": \"Self-contained Python pipeline that extracts Prolog KBs from FOLIO validation examples, runs dual-signal SBFL repair and four baselines, then computes bootstrap CIs, Cohen's h, McNemar tests, oracle calibration Spearman rho, hallucination proxy rates, and LLM call efficiency — outputting eval_out.json and matplotlib figures.\",\n  \"runpod_compute_profile\": \"cpu_heavy\",\n  \"metrics_descriptions\": \"## Metrics\\n\\n### Primary: Multi-Hop Deduction Accuracy\\n- **Definition**: For each FOLIO example, run the final Prolog KB against the gold conclusion query. A correct answer = Prolog returns the same truth value (True/False/Unknown) as the FOLIO gold label. Accuracy = correct / total examples.\\n- **Stratification**: Compute separately for examples requiring 1-hop, 2-hop, and 3-hop reasoning chains (approximate hop depth by counting conjuncts in the gold FOL conclusion). Report all three. Hypothesis predicts 3-hop improvement > 2-hop improvement.\\n- **Bootstrap CI**: 10,000 resamples with replacement on per-example binary correctness vectors. Report [2.5%, 97.5%] percentile interval for each method. CI excludes zero for the dual-SBFL minus one-shot difference = statistical significance criterion.\\n- **Cohen's h**: Effect size for two proportions: h = 2*arcsin(sqrt(p1)) - 2*arcsin(sqrt(p2)). Threshold h ≥ 0.2 = small meaningful effect per hypothesis success criteria.\\n- **McNemar Test**: Paired test on per-example correct/incorrect vectors between dual-SBFL and each baseline (one-shot, self-refine, flat-SBFL, CoT). p < 0.05 = significant. Use scipy.stats.contingency.mcnemar.\\n\\n### Oracle Calibration: Spearman ρ\\n- **Definition**: On the first 20 FOLIO examples (calibration subset), generate both LLM-oracle queries and use gold FOLIO labels as human-oracle. For each document compute Ochiai scores from LLM-oracle pass/fail, compute Ochiai scores from human-oracle pass/fail, then rank predicates by each. Spearman ρ = rank correlation between the two ranked lists, averaged across 20 documents.\\n- **Threshold**: ρ ≥ 0.6 validates the localization oracle. ρ < 0.6 is a pre-registered invalidation criterion — report it prominently.\\n- **Implementation note**: Human oracle uses FOLIO gold label directly as the single ground-truth query answer; LLM oracle generates 10-15 yes/no queries per document and runs them against the KB.\\n\\n### Hallucination Proxy Rate\\n- **Definition**: For each KB predicate, check if it can be grounded to a span in the source document using simple lexical overlap (≥2 content words from predicate name/arguments appear in a 50-word window in the source text). Hallucination rate = fraction of predicates with no grounding span.\\n- **Proxy caveat**: True hallucination detection requires human annotation; this lexical proxy is reported with that caveat. Success criterion is ≥20% reduction vs. one-shot baseline.\\n- **Computation**: Run on all 150 examples for each method. Report mean ± std.\\n\\n### LLM Call Efficiency\\n- **Definition**: Mean and std of LLM API calls consumed per document per method. Track calls at extraction, oracle generation, repair rounds. Dual-SBFL should use fewer calls than self-refine at matched accuracy.\\n- **Also track**: Cumulative USD cost per method (log each call's cost from OpenRouter response metadata).\\n\\n### Secondary: Atomic Fact Precision/Recall\\n- **Definition**: Treat FOLIO gold FOL premises as a set of atomic predicates. For each KB, extract the predicate names and arities. Precision = KB predicates that appear in gold / total KB predicates. Recall = gold predicates covered by KB / total gold predicates.\\n- **Note**: Exact predicate matching is approximate since LLM may use different naming; use fuzzy string match (token overlap ≥ 0.5) as fallback. Report both strict and fuzzy variants.\\n\\n## Methods Evaluated\\n1. **dual-SBFL**: Full pipeline — KB extraction + LLM oracle + stratified SLD trace + Ochiai + sub-goal harvesting + top-5 repair × 3 rounds\\n2. **one-shot**: Single LLM call to extract KB, no repair\\n3. **self-refine**: KB extraction + up to 3 rounds of LLM re-generation with global pass-rate feedback only\\n4. **flat-SBFL**: Same as dual-SBFL but with flat binary coverage (no stratification into direct/indirect fault, no sub-goal harvesting) — ablation\\n5. **CoT**: Chain-of-thought prompting — LLM reasons step-by-step and answers directly without Prolog KB\\n\\n## Output Schema (eval_out.json)\\n```json\\n{\\n  \\\"method_results\\\": {\\n    \\\"<method>\\\": {\\n      \\\"accuracy\\\": float,\\n      \\\"accuracy_1hop\\\": float,\\n      \\\"accuracy_2hop\\\": float,\\n      \\\"accuracy_3hop\\\": float,\\n      \\\"hallucination_rate\\\": float,\\n      \\\"mean_llm_calls\\\": float,\\n      \\\"total_cost_usd\\\": float,\\n      \\\"atomic_precision_strict\\\": float,\\n      \\\"atomic_recall_strict\\\": float,\\n      \\\"atomic_precision_fuzzy\\\": float,\\n      \\\"atomic_recall_fuzzy\\\": float\\n    }\\n  },\\n  \\\"bootstrap_cis\\\": {\\n    \\\"<method_vs_baseline>\\\": {\\\"lower\\\": float, \\\"upper\\\": float, \\\"n_resamples\\\": 10000}\\n  },\\n  \\\"effect_sizes\\\": {\\n    \\\"<method_vs_baseline>\\\": {\\\"cohens_h\\\": float}\\n  },\\n  \\\"mcnemar_pvalues\\\": {\\n    \\\"<method_vs_baseline>\\\": float\\n  },\\n  \\\"oracle_calibration_rho\\\": float,\\n  \\\"oracle_calibration_pvalue\\\": float,\\n  \\\"oracle_calibration_valid\\\": bool,\\n  \\\"n_examples\\\": int,\\n  \\\"n_calibration_examples\\\": 20\\n}\\n```\\n\\n## Implementation Details for Executor\\n\\n### Environment Setup\\n```\\nuv pip install pyswip datasets openai scipy numpy matplotlib pandas scikit-learn\\n# SWI-Prolog must be installed: apt-get install swi-prolog\\n```\\n\\n### FOLIO Dataset Loading\\n```python\\nfrom datasets import load_dataset\\nds = load_dataset('yale-nlp/FOLIO', split='validation')\\nexamples = list(ds)[:150]  # cap at 150\\n```\\nEach FOLIO example has: `premises` (list of NL sentences), `conclusion` (NL string), `label` (entailed/not-entailed/unknown).\\n\\n### LLM Calls via OpenRouter\\nUse `claude-3-haiku-20240307` via OpenRouter (cheapest capable model). Fallback to `meta-llama/llama-3.1-8b-instruct` if Haiku unavailable. Track cost with a global counter. HARD STOP at $8 cumulative.\\n\\n### Prolog KB Extraction Prompt\\nGive the LLM the premises text. Ask it to output a valid SWI-Prolog KB as a code block: facts and rules only, no queries. Parse out the ```prolog ... ``` block. Validate with pyswip.Prolog().assertz() — catch exceptions and flag as extraction failure.\\n\\n### Stratified SLD Trace Implementation\\nUse a pyswip meta-interpreter or SWI-Prolog's built-in `trace/0` + `catch/3`. For each oracle query:\\n1. Run `call(Query)` wrapped in a meta-interpreter that hooks on each resolution step\\n2. Record for each predicate: (accessed=True/False, unified=True/False, subgoal_failed=True/False)\\n3. On failure, extract the deepest sub-goal that was called but never succeeded\\n\\nAlternative simpler approach if meta-interpreter is complex: use SWI-Prolog's `listing/1` + manual coverage tracking via asserting hooks. Concretely:\\n- Use `assert(coverage_hit(Pred, unified))` inside a wrapper that tries `call(Goal)` and catches failures\\n- After each query, collect all `coverage_hit/2` facts\\n\\n### Ochiai Score Computation\\nFor each predicate P across N oracle queries:\\n- `ef` = queries where P was accessed AND query failed\\n- `ep` = queries where P was accessed AND query passed  \\n- `nf` = queries where P NOT accessed AND query failed\\n- `ochiai(P) = ef / sqrt((ef + nf) * (ef + ep))` (0 if denominator=0)\\n\\nFor stratified variant: only count `ef`/`ep` for DIRECT fault (unification failed), not indirect.\\n\\n### Sub-Goal Harvesting\\nCollect all deepest-unsatisfied sub-goals from failing queries. Count frequency. Sub-goals appearing in ≥2 failing queries and never appearing as a fact/rule head in the KB = missing predicate candidates.\\n\\n### Repair Prompt\\nFor each top-5 suspicious item, send: the suspicious predicate/sub-goal name, its current definition (if any), the surrounding KB context (±5 predicates), the relevant source text span. Ask LLM to output corrected or new Prolog clauses.\\n\\n### Hop Depth Estimation\\nFor FOLIO examples, count the number of distinct premise sentences referenced in the gold FOL conclusion's proof chain. Approximate: conclusions with 1 conjunct = 1-hop, 2 conjuncts = 2-hop, 3+ = 3-hop. Alternatively parse gold_conclusion FOL formula depth.\\n\\n### Figures to Generate (matplotlib)\\n1. Bar chart: accuracy by method with CI error bars\\n2. Bar chart: hallucination rate by method\\n3. Scatter plot: accuracy vs. LLM calls per method\\n4. Line plot: accuracy by hop depth for dual-SBFL vs. one-shot\\n5. Box plot: Spearman ρ distribution across calibration documents\\n\\nSave as PNG in ./figures/. Also embed as base64 in eval_out.json under `figures` key.\\n\\n### Failure Handling\\n- If pyswip/SWI-Prolog not available: fall back to a Python pure-Prolog interpreter (pylog or kanren)\\n- If LLM returns invalid Prolog: skip repair round, count as 0-repair attempt, continue\\n- If FOLIO label is 'Unknown': treat as 3-valued logic — Prolog returns no proof = unknown\\n- If budget $8 hit: stop processing new examples, evaluate on completed subset, note N_completed in output\\n\\n### Gradual Scaling\\nStart with 10 examples to verify pipeline works end-to-end (< 15 min). Then scale to 50, then 150. Log progress after every 10 examples. Save partial results to eval_out_partial.json every 10 examples so work is not lost.\",\n  \"metrics_justification\": \"These metrics directly test each component of the hypothesis:\\n\\n**Multi-hop deduction accuracy** is the primary hypothesis claim — that dual-signal SBFL improves Prolog-based reasoning accuracy. Using the FOLIO gold labels as ground truth provides human-annotated evaluation that is completely decoupled from the LLM oracle used for repair localization, directly resolving the oracle circularity problem stated in the hypothesis. Bootstrap CIs and McNemar tests provide the '95% CI excludes zero' criterion specified in the success criteria. Cohen's h ≥ 0.2 is the stated effect size threshold.\\n\\n**Hop-depth stratification** tests the specific directional prediction that '3-hop improvement exceeds 2-hop improvement' — a falsifiable sub-claim that distinguishes this from a generic accuracy improvement. If the pattern reverses, it provides nuanced disconfirmation.\\n\\n**Spearman ρ on oracle calibration** directly tests Assumption 1 (LLM oracle provides sufficient relative ranking signal) and implements the pre-registered invalidation criterion (ρ < 0.6 = abandon method). Measuring this on the first 20 examples before the main experiment mirrors the Phase 0 design in the investigation approach.\\n\\n**Hallucination proxy rate** tests the claim that repair reduces unsupported predicates by ≥20%. The lexical grounding proxy is imperfect but sufficient for a computational experiment — the plan notes it as a proxy, which matches the scope of this evaluation artifact (full human annotation would require a DATASET artifact).\\n\\n**Flat-SBFL ablation** tests whether stratified coverage is necessary (Assumption 3) by comparing against the non-stratified variant. If flat-SBFL matches dual-SBFL, stratification adds no value — a partial disconfirmation scenario.\\n\\n**LLM call efficiency** tests the 'fewer calls than self-refine at matched accuracy' claim, which is a practical efficiency argument central to the motivation.\\n\\n**Atomic fact precision/recall** provides a fine-grained signal about whether the KB structure (predicate schema) is a bottleneck even when deduction accuracy improves — this corresponds to the partial confirmation scenario in the hypothesis ('SBFL reduces hallucination without improving multi-hop accuracy').\\n\\nTogether these metrics cover all four success/disconfirmation/partial-confirmation scenarios specified in the hypothesis, making this a complete and rigorous evaluation.\"\n}\n", "structuredPatch": [], "originalFile": null, "userModified": false}
````

### [4] ASSISTANT · 2026-06-07 04:55:33 UTC

```
The plan is written to `.terminal_claude_agent_struct_out.json`. It covers:

- **End-to-end pipeline**: FOLIO download → KB extraction → dual-SBFL repair → 4 baselines → statistical evaluation
- **5 methods**: dual-SBFL, one-shot, self-refine, flat-SBFL (ablation), CoT
- **All required metrics**: bootstrap CIs (10k resamples), Cohen's h, McNemar p-values, Spearman ρ for oracle calibration, hallucination proxy, LLM call counts/costs
- **Hop-depth stratification**: Tests the directional prediction that 3-hop gains exceed 2-hop gains
- **Concrete implementation details**: pyswip stratified SLD trace approach, Ochiai formula, sub-goal harvesting logic, repair prompt structure, gradual scaling (10→50→150 examples), $8 cost hard stop, partial result saving every 10 examples
```
