# gen_plan_experiment_1 — test_idea

> Phase: `invention_loop` · round 1 · `gen_plan`
> Run: `run_ITc1Qruy7fap` — Text To Logic Pipeline
>
> Full, verbatim transcript of this agent task — every system/user prompt, assistant response, thinking block, tool call and tool result — in the order they occurred. Nothing truncated.

## Task: `gen_plan_experiment_1` (terminal_claude_agent, claude-sonnet-4-6)

### [1] CONFIG · 2026-06-07 04:54:03 UTC

```
model: claude-sonnet-4-6 | effort: low | permission: bypassPermissions | cwd: /ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_plan/gen_plan_experiment_1
```

### [2] TOOL CALL — Skill · 2026-06-07 04:54:16 UTC

```
aii-web-tools
```

### [3] TOOL RESULT — Skill · 2026-06-07 04:54:16 UTC

```
{"success": true, "commandName": "aii-web-tools"}
```

### [4] TOOL CALL — ToolSearch · 2026-06-07 04:54:18 UTC

```
{'query': 'select:WebSearch,WebFetch'}
```

### [5] TOOL RESULT — ToolSearch · 2026-06-07 04:54:18 UTC

```
{"matches": ["WebSearch", "WebFetch"], "query": "select:WebSearch,WebFetch", "total_deferred_tools": 30}
```

### [6] TOOL CALL — WebSearch · 2026-06-07 04:54:24 UTC

```
pyswip SWI-Prolog Python bridge API query examples 2024
```

### [7] TOOL CALL — WebSearch · 2026-06-07 04:54:24 UTC

```
FOLIO dataset yale-nlp HuggingFace NL-to-FOL format structure
```

### [8] TOOL RESULT — WebSearch · 2026-06-07 04:54:28 UTC

```
{"query": "pyswip SWI-Prolog Python bridge API query examples 2024", "results": [{"tool_use_id": "srvtoolu_01U2p5uoBnuugvxpnVdX9Trm", "content": [{"title": "GitHub - yuce/pyswip: PySwip is a Python-Prolog interface that enables querying SWI-Prolog in your Python programs. · GitHub", "url": "https://github.com/yuce/pyswip"}, {"title": "GitHub - jesg/pyswip: PySWIP is a bridge between Python and SWI-Prolog. (Fork)", "url": "https://github.com/jesg/pyswip"}, {"title": "pyswip", "url": "https://pypi.org/project/pyswip/0.3.0/"}, {"title": "GitHub - dsapandora/pyswip: PYSWIP integrator for Python and Swip Prolog · GitHub", "url": "https://github.com/dsapandora/pyswip"}, {"title": "pyswip · PyPI", "url": "https://pypi.org/project/pyswip/0.1.0/"}, {"title": "GitHub - tjvr/pyswip: Use SWI-Prolog from Python. Fork.", "url": "https://github.com/tjvr/pyswip"}, {"title": "Prolog — PySwip 0.3.2 documentation", "url": "https://pyswip.readthedocs.io/en/latest/api/prolog.html"}, {"title": "Home | PySwip", "url": "https://pyswip.org/"}, {"title": "pyswip/examples/register_foreign.py at master · yuce/pyswip", "url": "https://github.com/yuce/pyswip/blob/master/examples/register_foreign.py"}]}, "I've successfully retrieved search results for PySwip, the SWI-Prolog Python bridge. Here's a summary of what I found:\n\n## Overview\n\nPySwip is a Python-Prolog interface that enables querying SWI-Prolog in your Python programs and features an SWI-Prolog foreign language interface, a utility class that makes it easy querying with Prolog and also a Pythonic interface.\n\n## API Query Examples\n\n### Basic Query Interface\nUsing the Prolog class, you can assert facts and query them, such as asserting `father(michael,john)` and `father(michael,gina)`, then querying with `Prolog.query(\"father(michael,X)\")` to get results like `[{'X': 'john'}, {'X': 'gina'}]`.\n\n### Foreign Functions\nPySwip allows registering Python functions as Prolog predicates through the `registerForeign` function, enabling you to combine Python functions with Prolog queries.\n\n### Pythonic Interface\nThe library provides a Pythonic interface using `Functor`, `Variable`, and `Query` classes, where you can define predicates like `father = Functor(\"father\", 2)`, create variables with `X = Variable()`, and iterate through solutions with `q.nextSolution()`.\n\n## Recent Updates\n\nAs of 2024, PySwip includes enhanced functionality for registering foreign predicates with parameters for name, arity, module designation, and nondeterministic behavior.\n\nThe search results also reference the official PySwip documentation and multiple GitHub repositories with example implementations."], "durationSeconds": 6.840504967000001, "searchCount": 1}
```

### [9] TOOL RESULT — WebSearch · 2026-06-07 04:54:32 UTC

```
{"query": "FOLIO dataset yale-nlp HuggingFace NL-to-FOL format structure", "results": [{"tool_use_id": "srvtoolu_01QsfTMMMQZHrqLyGFcT8Uad", "content": [{"title": "yale-nlp/FOLIO · Datasets at Hugging Face", "url": "https://huggingface.co/datasets/yale-nlp/FOLIO"}, {"title": "LINC: A Neurosymbolic Approach for Logical Reasoning by Combining   Language Models with First-Order Logic Provers", "url": "https://arxiv.org/pdf/2310.15164"}, {"title": "FOLIO: Natural Language Reasoning with First-Order Logic", "url": "https://arxiv.org/pdf/2209.00840"}, {"title": "yale-nlp/P-FOLIO · Datasets at Hugging Face", "url": "https://huggingface.co/datasets/yale-nlp/P-FOLIO"}, {"title": "[2209.00840] FOLIO: Natural Language Reasoning with First-Order Logic", "url": "https://arxiv.org/abs/2209.00840"}, {"title": "GitHub - Yale-LILY/FOLIO · GitHub", "url": "https://github.com/Yale-LILY/FOLIO"}, {"title": "yale-nlp/P-FOLIO at main", "url": "https://huggingface.co/datasets/yale-nlp/P-FOLIO/tree/main"}, {"title": "FOLIO: Natural Language Reasoning with First-Order Logic", "url": "https://arxiv.org/html/2209.00840v2"}, {"title": "FOLIO: Natural Language Reasoning with First-Order Logic", "url": "https://aclanthology.org/2024.emnlp-main.1229.pdf"}, {"title": "Paper page - FOLIO: Natural Language Reasoning with First-Order Logic", "url": "https://huggingface.co/papers/2209.00840"}]}, "Based on the search results, here's what I found about the FOLIO dataset:\n\n## Overview\n\nFOLIO is an expert-written, open-domain, logically complex and diverse dataset for natural language reasoning with first-order logic. FOLIO consists of 1,430 examples (unique conclusions), each paired with one of 487 sets of premises used to deductively reason for the validity of each conclusion.\n\n## NL-to-FOL Format\n\nA new NL-FOL translation task was proposed where an NL reasoning example is translated into its FOL counterpart. The logical correctness of the premises and conclusions is ensured by their FOL annotations, which are automatically verified by an FOL inference engine.\n\n## Dataset Structure\n\nThe repo contains the train and validation sets in the format of jsonl and txt, with each line in a file containing one example consisting of the following fields. The dataset is available on both GitHub at Yale-LILY/FOLIO and on HuggingFace under yale-nlp/FOLIO.\n\n## Purpose\n\nFormal logic, i.e., FOL is used to ensure the logical validity of the examples written in NL and propose a new NL-FOL translation task. The dataset enables evaluation of language models' logical reasoning capabilities through both natural language reasoning tasks and NL-FOL translation benchmarks."], "durationSeconds": 8.930263673999997, "searchCount": 1}
```

### [10] TOOL CALL — WebFetch · 2026-06-07 04:54:36 UTC

```
URL: https://huggingface.co/datasets/yale-nlp/FOLIO
Prompt: What are the exact fields in each example? Show the data schema including premises, conclusions, label fields, and FOL annotations.
```

### [11] TOOL CALL — WebFetch · 2026-06-07 04:54:37 UTC

```
URL: https://pyswip.readthedocs.io/en/latest/api/prolog.html
Prompt: Show the main Prolog class API: query, assertz, retract methods and their signatures. Any exception handling examples.
```

### [12] TOOL RESULT — WebFetch · 2026-06-07 04:54:37 UTC

```
{"bytes": 144947, "code": 200, "codeText": "OK", "result": "Based on the web page provided, I cannot provide the specific data schema details you're requesting. The page shows that the FOLIO dataset exists on Hugging Face, but the \"README.md exists but content is empty,\" and access requires agreeing to share contact information.\n\nTo obtain information about the exact fields (premises, conclusions, labels, and FOL annotations), you would need to:\n\n1. Log in or sign up on Hugging Face\n2. Accept the dataset's access conditions\n3. Review the dataset card and documentation directly\n\nThe webpage does not contain the structural details of individual examples or field specifications needed to answer your question comprehensively.", "durationMs": 2909, "url": "https://huggingface.co/datasets/yale-nlp/FOLIO"}
```

### [13] TOOL RESULT — WebFetch · 2026-06-07 04:54:43 UTC

````
{"bytes": 10734, "code": 200, "codeText": "OK", "result": "# PySwip Prolog Class API Overview\n\n## Main Methods\n\n### query()\n```python\nclassmethod query(format, *args, maxresult=-1, catcherrors=True, normalize=True)\n```\nExecutes a Prolog query and yields results. Returns a generator producing dictionaries with variables as keys, or an empty dict `{}` for yes/no questions.\n\n**Example:**\n```python\nProlog.assertz(\"father(michael,john)\")\nprint(list(Prolog.query(\"father(michael,X)\")))\n# Output: [{'X': 'john'}]\n```\n\n### assertz()\n```python\nclassmethod assertz(format, *args, catcherrors=False)\n```\n\"Asserts the clause as the last clause of the predicate.\" Supports placeholder replacement with `%p`.\n\n**Example:**\n```python\nProlog.assertz(\"big(airplane)\")\nnums = list(range(5))\nProlog.assertz(\"numbers(%p)\", nums)\n```\n\n### retract()\n```python\nclassmethod retract(format, *args, catcherrors=False)\n```\nEliminates a single matching fact or clause from the database. Uses placeholder-based formatting.\n\n**Example:**\n```python\nProlog.retract(\"person(jane)\")\n```\n\n## Exception Handling\n\n**NestedQueryError** – Raised when attempting nested queries, though PySwip handles this automatically.\n\n**PrologError** – General exception for Prolog-related errors.\n\nBoth `asserta()` and `assertz()` include a `catcherrors` parameter to suppress exceptions during execution when set to `True`.", "durationMs": 5877, "url": "https://pyswip.readthedocs.io/en/latest/api/prolog.html"}
````

### [14] TOOL CALL — WebFetch · 2026-06-07 04:54:47 UTC

```
URL: https://arxiv.org/abs/2209.00840
Prompt: What are the exact fields in each FOLIO example? Specifically: premises format, conclusion format, gold label (True/False/Unknown), and FOL annotation format.
```

### [15] TOOL CALL — WebSearch · 2026-06-07 04:54:47 UTC

```
FOLIO dataset jsonl fields story premises conclusion label FOL annotation example format
```

### [16] TOOL RESULT — WebFetch · 2026-06-07 04:54:49 UTC

```
{"bytes": 51021, "code": 200, "codeText": "OK", "result": "# FOLIO Dataset Structure\n\nBased on the provided content, the abstract does not specify the exact field formats for individual examples. The document states that FOLIO consists of:\n\n- **1,430 examples** (unique conclusions)\n- **487 sets of premises** paired with conclusions\n- **FOL annotations** that \"are automatically verified by an FOL inference engine\"\n\nHowever, the webpage excerpt does not detail the specific structure of premises format, conclusion format, gold label categories, or FOL annotation format for each example. To obtain those precise specifications, you would need to access the full paper PDF or the dataset documentation directly.", "durationMs": 3011, "url": "https://arxiv.org/abs/2209.00840"}
```

### [17] TOOL RESULT — WebSearch · 2026-06-07 04:54:59 UTC

```
{"query": "FOLIO dataset jsonl fields story premises conclusion label FOL annotation example format", "results": [{"tool_use_id": "srvtoolu_01UBdUtGiKCaKA3xHiLZKoZK", "content": [{"title": "FOLIO: Natural Language Reasoning with First-Order Logic", "url": "https://arxiv.org/html/2209.00840v2"}, {"title": "[2209.00840] FOLIO: Natural Language Reasoning with First-Order Logic", "url": "https://ar5iv.labs.arxiv.org/html/2209.00840"}, {"title": "FOLIO/data/v0.0/folio-validation.jsonl at main · Yale-LILY/FOLIO", "url": "https://github.com/Yale-LILY/FOLIO/blob/main/data/v0.0/folio-validation.jsonl"}, {"title": "[2209.00840] FOLIO: Natural Language Reasoning with First-Order Logic", "url": "https://arxiv.org/abs/2209.00840"}, {"title": "FOLIO: Natural Language Reasoning with First-Order Logic | AI Research Paper Details", "url": "https://www.aimodels.fyi/papers/arxiv/folio-natural-language-reasoning-first-order-logic"}, {"title": "FOLIO: Natural Language Reasoning with First-Order Logic", "url": "https://aclanthology.org/2024.emnlp-main.1229.pdf"}, {"title": "FOLIO: Natural Language Reasoning with First-Order Logic Simeng Han1", "url": "https://arxiv.org/pdf/2209.00840"}, {"title": "FOLIO: Natural Language Reasoning with First-Order Logic - ACL Anthology", "url": "https://aclanthology.org/2024.emnlp-main.1229/"}, {"title": "Consistency-Guided Decoding with Proof-Driven Disambiguation for Three-Way Logical Question Answering", "url": "https://arxiv.org/pdf/2604.06196"}, {"title": "Do LLMs Really Struggle at NL-FOL Translation? Revealing their Strengths via a Novel Benchmarking Strategy", "url": "https://arxiv.org/pdf/2511.11816"}]}, "Based on the search results, here's information about the FOLIO dataset's JSONL format and fields:\n\n## Dataset Structure\n\nFOLIO consists of 1,430 examples (unique conclusions), each paired with one of 487 sets of premises used to deductively reason for the validity of each conclusion.\n\n## JSONL Fields and Format\n\nThe FOLIO dataset in JSONL format contains the following key fields:\n\n1. **\"premises\"** - A list of NL (natural language) premises\n2. **\"premises-FOL\"** - Their respective FOL translations\n3. **\"conclusion\"** - A phrase representing the conclusion\n4. **\"conclusion-FOL\"** - The formal logic representation of the conclusion\n5. **\"label\"** - Labeled with 'true', 'false', or 'unknown', depending on whether the conclusion is implied by the story, in contradiction with it, or cannot be resolved from the premises\n\n## Example Format\n\nFrom the actual JSONL file, an example entry contains:\n\n\"premises\" as an array of natural language statements, \"premises-FOL\" as an array of first-order logic formulas, \"conclusion\" as a natural language statement, \"conclusion-FOL\" as a FOL formula (e.g., \"Engaged(bonnie)\"), and \"label\" with values like \"Uncertain\"\n\n## Annotation Details\n\nAll reasoning examples are annotated with first-order logic (FOL) formulas to ensure conclusions follow premises logically, with logical correctness verified by an FOL inference engine."], "durationSeconds": 6.947206329999994, "searchCount": 1}
```

### [18] ASSISTANT · 2026-06-07 04:58:25 UTC

```
Now I have enough information to write the detailed plan.
```

### [19] TOOL CALL — Write · 2026-06-07 04:58:25 UTC

```
File: /ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_plan/gen_plan_experiment_1/.terminal_claude_agent_struct_out.json

{
  "title": "Dual-Signal SBFL Pipeline for NL-to-FOL Prolog KB Repair on FOLIO",
  "summary": "Implement and evaluate the dual-signal SBFL pipeline (Ochiai + sub-goal harvesting) for repairing LLM-extracted Prolog KBs on FOLIO, comparing against one-shot, self-refine, flat-SBFL, and CoT baselines. Output method_out.json with per-example predictions, metrics, and ablation results.",
  "runpod_compute_profile": "cpu_heavy",
  "implementation_pseudocode": "# ============================================================\n# SETUP AND DEPENDENCIES\n# ============================================================\n# Install: uv pip install pyswip datasets openai tqdm scipy numpy pandas\n# Requires SWI-Prolog installed system-wide: apt-get install swi-prolog\n# OpenRouter API key in env: OPENROUTER_API_KEY\n\n# ============================================================\n# STEP 0: CONSTANTS AND CONFIGURATION\n# ============================================================\nMODEL = 'meta-llama/llama-3.1-8b-instruct'  # cheap default; fallback: google/gemma-2-9b-it\nFALLBACK_MODEL = 'anthropic/claude-3-haiku'   # if llama errors, switch\nMAX_BUDGET_USD = 8.0  # hard stop\nMAX_EXAMPLES = 150    # start with 50 for validation\nN_ORACLE_QUERIES = 15  # LLM call 2 generates this many yes/no queries\nN_REPAIR_ROUNDS = 3\nK_REPAIR_TARGETS = 5\nCOST_TRACKER = {'total': 0.0}  # mutable dict for cumulative cost tracking\n\nOPENROUTER_BASE = 'https://openrouter.ai/api/v1'\n\n# ============================================================\n# STEP 1: LOAD FOLIO DATASET\n# ============================================================\n# Use HuggingFace datasets library:\n#   from datasets import load_dataset\n#   ds = load_dataset('yale-nlp/FOLIO', split='validation')\n# Each example has fields:\n#   'story': str (premises as a single block of text, newline-separated)\n#   'premises': list[str] (individual premise sentences)\n#   'premises-FOL': list[str] (FOL translations of premises)\n#   'conclusion': str\n#   'conclusion-FOL': str\n#   'label': str  -- 'True', 'False', or 'Uncertain'\n# NOTE: If the dataset field is 'story' instead of 'premises', join premise list.\n# NOTE: Some FOLIO versions use 'Uncertain' or 'Unknown' for the third label.\n# Take first 50 examples for smoke-test, then up to 150.\n\n# ============================================================\n# STEP 2: LLM HELPER FUNCTION\n# ============================================================\ndef llm_call(prompt_messages, model=MODEL, max_tokens=1024):\n    \"\"\"Call OpenRouter API. Return (text, cost_usd). Update COST_TRACKER.\"\"\"\n    # POST to https://openrouter.ai/api/v1/chat/completions\n    # Headers: Authorization: Bearer {OPENROUTER_API_KEY}\n    # Body: {model, messages, max_tokens}\n    # Response: choices[0].message.content, usage.prompt_tokens, usage.completion_tokens\n    # Cost estimation per model (approx, update from OpenRouter pricing page):\n    #   llama-3.1-8b-instruct: ~$0.055/M input, $0.055/M output\n    #   claude-3-haiku: ~$0.25/M input, $1.25/M output\n    # After every call: COST_TRACKER['total'] += estimated_cost\n    # If COST_TRACKER['total'] >= MAX_BUDGET_USD: raise BudgetExceededError\n    pass\n\n# ============================================================\n# STEP 3: KB EXTRACTION (LLM Call 1)\n# ============================================================\nEXTRACTION_PROMPT = \"\"\"\nYou are a Prolog knowledge base extractor. Given the following natural language premises,\nextract a Prolog knowledge base (facts and rules) that captures all stated relationships.\n\nRules:\n- Use snake_case predicate names (e.g., is_happy/1, parent_of/2)\n- Each fact/rule on its own line ending with '.'\n- No comments, no module declarations\n- Use only alphanumeric and underscore characters in predicate/atom names\n- Predicates must be Prolog-valid (no spaces, no special chars)\n- Output ONLY the Prolog code, nothing else\n\nPremises:\n{premises}\n\nProlog KB:\"\"\"\n\ndef extract_kb(premises_text):\n    \"\"\"Returns list of Prolog clause strings.\"\"\"\n    response, cost = llm_call([{'role': 'user', 'content': EXTRACTION_PROMPT.format(premises=premises_text)}])\n    # Parse: split by newlines, strip, filter lines ending with '.', filter empty\n    clauses = [line.strip() for line in response.split('\\n') if line.strip().endswith('.')]\n    return clauses\n\n# ============================================================\n# STEP 4: ORACLE QUERY GENERATION (LLM Call 2)\n# ============================================================\nORACLE_PROMPT = \"\"\"\nGiven these natural language premises, generate {n} yes/no reasoning questions\nthat test whether specific facts from the premises hold.\nGenerate a mix: roughly half should be answerable 'yes', half 'no' or 'unknown'.\nFormat: one question per line, starting with 'Q: '.\n\nPremises:\n{premises}\n\nQuestions:\"\"\"\n\ndef generate_oracle_queries(premises_text, n=N_ORACLE_QUERIES):\n    \"\"\"Returns list of NL yes/no question strings.\"\"\"\n    response, cost = llm_call([{'role': 'user', 'content': ORACLE_PROMPT.format(premises=premises_text, n=n)}])\n    lines = [l.strip().lstrip('Q:').strip() for l in response.split('\\n') if l.strip().startswith('Q:')]\n    return lines[:n]\n\n# ============================================================\n# STEP 5: ORACLE ANSWER GENERATION (Part of LLM Call 2)\n# ============================================================\nANSWER_PROMPT = \"\"\"\nBased on the following premises, answer each question with 'yes', 'no', or 'unknown'.\nProvide ONLY 'yes', 'no', or 'unknown' for each question, one per line.\n\nPremises:\n{premises}\n\nQuestions and answers:\n{questions}\n\nAnswers (one per line, only 'yes'/'no'/'unknown'):\"\"\"\n\ndef generate_oracle_answers(premises_text, questions):\n    \"\"\"Returns list of 'yes'/'no'/'unknown' strings matching questions.\"\"\"\n    q_block = '\\n'.join(f'{i+1}. {q}' for i, q in enumerate(questions))\n    response, cost = llm_call([{'role': 'user', 'content': ANSWER_PROMPT.format(premises=premises_text, questions=q_block)}])\n    answers = [l.strip().lower() for l in response.split('\\n') if l.strip().lower() in ('yes', 'no', 'unknown')]\n    # Pad or truncate to match questions length\n    while len(answers) < len(questions): answers.append('unknown')\n    return answers[:len(questions)]\n\n# ============================================================\n# STEP 6: NL QUERY TO PROLOG GOAL CONVERSION (LLM Call 2b, cheap)\n# ============================================================\nNL_TO_PROLOG_PROMPT = \"\"\"\nGiven this Prolog knowledge base and a natural language yes/no question,\nwrite a Prolog query goal (without '?-', just the goal expression) that answers the question.\nIf the question asks 'Is X happy?', write: is_happy(x).\nIf not answerable as Prolog query, write: fail.\nOutput ONLY the Prolog goal, nothing else.\n\nKB predicates available:\n{predicate_signatures}\n\nQuestion: {question}\nProlog goal:\"\"\"\n\ndef nl_query_to_prolog(question, predicate_sigs):\n    \"\"\"Convert NL question to Prolog goal string.\"\"\"\n    sig_block = '\\n'.join(predicate_sigs)\n    response, cost = llm_call([{'role': 'user', 'content': NL_TO_PROLOG_PROMPT.format(\n        predicate_signatures=sig_block, question=question)}], max_tokens=100)\n    goal = response.strip().rstrip('.').strip()\n    return goal if goal else 'fail'\n\n# ============================================================\n# STEP 7: PYSWIP META-INTERPRETER WITH STRATIFIED COVERAGE\n# ============================================================\n# pyswip Prolog class is NOT thread-safe; use a single Prolog instance per process.\n# IMPORTANT: Always retract all user-defined clauses between examples to reset state.\n#\n# Predicate outcome enum:\n#   NOT_ACCESSED = 0\n#   UNIFIED_OK_SUBGOAL_FAILED = 1  (indirect fault)\n#   UNIFY_FAILED = 2               (direct fault)\n#   SUCCEEDED = 3\n\ndef load_kb_into_prolog(prolog_instance, clauses):\n    \"\"\"Assert all clauses. Returns list of successfully asserted clauses.\"\"\"\n    ok_clauses = []\n    for clause in clauses:\n        try:\n            prolog_instance.assertz(clause.rstrip('.'))\n            ok_clauses.append(clause)\n        except Exception as e:\n            pass  # skip malformed clauses\n    return ok_clauses\n\ndef clear_kb(prolog_instance, predicate_names):\n    \"\"\"Retract all user-defined predicates to reset state between examples.\"\"\"\n    for pred_name, arity in predicate_names:\n        try:\n            list(prolog_instance.query(f'abolish({pred_name}/{arity})'))\n        except:\n            pass\n\ndef extract_predicate_names_from_clauses(clauses):\n    \"\"\"Parse clauses to extract (name, arity) pairs using regex.\n    Pattern: match head of clause before ':-' or end.\n    E.g., 'happy(john).' -> ('happy', 1)\n          'parent(X,Y) :- father(X,Y).' -> ('parent', 2)\n    \"\"\"\n    import re\n    preds = set()\n    for clause in clauses:\n        head = clause.split(':-')[0].strip().rstrip('.')\n        m = re.match(r'([a-z][a-zA-Z0-9_]*)\\((.*)\\)', head)\n        if m:\n            name = m.group(1)\n            args = m.group(2)\n            arity = len(args.split(',')) if args.strip() else 0\n            preds.add((name, arity))\n        elif re.match(r'[a-z][a-zA-Z0-9_]*$', head):\n            preds.add((head, 0))\n    return list(preds)\n\n# Stratified coverage tracking via Prolog meta-interpreter\n# Use a Python-side tracer approach:\n# 1. Enable Prolog trace/debugging hooks via assert of meta-level spy points\n# 2. OR: use a simpler approach -- wrap pyswip query with try/except and\n#    post-hoc analyze which predicates appear in the KB vs which succeeded.\n#\n# SIMPLER APPROACH (recommended for reliability):\n# For each oracle query, attempt Prolog proof:\n#   - If query succeeds: all accessed predicates marked SUCCEEDED\n#   - If query fails: we need to identify WHERE it failed\n#     Strategy: iteratively probe sub-goals\n#       * For each predicate P in KB, test P(...) with appropriate bindings\n#       * Record which predicates can be proved at all (sanity check)\n#     Better: use Prolog's exception mechanism + a custom meta-interpreter\n#\n# META-INTERPRETER APPROACH (more robust):\n# Assert a solve/2 meta-interpreter in Prolog that tracks failures:\n#\nMETA_INTERPRETER_PROLOG = \"\"\"\n:- dynamic coverage_log/3.  % coverage_log(Pred, Arity, Outcome)\n:- dynamic failed_subgoal/1.  % failed_subgoal(Goal)\n\nreset_coverage :-\n    retractall(coverage_log(_, _, _)),\n    retractall(failed_subgoal(_)).\n\n% Meta-interpreter solve(Goal, Depth)\nsolve(true, _) :- !.\nsolve((A, B), D) :- !, solve(A, D), solve(B, D).\nsolve((A ; B), D) :- !, (solve(A, D) ; solve(B, D)).\nsolve(\\+(A), D) :- !, (solve(A, D) -> fail ; true).\nsolve(Goal, D) :-\n    D > 0,\n    functor(Goal, Name, Arity),\n    % Try to find a matching clause\n    (clause(Goal, Body) ->\n        D1 is D - 1,\n        (solve(Body, D1) ->\n            assert(coverage_log(Name, Arity, succeeded))\n        ;\n            assert(coverage_log(Name, Arity, subgoal_failed)),\n            assert(failed_subgoal(Body)),\n            fail\n        )\n    ;\n        % No matching clause - direct unification failure\n        assert(coverage_log(Name, Arity, unify_failed)),\n        assert(failed_subgoal(Goal)),\n        fail\n    ).\nsolve(Goal, 0) :-\n    functor(Goal, Name, Arity),\n    assert(coverage_log(Name, Arity, depth_exceeded)),\n    fail.\n\"\"\"\n# NOTE: The meta-interpreter above is an approximation. Adjust depth limit (start with 10).\n# In practice, the executor should test this meta-interpreter carefully on simple examples first.\n\ndef run_oracle_query_with_coverage(prolog, goal_str, depth=10):\n    \"\"\"\n    Run goal_str through meta-interpreter. Returns:\n    {\n        'succeeded': bool,\n        'coverage': dict of {pred_name: outcome_list},  # outcome in {succeeded, subgoal_failed, unify_failed}\n        'failed_subgoals': list of goal strings\n    }\n    \"\"\"\n    try:\n        list(prolog.query('reset_coverage'))\n    except:\n        pass\n    succeeded = False\n    try:\n        results = list(prolog.query(f'solve({goal_str}, {depth})'))\n        succeeded = len(results) > 0\n    except Exception as e:\n        pass\n    # Read coverage log\n    coverage = {}\n    try:\n        for sol in prolog.query('coverage_log(N, A, O)'):\n            key = f\"{sol['N']}/{sol['A']}\"\n            if key not in coverage: coverage[key] = []\n            coverage[key].append(str(sol['O']))\n    except:\n        pass\n    failed_subgoals = []\n    try:\n        for sol in prolog.query('failed_subgoal(G)'):\n            failed_subgoals.append(str(sol['G']))\n    except:\n        pass\n    return {'succeeded': succeeded, 'coverage': coverage, 'failed_subgoals': failed_subgoals}\n\n# ============================================================\n# STEP 8: OCHIAI SCORE COMPUTATION\n# ============================================================\ndef compute_ochiai_scores(coverage_matrix, pass_fail):\n    \"\"\"\n    coverage_matrix: dict {pred_key: list of per-query outcomes}\n      outcome: 'succeeded' = accessed+unified, 'unify_failed' = direct fault,\n               'subgoal_failed' = indirect fault, None = not accessed\n    pass_fail: list of bool (True=oracle says yes/pass, False=no/fail)\n    \n    For Ochiai, treat UNIFY_FAILED as 'covered in failing test' signal.\n    Binary coverage for Ochiai: pred accessed in query i = 1 if outcome != None\n    \n    Ochiai(p) = ef / sqrt((ef+nf) * (ef+ep))\n      ef = failing tests where predicate accessed\n      ep = passing tests where predicate accessed\n      nf = failing tests where predicate NOT accessed\n    \n    Returns dict {pred_key: ochiai_score} for predicates with unify_failed outcome.\n    \"\"\"\n    import math\n    scores = {}\n    n_queries = len(pass_fail)\n    n_failing = sum(1 for p in pass_fail if not p)\n    \n    for pred_key, outcomes in coverage_matrix.items():\n        # Only score direct-fault candidates (unify_failed at least once)\n        if not any(o == 'unify_failed' for o in outcomes):\n            continue\n        ef = sum(1 for i, o in enumerate(outcomes) if o is not None and not pass_fail[i])\n        ep = sum(1 for i, o in enumerate(outcomes) if o is not None and pass_fail[i])\n        nf = n_failing - ef\n        denom = math.sqrt((ef + nf) * (ef + ep))\n        scores[pred_key] = ef / denom if denom > 0 else 0.0\n    return scores\n\ndef compute_missing_predicate_scores(all_failed_subgoals, kb_pred_keys):\n    \"\"\"\n    Count frequency of failed subgoals that are NOT in the KB.\n    Returns dict {subgoal_str: frequency} for truly missing predicates.\n    \"\"\"\n    from collections import Counter\n    counts = Counter()\n    for subgoal in all_failed_subgoals:\n        # Extract predicate name from subgoal string\n        import re\n        m = re.match(r'([a-z][a-zA-Z0-9_]*).*', subgoal)\n        if m:\n            pred_name = m.group(1)\n            # Check if it appears in KB\n            in_kb = any(k.split('/')[0] == pred_name for k in kb_pred_keys)\n            if not in_kb:\n                counts[subgoal] += 1\n    return dict(counts)\n\n# ============================================================\n# STEP 9: REPAIR AGENDA CONSTRUCTION AND LLM REPAIR (LLM Call 3)\n# ============================================================\nREPAIR_PROMPT = \"\"\"\nYou are repairing a Prolog knowledge base extracted from natural language.\nThe following Prolog predicate/sub-goal is suspected to be erroneous or missing.\n\nOriginal premises text:\n{premises}\n\nCurrent Prolog KB:\n{kb}\n\nSuspicious item: {item}\nReason: {reason}\n\nPlease provide the corrected or new Prolog clause(s) that should replace or supplement this item.\nOutput ONLY valid Prolog clause(s), one per line, ending with '.'\nDo not include explanations.\"\"\"\n\ndef repair_item(premises_text, kb_clauses, item, reason):\n    \"\"\"LLM repairs a suspicious predicate or adds a missing one.\n    Returns list of new/replacement clause strings.\"\"\"\n    kb_str = '\\n'.join(kb_clauses)\n    response, cost = llm_call([{'role': 'user', 'content': REPAIR_PROMPT.format(\n        premises=premises_text, kb=kb_str, item=item, reason=reason)}])\n    new_clauses = [l.strip() for l in response.split('\\n') if l.strip().endswith('.')]\n    return new_clauses\n\ndef build_repair_agenda(ochiai_scores, missing_scores, k=K_REPAIR_TARGETS):\n    \"\"\"Merge Ochiai and missing-predicate scores into top-K repair agenda.\"\"\"\n    # Normalize both score sets to [0,1] and interleave\n    max_ochiai = max(ochiai_scores.values()) if ochiai_scores else 1.0\n    max_missing = max(missing_scores.values()) if missing_scores else 1.0\n    agenda = []\n    for pred, score in ochiai_scores.items():\n        agenda.append({'item': pred, 'score': score / max_ochiai, 'type': 'erroneous'})\n    for subgoal, freq in missing_scores.items():\n        agenda.append({'item': subgoal, 'score': freq / max_missing, 'type': 'missing'})\n    agenda.sort(key=lambda x: x['score'], reverse=True)\n    return agenda[:k]\n\n# ============================================================\n# STEP 10: MAIN PIPELINE PER EXAMPLE\n# ============================================================\ndef run_dual_sbfl_pipeline(example, prolog, pred_key_index):\n    \"\"\"\n    Returns dict with: predicted_label, num_llm_calls, num_repairs_applied,\n    ochiai_scores_top5, subgoal_harvest_top5, kb_after_repair\n    \"\"\"\n    premises_text = '\\n'.join(example['premises'])\n    gold_label = example['label']  # 'True', 'False', 'Uncertain'\n    \n    # --- LLM Call 1: Extract KB ---\n    clauses = extract_kb(premises_text)  # ~1 call\n    \n    # --- LLM Call 2: Generate oracle queries + answers ---\n    oracle_questions = generate_oracle_queries(premises_text)  # ~1 call\n    oracle_answers = generate_oracle_answers(premises_text, oracle_questions)  # ~1 call\n    # oracle_answers[i] in {'yes', 'no', 'unknown'}\n    # pass_fail[i] = True if oracle_answers[i] == 'yes', False otherwise\n    pass_fail = [a == 'yes' for a in oracle_answers]\n    \n    # Convert oracle questions to Prolog goals (batch in one LLM call if possible)\n    pred_sigs = [f'{n}/{a}' for n, a in extract_predicate_names_from_clauses(clauses)]\n    prolog_goals = [nl_query_to_prolog(q, pred_sigs) for q in oracle_questions]  # up to N calls\n    \n    # --- SBFL Repair Loop ---\n    current_clauses = clauses[:]\n    all_repairs = []\n    \n    for round_idx in range(N_REPAIR_ROUNDS):\n        if COST_TRACKER['total'] >= MAX_BUDGET_USD:\n            break\n        \n        # Load KB\n        pred_names = extract_predicate_names_from_clauses(current_clauses)\n        clear_kb(prolog, pred_key_index.get('all_preds', []))\n        loaded = load_kb_into_prolog(prolog, current_clauses)\n        pred_key_index['all_preds'] = pred_names\n        \n        # Run each oracle query through meta-interpreter\n        coverage_matrix = {}  # {pred_key: [outcome_per_query]}\n        all_failed_subgoals = []\n        n_queries = len(prolog_goals)\n        for i, goal in enumerate(prolog_goals):\n            if goal == 'fail':\n                continue\n            result = run_oracle_query_with_coverage(prolog, goal)\n            all_failed_subgoals.extend(result['failed_subgoals'])\n            for pred_key, outcomes in result['coverage'].items():\n                if pred_key not in coverage_matrix:\n                    coverage_matrix[pred_key] = [None] * n_queries\n                # Take worst (most suspicious) outcome for this query\n                worst = outcomes[-1] if outcomes else None\n                coverage_matrix[pred_key][i] = worst\n        \n        # Fill None for unaccessed preds\n        for pred_key in coverage_matrix:\n            coverage_matrix[pred_key] = [o if o else None for o in coverage_matrix[pred_key]]\n        \n        # Compute Ochiai scores (direct-fault predicates)\n        ochiai = compute_ochiai_scores(coverage_matrix, pass_fail)\n        # Compute missing predicate scores\n        kb_pred_keys = set(coverage_matrix.keys())\n        missing = compute_missing_predicate_scores(all_failed_subgoals, kb_pred_keys)\n        \n        # Build repair agenda\n        agenda = build_repair_agenda(ochiai, missing, k=K_REPAIR_TARGETS)\n        if not agenda:\n            break  # nothing to repair\n        \n        # LLM Call 3: Repair each item in agenda\n        for item_info in agenda:\n            if COST_TRACKER['total'] >= MAX_BUDGET_USD:\n                break\n            new_clauses = repair_item(\n                premises_text, current_clauses,\n                item_info['item'],\n                f\"Type: {item_info['type']}, Score: {item_info['score']:.3f}\"\n            )\n            if new_clauses:\n                # For erroneous: try to find and replace matching predicate clauses\n                if item_info['type'] == 'erroneous':\n                    pred_name = item_info['item'].split('/')[0]\n                    current_clauses = [c for c in current_clauses\n                                       if not c.split('(')[0].strip() == pred_name]\n                current_clauses.extend(new_clauses)\n                all_repairs.append({'item': item_info['item'], 'type': item_info['type'],\n                                    'new_clauses': new_clauses, 'round': round_idx})\n    \n    # --- Final evaluation: run gold conclusion as Prolog query ---\n    pred_names = extract_predicate_names_from_clauses(current_clauses)\n    clear_kb(prolog, pred_key_index.get('all_preds', []))\n    load_kb_into_prolog(prolog, current_clauses)\n    \n    conclusion_fol = example.get('conclusion-FOL', '')\n    predicted_label = evaluate_conclusion(prolog, conclusion_fol, current_clauses, premises_text)\n    \n    return {\n        'predicted_label': predicted_label,\n        'num_llm_calls': None,  # filled by caller tracking\n        'num_repairs_applied': len(all_repairs),\n        'ochiai_scores_top5': sorted([(k,v) for k,v in ochiai.items()], key=lambda x:-x[1])[:5] if 'ochiai' in dir() else [],\n        'subgoal_harvest_top5': sorted([(k,v) for k,v in missing.items()], key=lambda x:-x[1])[:5] if 'missing' in dir() else [],\n        'repairs': all_repairs\n    }\n\n# ============================================================\n# STEP 11: CONCLUSION EVALUATION\n# ============================================================\nCONCLUSION_EVAL_PROMPT = \"\"\"\nGiven this Prolog knowledge base and this conclusion in first-order logic notation:\n{conclusion_fol}\n\nConvert the conclusion to a Prolog query goal (without '?-'). Output only the Prolog goal.\nIf it cannot be converted, output: fail\"\"\"\n\ndef evaluate_conclusion(prolog, conclusion_fol, kb_clauses, premises_text):\n    \"\"\"\n    Try to evaluate conclusion_fol against loaded KB.\n    Returns 'True', 'False', or 'Uncertain'.\n    Strategy:\n    1. LLM converts conclusion_fol to Prolog goal (if not already Prolog-like)\n    2. Try to prove goal via Prolog query\n    3. Try to prove negation of goal\n    4. If neither succeeds: 'Uncertain'\n    \"\"\"\n    # If conclusion_fol already looks like Prolog (no quantifiers, no =>)\n    # try direct query; else use LLM to convert\n    import re\n    goal = conclusion_fol.strip().rstrip('.')\n    if any(tok in goal for tok in ['∀', '∃', '→', '⟹', '=>', 'forall', 'exists']):\n        # Need LLM conversion\n        kb_str = '\\n'.join(kb_clauses[:20])  # truncate for prompt\n        response, _ = llm_call([{'role': 'user', 'content': CONCLUSION_EVAL_PROMPT.format(\n            conclusion_fol=conclusion_fol) + f'\\nKB (excerpt):\\n{kb_str}'}], max_tokens=100)\n        goal = response.strip().rstrip('.')\n    \n    if not goal or goal == 'fail':\n        return 'Uncertain'\n    \n    try:\n        results = list(prolog.query(goal))\n        if results:  # proved\n            # Now try negation\n            try:\n                neg_results = list(prolog.query(f'\\\\+({goal})'))\n                if neg_results:\n                    return 'False'  # negation also provable - contradiction\n            except:\n                pass\n            return 'True'\n        else:\n            # Failed - try negation as failure\n            try:\n                neg_results = list(prolog.query(f'\\\\+({goal})'))\n                if neg_results:\n                    return 'False'\n            except:\n                pass\n            return 'Uncertain'\n    except Exception:\n        return 'Uncertain'\n\n# ============================================================\n# STEP 12: BASELINES\n# ============================================================\ndef run_oneshot_baseline(example, prolog, pred_key_index):\n    \"\"\"One-shot: extract KB, query conclusion directly, no repair.\"\"\"\n    premises_text = '\\n'.join(example['premises'])\n    clauses = extract_kb(premises_text)\n    pred_names = extract_predicate_names_from_clauses(clauses)\n    clear_kb(prolog, pred_key_index.get('all_preds', []))\n    load_kb_into_prolog(prolog, clauses)\n    pred_key_index['all_preds'] = pred_names\n    predicted = evaluate_conclusion(prolog, example.get('conclusion-FOL',''), clauses, premises_text)\n    return {'predicted_label': predicted, 'num_llm_calls': 2, 'method': 'oneshot'}\n\ndef run_cot_baseline(example):\n    \"\"\"Chain-of-thought: single LLM call with reasoning prompt.\"\"\"\n    premises_text = '\\n'.join(example['premises'])\n    conclusion = example['conclusion']\n    prompt = f\"\"\"Given the following premises:\\n{premises_text}\\n\\nConclusion: {conclusion}\\n\nThink step by step. Then answer with exactly one word: True, False, or Uncertain.\"\"\"\n    response, _ = llm_call([{'role': 'user', 'content': prompt}], max_tokens=512)\n    # Extract last word\n    for word in reversed(response.split()):\n        w = word.strip('.,!?').capitalize()\n        if w in ('True', 'False', 'Uncertain'):\n            return {'predicted_label': w, 'num_llm_calls': 1, 'method': 'cot'}\n    return {'predicted_label': 'Uncertain', 'num_llm_calls': 1, 'method': 'cot'}\n\ndef run_selfrefine_baseline(example, prolog, pred_key_index, n_rounds=3):\n    \"\"\"Self-refine: re-generate KB with global pass-rate feedback.\"\"\"\n    premises_text = '\\n'.join(example['premises'])\n    oracle_qs = generate_oracle_queries(premises_text)\n    oracle_as = generate_oracle_answers(premises_text, oracle_qs)\n    clauses = extract_kb(premises_text)\n    for r in range(n_rounds):\n        if COST_TRACKER['total'] >= MAX_BUDGET_USD: break\n        # Compute pass rate\n        pred_names = extract_predicate_names_from_clauses(clauses)\n        clear_kb(prolog, pred_key_index.get('all_preds', []))\n        load_kb_into_prolog(prolog, clauses)\n        pred_key_index['all_preds'] = pred_names\n        pred_sigs = [f'{n}/{a}' for n, a in pred_names]\n        goals = [nl_query_to_prolog(q, pred_sigs) for q in oracle_qs]\n        passed = 0\n        for i, goal in enumerate(goals):\n            if goal == 'fail': continue\n            try:\n                res = list(prolog.query(goal))\n                if (res and oracle_as[i]=='yes') or (not res and oracle_as[i]!='yes'):\n                    passed += 1\n            except: pass\n        pass_rate = passed / max(len(oracle_qs), 1)\n        # Re-generate with feedback\n        refine_prompt = f\"\"\"Your Prolog KB achieved {pass_rate:.0%} on oracle queries.\nPremises:\\n{premises_text}\\n\\nCurrent KB:\\n{'\\n'.join(clauses)}\\n\\nPlease regenerate the KB to improve coverage. Output only Prolog clauses.\"\"\"\n        response, _ = llm_call([{'role': 'user', 'content': refine_prompt}])\n        clauses = [l.strip() for l in response.split('\\n') if l.strip().endswith('.')]\n    pred_names = extract_predicate_names_from_clauses(clauses)\n    clear_kb(prolog, pred_key_index.get('all_preds', []))\n    load_kb_into_prolog(prolog, clauses)\n    pred_key_index['all_preds'] = pred_names\n    predicted = evaluate_conclusion(prolog, example.get('conclusion-FOL',''), clauses, premises_text)\n    return {'predicted_label': predicted, 'num_llm_calls': 2 + 2*n_rounds, 'method': 'selfrefine'}\n\ndef run_flat_sbfl_baseline(example, prolog, pred_key_index):\n    \"\"\"Flat SBFL: like dual-signal but binary coverage (no stratification), no sub-goal harvesting.\"\"\"\n    # Same as dual-signal but:\n    # - coverage_matrix[pred][i] = 1 if accessed (regardless of outcome), 0 if not\n    # - No missing_predicate_scores\n    # Implementation mirrors run_dual_sbfl_pipeline but with simplified coverage\n    # (reuse same code with a flat_mode=True flag)\n    pass  # Implement as simplified version of main pipeline\n\n# ============================================================\n# STEP 13: MAIN EXPERIMENT LOOP\n# ============================================================\ndef main():\n    import json\n    from pyswip import Prolog\n    from datasets import load_dataset\n    \n    # Load FOLIO validation split\n    ds = load_dataset('yale-nlp/FOLIO', split='validation')\n    examples = list(ds)\n    \n    # Smoke test on 5 examples first\n    test_examples = examples[:5]\n    full_examples = examples[:150]\n    \n    prolog = Prolog()\n    # Assert meta-interpreter\n    for line in META_INTERPRETER_PROLOG.strip().split('\\n'):\n        line = line.strip()\n        if line and not line.startswith('%'):\n            try:\n                if line.endswith('.'):\n                    list(prolog.query(f'assert(({line.rstrip(\".\")}))'))  # use consult_string or file\n            except: pass\n    # Better: write meta-interpreter to a temp .pl file and consult it\n    # import tempfile, os\n    # with tempfile.NamedTemporaryFile(suffix='.pl', mode='w', delete=False) as f:\n    #     f.write(META_INTERPRETER_PROLOG); tmpfile = f.name\n    # list(prolog.query(f'consult(\"{tmpfile}\")'))\n    \n    results = []\n    pred_key_index = {'all_preds': []}\n    \n    for i, ex in enumerate(full_examples):\n        if COST_TRACKER['total'] >= MAX_BUDGET_USD:\n            print(f'Budget exceeded at example {i}. Stopping.')\n            break\n        \n        ex_id = ex.get('story_id', i)\n        gold = ex['label']  # 'True', 'False', 'Uncertain'\n        \n        # Run all methods\n        try:\n            dual_sbfl = run_dual_sbfl_pipeline(ex, prolog, pred_key_index)\n        except Exception as e:\n            dual_sbfl = {'predicted_label': 'Uncertain', 'error': str(e)}\n        \n        try:\n            oneshot = run_oneshot_baseline(ex, prolog, pred_key_index)\n        except Exception as e:\n            oneshot = {'predicted_label': 'Uncertain', 'error': str(e)}\n        \n        cot = run_cot_baseline(ex)\n        \n        try:\n            selfrefine = run_selfrefine_baseline(ex, prolog, pred_key_index)\n        except Exception as e:\n            selfrefine = {'predicted_label': 'Uncertain', 'error': str(e)}\n        \n        result = {\n            'id': ex_id,\n            'gold_label': gold,\n            'predicted_label': dual_sbfl.get('predicted_label'),\n            'correct': dual_sbfl.get('predicted_label') == gold,\n            'num_llm_calls': dual_sbfl.get('num_llm_calls', 0),\n            'num_repairs_applied': dual_sbfl.get('num_repairs_applied', 0),\n            'ochiai_scores_top5': dual_sbfl.get('ochiai_scores_top5', []),\n            'subgoal_harvest_top5': dual_sbfl.get('subgoal_harvest_top5', []),\n            'cumulative_cost_usd': COST_TRACKER['total'],\n            'baseline_results': {\n                'oneshot': {'predicted': oneshot.get('predicted_label'), 'correct': oneshot.get('predicted_label') == gold},\n                'cot': {'predicted': cot.get('predicted_label'), 'correct': cot.get('predicted_label') == gold},\n                'selfrefine': {'predicted': selfrefine.get('predicted_label'), 'correct': selfrefine.get('predicted_label') == gold},\n                'flat_sbfl': {}  # fill in if budget permits\n            }\n        }\n        results.append(result)\n        \n        # Log progress every 10 examples\n        if (i+1) % 10 == 0:\n            acc = sum(r['correct'] for r in results) / len(results)\n            print(f'[{i+1}/{len(full_examples)}] Dual-SBFL acc={acc:.3f}, cost=${COST_TRACKER[\"total\"]:.2f}')\n    \n    # Compute aggregate metrics\n    def accuracy(preds, golds): return sum(p==g for p,g in zip(preds,golds))/max(len(golds),1)\n    \n    summary = {\n        'n_examples': len(results),\n        'total_cost_usd': COST_TRACKER['total'],\n        'dual_sbfl_accuracy': accuracy([r['predicted_label'] for r in results], [r['gold_label'] for r in results]),\n        'oneshot_accuracy': accuracy([r['baseline_results']['oneshot']['predicted'] for r in results], [r['gold_label'] for r in results]),\n        'cot_accuracy': accuracy([r['baseline_results']['cot']['predicted'] for r in results], [r['gold_label'] for r in results]),\n        'selfrefine_accuracy': accuracy([r['baseline_results']['selfrefine']['predicted'] for r in results], [r['gold_label'] for r in results]),\n        'avg_repairs_per_example': sum(r['num_repairs_applied'] for r in results) / max(len(results),1),\n    }\n    \n    output = {'summary': summary, 'results': results}\n    with open('method_out.json', 'w') as f:\n        json.dump(output, f, indent=2)\n    print('Done. Results saved to method_out.json')\n    print(json.dumps(summary, indent=2))\n\nif __name__ == '__main__':\n    main()\n\n# ============================================================\n# CRITICAL IMPLEMENTATION NOTES FOR EXECUTOR\n# ============================================================\n# 1. SWI-Prolog must be installed: `apt-get install -y swi-prolog`\n#    Then: `uv pip install pyswip datasets requests tqdm scipy numpy`\n# 2. pyswip Prolog class is NOT thread-safe. Use a single instance throughout.\n# 3. The meta-interpreter MUST be loaded via a .pl file (consult) not assertz.\n#    Write META_INTERPRETER_PROLOG to /tmp/meta_interp.pl, then:\n#    list(prolog.query('consult(\"/tmp/meta_interp.pl\")'))\n# 4. Between examples, ABOLISH all user predicates. Track predicate names.\n#    Do NOT use 'abolish' on built-in predicates (true/fail/nl/write etc.).\n# 5. The FOLIO 'label' field may be 'True'/'False'/'Uncertain' or\n#    'true'/'false'/'unknown' depending on version. Normalize on load.\n# 6. The 'premises' field may be a list (use '\\n'.join()) or a single string.\n#    The 'conclusion-FOL' field contains FOL syntax (may include ∀, ∃, →);\n#    the LLM must translate these to Prolog goals.\n# 7. OpenRouter API: use model 'meta-llama/llama-3.1-8b-instruct' for cost.\n#    Budget alert: at 15 oracle queries + 3 repair rounds × 5 items per example,\n#    worst case ~25 LLM calls per example × 150 examples = 3750 calls.\n#    At ~$0.00005/call for llama-3.1-8b: ~$0.19 total — well within $8 budget.\n#    Track costs: prompt_tokens * input_price + completion_tokens * output_price.\n# 8. FALLBACK for pyswip failures: if pyswip cannot be installed or SWI-Prolog\n#    is not available, implement a PURE PYTHON Prolog subset (see fallback plan).\n# 9. Smoke test first: run 5 examples, verify KB extraction produces valid Prolog,\n#    verify meta-interpreter loads, verify oracle query generation is sensible.\n# 10. LLM-generated Prolog may contain syntax errors. Wrap every assertz in try/except.\n#     Log failed assertions. If >50% of clauses fail, flag as extraction failure.\n# 11. For the flat-SBFL ablation: reuse the same infrastructure but set\n#     all coverage outcomes to binary (accessed=1, not_accessed=0), skip subgoal harvesting.\n",
  "fallback_plan": "## Fallback 1: pyswip Installation Fails\nIf SWI-Prolog or pyswip cannot be installed on the target machine:\n- Implement a minimal pure-Python Prolog interpreter supporting:\n  - Ground facts: `fact(a).`\n  - Simple rules: `head(X) :- body(X).`\n  - Backward-chaining resolution with occurs-check\n  - depth-limited DFS (depth <= 10)\n- Python packages: none required beyond stdlib\n- This covers 80% of FOLIO KB patterns (simple ground facts + chain rules)\n- The meta-interpreter becomes Python-native, simpler to instrument for coverage tracking\n\n## Fallback 2: FOLIO Dataset Access Issues\nIf `load_dataset('yale-nlp/FOLIO')` fails (auth required or unavailable):\n- Try `load_dataset('yale-nlp/FOLIO', use_auth_token=True)` with HF_TOKEN env var\n- Alternatively: download from GitHub raw files:\n  `https://raw.githubusercontent.com/Yale-LILY/FOLIO/main/data/v0.0/folio-validation.jsonl`\n  Fetch with requests and parse jsonl manually.\n- Fields in each line: story_id, premises (list), premises-FOL (list), conclusion, conclusion-FOL, label\n\n## Fallback 3: LLM-to-Prolog Conversion Quality Too Low\nIf >40% of extracted KBs have 0 valid clauses (all fail assertz):\n- Switch to a more structured extraction prompt: provide a fixed predicate vocabulary\n  derived from the FOLIO FOL annotations (extract unique predicate names from premises-FOL)\n- Use the FOL annotations as hints: 'The KB should use predicates: {fol_predicates}'\n- If that still fails: use the premises-FOL directly, converting FOL syntax to Prolog\n  (quantifiers → Prolog variables, → becomes :-, ¬ becomes \\+)\n\n## Fallback 4: Budget Exceeded Early\nIf $8 budget is consumed before 150 examples:\n- Report results on however many examples were processed\n- Reduce to 50 examples with all methods, skip selfrefine baseline (most expensive)\n- Switch from llama-3.1-8b to a cheaper model if available on OpenRouter\n  (check /api/v1/models for models with input price < $0.05/M tokens)\n\n## Fallback 5: Meta-Interpreter Too Slow or Buggy\nIf the Prolog meta-interpreter causes timeouts or incorrect results:\n- Replace meta-interpreter with exception-based coverage: use pyswip's native query\n  with a timeout, catch PrologError, and infer coverage from which predicates\n  appear in the error message\n- Simpler coverage approximation: for each predicate P in KB, run `P(...)` directly\n  with fresh variables; mark as accessible if it returns any results; mark as\n  inaccessible (= unify_failed) if it throws or returns []\n- This loses stratification but retains Ochiai computation\n\n## Fallback 6: Conclusion Evaluation Unreliable\nIf LLM-to-Prolog goal conversion for conclusion-FOL fails frequently:\n- Use the FOLIO FOL annotations more directly: the conclusion-FOL is often simple\n  atomic predicates like 'Engaged(bonnie)' — convert with regex:\n  `re.sub(r'([A-Z][a-zA-Z]*)\\(([^)]+)\\)', lambda m: f'{m.group(1).lower()}({m.group(2).lower()})', fol)`\n- As last resort: use LLM-only evaluation (ask LLM if conclusion follows from premises,\n  classify as True/False/Uncertain from response) — no Prolog needed, but loses\n  symbolic grounding. Report separately as 'LLM-judge' baseline.",
  "testing_plan": "## Phase 0: Environment Smoke Test (5 minutes)\n1. Install dependencies: `apt-get install -y swi-prolog && uv pip install pyswip datasets requests`\n2. Test pyswip: `python -c \"from pyswip import Prolog; p=Prolog(); p.assertz('happy(john)'); print(list(p.query('happy(X)')))\"`\n   Expected: `[{'X': 'john'}]`\n3. Test FOLIO load: `python -c \"from datasets import load_dataset; ds=load_dataset('yale-nlp/FOLIO',split='validation'); print(ds[0].keys()); print(ds[0]['label'])\"`\n   Expected: keys include 'premises', 'conclusion', 'conclusion-FOL', 'label'\n4. Test OpenRouter: simple LLM call with `meta-llama/llama-3.1-8b-instruct`, verify cost tracking works\n5. Load meta-interpreter .pl file via consult, run `solve(true, 5)`, verify returns `[{}]`\n\n## Phase 1: Single Example Dry Run (10 minutes)\n1. Take FOLIO example index 0\n2. Run extraction prompt → verify output contains valid Prolog clauses (check `.` endings)\n3. Assert clauses into Prolog, run a trivial query, verify no exception\n4. Run oracle query generation → verify 10+ questions generated\n5. Convert 3 questions to Prolog goals manually, run through meta-interpreter\n6. Verify coverage_log/3 and failed_subgoal/1 are populated after failing query\n7. Compute Ochiai scores manually on this example, check values in [0,1]\n8. Run one repair round, check new clauses are valid Prolog\n9. Run conclusion evaluation, check output is 'True'/'False'/'Uncertain'\n10. Log: total cost for this example, verify < $0.05\n\n## Phase 2: 5-Example Validation (15 minutes)\n1. Run all 4 methods (dual-SBFL, oneshot, CoT, selfrefine) on first 5 FOLIO examples\n2. Check: no crashes, all methods return valid labels\n3. Check: dual-SBFL accuracy >= oneshot (even on 5 examples, shouldn't be worse)\n4. Check: cost per example < $0.20 average (5 × $0.20 = $1.00 max for smoke test)\n5. Inspect ochiai_scores_top5 and subgoal_harvest_top5 for sanity:\n   - Are the suspicious predicates actually in failing queries?\n   - Are missing subgoals actually absent from KB?\n6. Confirm method_out.json is written with correct schema\n\n## Phase 3: 50-Example Run (1-2 hours)\n1. Run all methods on first 50 examples\n2. Check for runtime errors after each batch of 10\n3. Monitor cost: should be < $2 for 50 examples\n4. Check accuracy distribution: label breakdown (True/False/Uncertain) should match FOLIO distribution\n5. Compute preliminary accuracy numbers; if dual-SBFL < oneshot by >10%, investigate\n   (likely issue: KB extraction quality too low, or conclusion evaluation broken)\n6. Check avg_repairs_per_example: should be > 0 (repairs are happening)\n\n## Phase 4: Full 150-Example Run (2-4 hours)\n1. Run remaining examples (50-150)\n2. Monitor budget: stop if > $7 cumulative\n3. Compute final summary statistics\n4. Validate output schema: all required fields present in every result\n5. Run aii-json validation if schema available\n\n## Key Sanity Checks\n- Label distribution in predictions should roughly match gold: ~40% True, ~30% False, ~30% Uncertain (approx FOLIO distribution)\n- If all predictions are 'Uncertain', conclusion evaluation is broken\n- If dual-SBFL and oneshot have identical predictions on all examples, repair is not applying\n- Ochiai scores should vary per example (not all zero and not all one)\n- Cost tracking: print cumulative cost every 10 examples"
}
```

### [20] TOOL RESULT — Write · 2026-06-07 04:58:25 UTC

```
{"type": "create", "filePath": "/ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_plan/gen_plan_experiment_1/.terminal_claude_agent_struct_out.json", "content": "{\n  \"title\": \"Dual-Signal SBFL Pipeline for NL-to-FOL Prolog KB Repair on FOLIO\",\n  \"summary\": \"Implement and evaluate the dual-signal SBFL pipeline (Ochiai + sub-goal harvesting) for repairing LLM-extracted Prolog KBs on FOLIO, comparing against one-shot, self-refine, flat-SBFL, and CoT baselines. Output method_out.json with per-example predictions, metrics, and ablation results.\",\n  \"runpod_compute_profile\": \"cpu_heavy\",\n  \"implementation_pseudocode\": \"# ============================================================\\n# SETUP AND DEPENDENCIES\\n# ============================================================\\n# Install: uv pip install pyswip datasets openai tqdm scipy numpy pandas\\n# Requires SWI-Prolog installed system-wide: apt-get install swi-prolog\\n# OpenRouter API key in env: OPENROUTER_API_KEY\\n\\n# ============================================================\\n# STEP 0: CONSTANTS AND CONFIGURATION\\n# ============================================================\\nMODEL = 'meta-llama/llama-3.1-8b-instruct'  # cheap default; fallback: google/gemma-2-9b-it\\nFALLBACK_MODEL = 'anthropic/claude-3-haiku'   # if llama errors, switch\\nMAX_BUDGET_USD = 8.0  # hard stop\\nMAX_EXAMPLES = 150    # start with 50 for validation\\nN_ORACLE_QUERIES = 15  # LLM call 2 generates this many yes/no queries\\nN_REPAIR_ROUNDS = 3\\nK_REPAIR_TARGETS = 5\\nCOST_TRACKER = {'total': 0.0}  # mutable dict for cumulative cost tracking\\n\\nOPENROUTER_BASE = 'https://openrouter.ai/api/v1'\\n\\n# ============================================================\\n# STEP 1: LOAD FOLIO DATASET\\n# ============================================================\\n# Use HuggingFace datasets library:\\n#   from datasets import load_dataset\\n#   ds = load_dataset('yale-nlp/FOLIO', split='validation')\\n# Each example has fields:\\n#   'story': str (premises as a single block of text, newline-separated)\\n#   'premises': list[str] (individual premise sentences)\\n#   'premises-FOL': list[str] (FOL translations of premises)\\n#   'conclusion': str\\n#   'conclusion-FOL': str\\n#   'label': str  -- 'True', 'False', or 'Uncertain'\\n# NOTE: If the dataset field is 'story' instead of 'premises', join premise list.\\n# NOTE: Some FOLIO versions use 'Uncertain' or 'Unknown' for the third label.\\n# Take first 50 examples for smoke-test, then up to 150.\\n\\n# ============================================================\\n# STEP 2: LLM HELPER FUNCTION\\n# ============================================================\\ndef llm_call(prompt_messages, model=MODEL, max_tokens=1024):\\n    \\\"\\\"\\\"Call OpenRouter API. Return (text, cost_usd). Update COST_TRACKER.\\\"\\\"\\\"\\n    # POST to https://openrouter.ai/api/v1/chat/completions\\n    # Headers: Authorization: Bearer {OPENROUTER_API_KEY}\\n    # Body: {model, messages, max_tokens}\\n    # Response: choices[0].message.content, usage.prompt_tokens, usage.completion_tokens\\n    # Cost estimation per model (approx, update from OpenRouter pricing page):\\n    #   llama-3.1-8b-instruct: ~$0.055/M input, $0.055/M output\\n    #   claude-3-haiku: ~$0.25/M input, $1.25/M output\\n    # After every call: COST_TRACKER['total'] += estimated_cost\\n    # If COST_TRACKER['total'] >= MAX_BUDGET_USD: raise BudgetExceededError\\n    pass\\n\\n# ============================================================\\n# STEP 3: KB EXTRACTION (LLM Call 1)\\n# ============================================================\\nEXTRACTION_PROMPT = \\\"\\\"\\\"\\nYou are a Prolog knowledge base extractor. Given the following natural language premises,\\nextract a Prolog knowledge base (facts and rules) that captures all stated relationships.\\n\\nRules:\\n- Use snake_case predicate names (e.g., is_happy/1, parent_of/2)\\n- Each fact/rule on its own line ending with '.'\\n- No comments, no module declarations\\n- Use only alphanumeric and underscore characters in predicate/atom names\\n- Predicates must be Prolog-valid (no spaces, no special chars)\\n- Output ONLY the Prolog code, nothing else\\n\\nPremises:\\n{premises}\\n\\nProlog KB:\\\"\\\"\\\"\\n\\ndef extract_kb(premises_text):\\n    \\\"\\\"\\\"Returns list of Prolog clause strings.\\\"\\\"\\\"\\n    response, cost = llm_call([{'role': 'user', 'content': EXTRACTION_PROMPT.format(premises=premises_text)}])\\n    # Parse: split by newlines, strip, filter lines ending with '.', filter empty\\n    clauses = [line.strip() for line in response.split('\\\\n') if line.strip().endswith('.')]\\n    return clauses\\n\\n# ============================================================\\n# STEP 4: ORACLE QUERY GENERATION (LLM Call 2)\\n# ============================================================\\nORACLE_PROMPT = \\\"\\\"\\\"\\nGiven these natural language premises, generate {n} yes/no reasoning questions\\nthat test whether specific facts from the premises hold.\\nGenerate a mix: roughly half should be answerable 'yes', half 'no' or 'unknown'.\\nFormat: one question per line, starting with 'Q: '.\\n\\nPremises:\\n{premises}\\n\\nQuestions:\\\"\\\"\\\"\\n\\ndef generate_oracle_queries(premises_text, n=N_ORACLE_QUERIES):\\n    \\\"\\\"\\\"Returns list of NL yes/no question strings.\\\"\\\"\\\"\\n    response, cost = llm_call([{'role': 'user', 'content': ORACLE_PROMPT.format(premises=premises_text, n=n)}])\\n    lines = [l.strip().lstrip('Q:').strip() for l in response.split('\\\\n') if l.strip().startswith('Q:')]\\n    return lines[:n]\\n\\n# ============================================================\\n# STEP 5: ORACLE ANSWER GENERATION (Part of LLM Call 2)\\n# ============================================================\\nANSWER_PROMPT = \\\"\\\"\\\"\\nBased on the following premises, answer each question with 'yes', 'no', or 'unknown'.\\nProvide ONLY 'yes', 'no', or 'unknown' for each question, one per line.\\n\\nPremises:\\n{premises}\\n\\nQuestions and answers:\\n{questions}\\n\\nAnswers (one per line, only 'yes'/'no'/'unknown'):\\\"\\\"\\\"\\n\\ndef generate_oracle_answers(premises_text, questions):\\n    \\\"\\\"\\\"Returns list of 'yes'/'no'/'unknown' strings matching questions.\\\"\\\"\\\"\\n    q_block = '\\\\n'.join(f'{i+1}. {q}' for i, q in enumerate(questions))\\n    response, cost = llm_call([{'role': 'user', 'content': ANSWER_PROMPT.format(premises=premises_text, questions=q_block)}])\\n    answers = [l.strip().lower() for l in response.split('\\\\n') if l.strip().lower() in ('yes', 'no', 'unknown')]\\n    # Pad or truncate to match questions length\\n    while len(answers) < len(questions): answers.append('unknown')\\n    return answers[:len(questions)]\\n\\n# ============================================================\\n# STEP 6: NL QUERY TO PROLOG GOAL CONVERSION (LLM Call 2b, cheap)\\n# ============================================================\\nNL_TO_PROLOG_PROMPT = \\\"\\\"\\\"\\nGiven this Prolog knowledge base and a natural language yes/no question,\\nwrite a Prolog query goal (without '?-', just the goal expression) that answers the question.\\nIf the question asks 'Is X happy?', write: is_happy(x).\\nIf not answerable as Prolog query, write: fail.\\nOutput ONLY the Prolog goal, nothing else.\\n\\nKB predicates available:\\n{predicate_signatures}\\n\\nQuestion: {question}\\nProlog goal:\\\"\\\"\\\"\\n\\ndef nl_query_to_prolog(question, predicate_sigs):\\n    \\\"\\\"\\\"Convert NL question to Prolog goal string.\\\"\\\"\\\"\\n    sig_block = '\\\\n'.join(predicate_sigs)\\n    response, cost = llm_call([{'role': 'user', 'content': NL_TO_PROLOG_PROMPT.format(\\n        predicate_signatures=sig_block, question=question)}], max_tokens=100)\\n    goal = response.strip().rstrip('.').strip()\\n    return goal if goal else 'fail'\\n\\n# ============================================================\\n# STEP 7: PYSWIP META-INTERPRETER WITH STRATIFIED COVERAGE\\n# ============================================================\\n# pyswip Prolog class is NOT thread-safe; use a single Prolog instance per process.\\n# IMPORTANT: Always retract all user-defined clauses between examples to reset state.\\n#\\n# Predicate outcome enum:\\n#   NOT_ACCESSED = 0\\n#   UNIFIED_OK_SUBGOAL_FAILED = 1  (indirect fault)\\n#   UNIFY_FAILED = 2               (direct fault)\\n#   SUCCEEDED = 3\\n\\ndef load_kb_into_prolog(prolog_instance, clauses):\\n    \\\"\\\"\\\"Assert all clauses. Returns list of successfully asserted clauses.\\\"\\\"\\\"\\n    ok_clauses = []\\n    for clause in clauses:\\n        try:\\n            prolog_instance.assertz(clause.rstrip('.'))\\n            ok_clauses.append(clause)\\n        except Exception as e:\\n            pass  # skip malformed clauses\\n    return ok_clauses\\n\\ndef clear_kb(prolog_instance, predicate_names):\\n    \\\"\\\"\\\"Retract all user-defined predicates to reset state between examples.\\\"\\\"\\\"\\n    for pred_name, arity in predicate_names:\\n        try:\\n            list(prolog_instance.query(f'abolish({pred_name}/{arity})'))\\n        except:\\n            pass\\n\\ndef extract_predicate_names_from_clauses(clauses):\\n    \\\"\\\"\\\"Parse clauses to extract (name, arity) pairs using regex.\\n    Pattern: match head of clause before ':-' or end.\\n    E.g., 'happy(john).' -> ('happy', 1)\\n          'parent(X,Y) :- father(X,Y).' -> ('parent', 2)\\n    \\\"\\\"\\\"\\n    import re\\n    preds = set()\\n    for clause in clauses:\\n        head = clause.split(':-')[0].strip().rstrip('.')\\n        m = re.match(r'([a-z][a-zA-Z0-9_]*)\\\\((.*)\\\\)', head)\\n        if m:\\n            name = m.group(1)\\n            args = m.group(2)\\n            arity = len(args.split(',')) if args.strip() else 0\\n            preds.add((name, arity))\\n        elif re.match(r'[a-z][a-zA-Z0-9_]*$', head):\\n            preds.add((head, 0))\\n    return list(preds)\\n\\n# Stratified coverage tracking via Prolog meta-interpreter\\n# Use a Python-side tracer approach:\\n# 1. Enable Prolog trace/debugging hooks via assert of meta-level spy points\\n# 2. OR: use a simpler approach -- wrap pyswip query with try/except and\\n#    post-hoc analyze which predicates appear in the KB vs which succeeded.\\n#\\n# SIMPLER APPROACH (recommended for reliability):\\n# For each oracle query, attempt Prolog proof:\\n#   - If query succeeds: all accessed predicates marked SUCCEEDED\\n#   - If query fails: we need to identify WHERE it failed\\n#     Strategy: iteratively probe sub-goals\\n#       * For each predicate P in KB, test P(...) with appropriate bindings\\n#       * Record which predicates can be proved at all (sanity check)\\n#     Better: use Prolog's exception mechanism + a custom meta-interpreter\\n#\\n# META-INTERPRETER APPROACH (more robust):\\n# Assert a solve/2 meta-interpreter in Prolog that tracks failures:\\n#\\nMETA_INTERPRETER_PROLOG = \\\"\\\"\\\"\\n:- dynamic coverage_log/3.  % coverage_log(Pred, Arity, Outcome)\\n:- dynamic failed_subgoal/1.  % failed_subgoal(Goal)\\n\\nreset_coverage :-\\n    retractall(coverage_log(_, _, _)),\\n    retractall(failed_subgoal(_)).\\n\\n% Meta-interpreter solve(Goal, Depth)\\nsolve(true, _) :- !.\\nsolve((A, B), D) :- !, solve(A, D), solve(B, D).\\nsolve((A ; B), D) :- !, (solve(A, D) ; solve(B, D)).\\nsolve(\\\\+(A), D) :- !, (solve(A, D) -> fail ; true).\\nsolve(Goal, D) :-\\n    D > 0,\\n    functor(Goal, Name, Arity),\\n    % Try to find a matching clause\\n    (clause(Goal, Body) ->\\n        D1 is D - 1,\\n        (solve(Body, D1) ->\\n            assert(coverage_log(Name, Arity, succeeded))\\n        ;\\n            assert(coverage_log(Name, Arity, subgoal_failed)),\\n            assert(failed_subgoal(Body)),\\n            fail\\n        )\\n    ;\\n        % No matching clause - direct unification failure\\n        assert(coverage_log(Name, Arity, unify_failed)),\\n        assert(failed_subgoal(Goal)),\\n        fail\\n    ).\\nsolve(Goal, 0) :-\\n    functor(Goal, Name, Arity),\\n    assert(coverage_log(Name, Arity, depth_exceeded)),\\n    fail.\\n\\\"\\\"\\\"\\n# NOTE: The meta-interpreter above is an approximation. Adjust depth limit (start with 10).\\n# In practice, the executor should test this meta-interpreter carefully on simple examples first.\\n\\ndef run_oracle_query_with_coverage(prolog, goal_str, depth=10):\\n    \\\"\\\"\\\"\\n    Run goal_str through meta-interpreter. Returns:\\n    {\\n        'succeeded': bool,\\n        'coverage': dict of {pred_name: outcome_list},  # outcome in {succeeded, subgoal_failed, unify_failed}\\n        'failed_subgoals': list of goal strings\\n    }\\n    \\\"\\\"\\\"\\n    try:\\n        list(prolog.query('reset_coverage'))\\n    except:\\n        pass\\n    succeeded = False\\n    try:\\n        results = list(prolog.query(f'solve({goal_str}, {depth})'))\\n        succeeded = len(results) > 0\\n    except Exception as e:\\n        pass\\n    # Read coverage log\\n    coverage = {}\\n    try:\\n        for sol in prolog.query('coverage_log(N, A, O)'):\\n            key = f\\\"{sol['N']}/{sol['A']}\\\"\\n            if key not in coverage: coverage[key] = []\\n            coverage[key].append(str(sol['O']))\\n    except:\\n        pass\\n    failed_subgoals = []\\n    try:\\n        for sol in prolog.query('failed_subgoal(G)'):\\n            failed_subgoals.append(str(sol['G']))\\n    except:\\n        pass\\n    return {'succeeded': succeeded, 'coverage': coverage, 'failed_subgoals': failed_subgoals}\\n\\n# ============================================================\\n# STEP 8: OCHIAI SCORE COMPUTATION\\n# ============================================================\\ndef compute_ochiai_scores(coverage_matrix, pass_fail):\\n    \\\"\\\"\\\"\\n    coverage_matrix: dict {pred_key: list of per-query outcomes}\\n      outcome: 'succeeded' = accessed+unified, 'unify_failed' = direct fault,\\n               'subgoal_failed' = indirect fault, None = not accessed\\n    pass_fail: list of bool (True=oracle says yes/pass, False=no/fail)\\n    \\n    For Ochiai, treat UNIFY_FAILED as 'covered in failing test' signal.\\n    Binary coverage for Ochiai: pred accessed in query i = 1 if outcome != None\\n    \\n    Ochiai(p) = ef / sqrt((ef+nf) * (ef+ep))\\n      ef = failing tests where predicate accessed\\n      ep = passing tests where predicate accessed\\n      nf = failing tests where predicate NOT accessed\\n    \\n    Returns dict {pred_key: ochiai_score} for predicates with unify_failed outcome.\\n    \\\"\\\"\\\"\\n    import math\\n    scores = {}\\n    n_queries = len(pass_fail)\\n    n_failing = sum(1 for p in pass_fail if not p)\\n    \\n    for pred_key, outcomes in coverage_matrix.items():\\n        # Only score direct-fault candidates (unify_failed at least once)\\n        if not any(o == 'unify_failed' for o in outcomes):\\n            continue\\n        ef = sum(1 for i, o in enumerate(outcomes) if o is not None and not pass_fail[i])\\n        ep = sum(1 for i, o in enumerate(outcomes) if o is not None and pass_fail[i])\\n        nf = n_failing - ef\\n        denom = math.sqrt((ef + nf) * (ef + ep))\\n        scores[pred_key] = ef / denom if denom > 0 else 0.0\\n    return scores\\n\\ndef compute_missing_predicate_scores(all_failed_subgoals, kb_pred_keys):\\n    \\\"\\\"\\\"\\n    Count frequency of failed subgoals that are NOT in the KB.\\n    Returns dict {subgoal_str: frequency} for truly missing predicates.\\n    \\\"\\\"\\\"\\n    from collections import Counter\\n    counts = Counter()\\n    for subgoal in all_failed_subgoals:\\n        # Extract predicate name from subgoal string\\n        import re\\n        m = re.match(r'([a-z][a-zA-Z0-9_]*).*', subgoal)\\n        if m:\\n            pred_name = m.group(1)\\n            # Check if it appears in KB\\n            in_kb = any(k.split('/')[0] == pred_name for k in kb_pred_keys)\\n            if not in_kb:\\n                counts[subgoal] += 1\\n    return dict(counts)\\n\\n# ============================================================\\n# STEP 9: REPAIR AGENDA CONSTRUCTION AND LLM REPAIR (LLM Call 3)\\n# ============================================================\\nREPAIR_PROMPT = \\\"\\\"\\\"\\nYou are repairing a Prolog knowledge base extracted from natural language.\\nThe following Prolog predicate/sub-goal is suspected to be erroneous or missing.\\n\\nOriginal premises text:\\n{premises}\\n\\nCurrent Prolog KB:\\n{kb}\\n\\nSuspicious item: {item}\\nReason: {reason}\\n\\nPlease provide the corrected or new Prolog clause(s) that should replace or supplement this item.\\nOutput ONLY valid Prolog clause(s), one per line, ending with '.'\\nDo not include explanations.\\\"\\\"\\\"\\n\\ndef repair_item(premises_text, kb_clauses, item, reason):\\n    \\\"\\\"\\\"LLM repairs a suspicious predicate or adds a missing one.\\n    Returns list of new/replacement clause strings.\\\"\\\"\\\"\\n    kb_str = '\\\\n'.join(kb_clauses)\\n    response, cost = llm_call([{'role': 'user', 'content': REPAIR_PROMPT.format(\\n        premises=premises_text, kb=kb_str, item=item, reason=reason)}])\\n    new_clauses = [l.strip() for l in response.split('\\\\n') if l.strip().endswith('.')]\\n    return new_clauses\\n\\ndef build_repair_agenda(ochiai_scores, missing_scores, k=K_REPAIR_TARGETS):\\n    \\\"\\\"\\\"Merge Ochiai and missing-predicate scores into top-K repair agenda.\\\"\\\"\\\"\\n    # Normalize both score sets to [0,1] and interleave\\n    max_ochiai = max(ochiai_scores.values()) if ochiai_scores else 1.0\\n    max_missing = max(missing_scores.values()) if missing_scores else 1.0\\n    agenda = []\\n    for pred, score in ochiai_scores.items():\\n        agenda.append({'item': pred, 'score': score / max_ochiai, 'type': 'erroneous'})\\n    for subgoal, freq in missing_scores.items():\\n        agenda.append({'item': subgoal, 'score': freq / max_missing, 'type': 'missing'})\\n    agenda.sort(key=lambda x: x['score'], reverse=True)\\n    return agenda[:k]\\n\\n# ============================================================\\n# STEP 10: MAIN PIPELINE PER EXAMPLE\\n# ============================================================\\ndef run_dual_sbfl_pipeline(example, prolog, pred_key_index):\\n    \\\"\\\"\\\"\\n    Returns dict with: predicted_label, num_llm_calls, num_repairs_applied,\\n    ochiai_scores_top5, subgoal_harvest_top5, kb_after_repair\\n    \\\"\\\"\\\"\\n    premises_text = '\\\\n'.join(example['premises'])\\n    gold_label = example['label']  # 'True', 'False', 'Uncertain'\\n    \\n    # --- LLM Call 1: Extract KB ---\\n    clauses = extract_kb(premises_text)  # ~1 call\\n    \\n    # --- LLM Call 2: Generate oracle queries + answers ---\\n    oracle_questions = generate_oracle_queries(premises_text)  # ~1 call\\n    oracle_answers = generate_oracle_answers(premises_text, oracle_questions)  # ~1 call\\n    # oracle_answers[i] in {'yes', 'no', 'unknown'}\\n    # pass_fail[i] = True if oracle_answers[i] == 'yes', False otherwise\\n    pass_fail = [a == 'yes' for a in oracle_answers]\\n    \\n    # Convert oracle questions to Prolog goals (batch in one LLM call if possible)\\n    pred_sigs = [f'{n}/{a}' for n, a in extract_predicate_names_from_clauses(clauses)]\\n    prolog_goals = [nl_query_to_prolog(q, pred_sigs) for q in oracle_questions]  # up to N calls\\n    \\n    # --- SBFL Repair Loop ---\\n    current_clauses = clauses[:]\\n    all_repairs = []\\n    \\n    for round_idx in range(N_REPAIR_ROUNDS):\\n        if COST_TRACKER['total'] >= MAX_BUDGET_USD:\\n            break\\n        \\n        # Load KB\\n        pred_names = extract_predicate_names_from_clauses(current_clauses)\\n        clear_kb(prolog, pred_key_index.get('all_preds', []))\\n        loaded = load_kb_into_prolog(prolog, current_clauses)\\n        pred_key_index['all_preds'] = pred_names\\n        \\n        # Run each oracle query through meta-interpreter\\n        coverage_matrix = {}  # {pred_key: [outcome_per_query]}\\n        all_failed_subgoals = []\\n        n_queries = len(prolog_goals)\\n        for i, goal in enumerate(prolog_goals):\\n            if goal == 'fail':\\n                continue\\n            result = run_oracle_query_with_coverage(prolog, goal)\\n            all_failed_subgoals.extend(result['failed_subgoals'])\\n            for pred_key, outcomes in result['coverage'].items():\\n                if pred_key not in coverage_matrix:\\n                    coverage_matrix[pred_key] = [None] * n_queries\\n                # Take worst (most suspicious) outcome for this query\\n                worst = outcomes[-1] if outcomes else None\\n                coverage_matrix[pred_key][i] = worst\\n        \\n        # Fill None for unaccessed preds\\n        for pred_key in coverage_matrix:\\n            coverage_matrix[pred_key] = [o if o else None for o in coverage_matrix[pred_key]]\\n        \\n        # Compute Ochiai scores (direct-fault predicates)\\n        ochiai = compute_ochiai_scores(coverage_matrix, pass_fail)\\n        # Compute missing predicate scores\\n        kb_pred_keys = set(coverage_matrix.keys())\\n        missing = compute_missing_predicate_scores(all_failed_subgoals, kb_pred_keys)\\n        \\n        # Build repair agenda\\n        agenda = build_repair_agenda(ochiai, missing, k=K_REPAIR_TARGETS)\\n        if not agenda:\\n            break  # nothing to repair\\n        \\n        # LLM Call 3: Repair each item in agenda\\n        for item_info in agenda:\\n            if COST_TRACKER['total'] >= MAX_BUDGET_USD:\\n                break\\n            new_clauses = repair_item(\\n                premises_text, current_clauses,\\n                item_info['item'],\\n                f\\\"Type: {item_info['type']}, Score: {item_info['score']:.3f}\\\"\\n            )\\n            if new_clauses:\\n                # For erroneous: try to find and replace matching predicate clauses\\n                if item_info['type'] == 'erroneous':\\n                    pred_name = item_info['item'].split('/')[0]\\n                    current_clauses = [c for c in current_clauses\\n                                       if not c.split('(')[0].strip() == pred_name]\\n                current_clauses.extend(new_clauses)\\n                all_repairs.append({'item': item_info['item'], 'type': item_info['type'],\\n                                    'new_clauses': new_clauses, 'round': round_idx})\\n    \\n    # --- Final evaluation: run gold conclusion as Prolog query ---\\n    pred_names = extract_predicate_names_from_clauses(current_clauses)\\n    clear_kb(prolog, pred_key_index.get('all_preds', []))\\n    load_kb_into_prolog(prolog, current_clauses)\\n    \\n    conclusion_fol = example.get('conclusion-FOL', '')\\n    predicted_label = evaluate_conclusion(prolog, conclusion_fol, current_clauses, premises_text)\\n    \\n    return {\\n        'predicted_label': predicted_label,\\n        'num_llm_calls': None,  # filled by caller tracking\\n        'num_repairs_applied': len(all_repairs),\\n        'ochiai_scores_top5': sorted([(k,v) for k,v in ochiai.items()], key=lambda x:-x[1])[:5] if 'ochiai' in dir() else [],\\n        'subgoal_harvest_top5': sorted([(k,v) for k,v in missing.items()], key=lambda x:-x[1])[:5] if 'missing' in dir() else [],\\n        'repairs': all_repairs\\n    }\\n\\n# ============================================================\\n# STEP 11: CONCLUSION EVALUATION\\n# ============================================================\\nCONCLUSION_EVAL_PROMPT = \\\"\\\"\\\"\\nGiven this Prolog knowledge base and this conclusion in first-order logic notation:\\n{conclusion_fol}\\n\\nConvert the conclusion to a Prolog query goal (without '?-'). Output only the Prolog goal.\\nIf it cannot be converted, output: fail\\\"\\\"\\\"\\n\\ndef evaluate_conclusion(prolog, conclusion_fol, kb_clauses, premises_text):\\n    \\\"\\\"\\\"\\n    Try to evaluate conclusion_fol against loaded KB.\\n    Returns 'True', 'False', or 'Uncertain'.\\n    Strategy:\\n    1. LLM converts conclusion_fol to Prolog goal (if not already Prolog-like)\\n    2. Try to prove goal via Prolog query\\n    3. Try to prove negation of goal\\n    4. If neither succeeds: 'Uncertain'\\n    \\\"\\\"\\\"\\n    # If conclusion_fol already looks like Prolog (no quantifiers, no =>)\\n    # try direct query; else use LLM to convert\\n    import re\\n    goal = conclusion_fol.strip().rstrip('.')\\n    if any(tok in goal for tok in ['∀', '∃', '→', '⟹', '=>', 'forall', 'exists']):\\n        # Need LLM conversion\\n        kb_str = '\\\\n'.join(kb_clauses[:20])  # truncate for prompt\\n        response, _ = llm_call([{'role': 'user', 'content': CONCLUSION_EVAL_PROMPT.format(\\n            conclusion_fol=conclusion_fol) + f'\\\\nKB (excerpt):\\\\n{kb_str}'}], max_tokens=100)\\n        goal = response.strip().rstrip('.')\\n    \\n    if not goal or goal == 'fail':\\n        return 'Uncertain'\\n    \\n    try:\\n        results = list(prolog.query(goal))\\n        if results:  # proved\\n            # Now try negation\\n            try:\\n                neg_results = list(prolog.query(f'\\\\\\\\+({goal})'))\\n                if neg_results:\\n                    return 'False'  # negation also provable - contradiction\\n            except:\\n                pass\\n            return 'True'\\n        else:\\n            # Failed - try negation as failure\\n            try:\\n                neg_results = list(prolog.query(f'\\\\\\\\+({goal})'))\\n                if neg_results:\\n                    return 'False'\\n            except:\\n                pass\\n            return 'Uncertain'\\n    except Exception:\\n        return 'Uncertain'\\n\\n# ============================================================\\n# STEP 12: BASELINES\\n# ============================================================\\ndef run_oneshot_baseline(example, prolog, pred_key_index):\\n    \\\"\\\"\\\"One-shot: extract KB, query conclusion directly, no repair.\\\"\\\"\\\"\\n    premises_text = '\\\\n'.join(example['premises'])\\n    clauses = extract_kb(premises_text)\\n    pred_names = extract_predicate_names_from_clauses(clauses)\\n    clear_kb(prolog, pred_key_index.get('all_preds', []))\\n    load_kb_into_prolog(prolog, clauses)\\n    pred_key_index['all_preds'] = pred_names\\n    predicted = evaluate_conclusion(prolog, example.get('conclusion-FOL',''), clauses, premises_text)\\n    return {'predicted_label': predicted, 'num_llm_calls': 2, 'method': 'oneshot'}\\n\\ndef run_cot_baseline(example):\\n    \\\"\\\"\\\"Chain-of-thought: single LLM call with reasoning prompt.\\\"\\\"\\\"\\n    premises_text = '\\\\n'.join(example['premises'])\\n    conclusion = example['conclusion']\\n    prompt = f\\\"\\\"\\\"Given the following premises:\\\\n{premises_text}\\\\n\\\\nConclusion: {conclusion}\\\\n\\nThink step by step. Then answer with exactly one word: True, False, or Uncertain.\\\"\\\"\\\"\\n    response, _ = llm_call([{'role': 'user', 'content': prompt}], max_tokens=512)\\n    # Extract last word\\n    for word in reversed(response.split()):\\n        w = word.strip('.,!?').capitalize()\\n        if w in ('True', 'False', 'Uncertain'):\\n            return {'predicted_label': w, 'num_llm_calls': 1, 'method': 'cot'}\\n    return {'predicted_label': 'Uncertain', 'num_llm_calls': 1, 'method': 'cot'}\\n\\ndef run_selfrefine_baseline(example, prolog, pred_key_index, n_rounds=3):\\n    \\\"\\\"\\\"Self-refine: re-generate KB with global pass-rate feedback.\\\"\\\"\\\"\\n    premises_text = '\\\\n'.join(example['premises'])\\n    oracle_qs = generate_oracle_queries(premises_text)\\n    oracle_as = generate_oracle_answers(premises_text, oracle_qs)\\n    clauses = extract_kb(premises_text)\\n    for r in range(n_rounds):\\n        if COST_TRACKER['total'] >= MAX_BUDGET_USD: break\\n        # Compute pass rate\\n        pred_names = extract_predicate_names_from_clauses(clauses)\\n        clear_kb(prolog, pred_key_index.get('all_preds', []))\\n        load_kb_into_prolog(prolog, clauses)\\n        pred_key_index['all_preds'] = pred_names\\n        pred_sigs = [f'{n}/{a}' for n, a in pred_names]\\n        goals = [nl_query_to_prolog(q, pred_sigs) for q in oracle_qs]\\n        passed = 0\\n        for i, goal in enumerate(goals):\\n            if goal == 'fail': continue\\n            try:\\n                res = list(prolog.query(goal))\\n                if (res and oracle_as[i]=='yes') or (not res and oracle_as[i]!='yes'):\\n                    passed += 1\\n            except: pass\\n        pass_rate = passed / max(len(oracle_qs), 1)\\n        # Re-generate with feedback\\n        refine_prompt = f\\\"\\\"\\\"Your Prolog KB achieved {pass_rate:.0%} on oracle queries.\\nPremises:\\\\n{premises_text}\\\\n\\\\nCurrent KB:\\\\n{'\\\\n'.join(clauses)}\\\\n\\\\nPlease regenerate the KB to improve coverage. Output only Prolog clauses.\\\"\\\"\\\"\\n        response, _ = llm_call([{'role': 'user', 'content': refine_prompt}])\\n        clauses = [l.strip() for l in response.split('\\\\n') if l.strip().endswith('.')]\\n    pred_names = extract_predicate_names_from_clauses(clauses)\\n    clear_kb(prolog, pred_key_index.get('all_preds', []))\\n    load_kb_into_prolog(prolog, clauses)\\n    pred_key_index['all_preds'] = pred_names\\n    predicted = evaluate_conclusion(prolog, example.get('conclusion-FOL',''), clauses, premises_text)\\n    return {'predicted_label': predicted, 'num_llm_calls': 2 + 2*n_rounds, 'method': 'selfrefine'}\\n\\ndef run_flat_sbfl_baseline(example, prolog, pred_key_index):\\n    \\\"\\\"\\\"Flat SBFL: like dual-signal but binary coverage (no stratification), no sub-goal harvesting.\\\"\\\"\\\"\\n    # Same as dual-signal but:\\n    # - coverage_matrix[pred][i] = 1 if accessed (regardless of outcome), 0 if not\\n    # - No missing_predicate_scores\\n    # Implementation mirrors run_dual_sbfl_pipeline but with simplified coverage\\n    # (reuse same code with a flat_mode=True flag)\\n    pass  # Implement as simplified version of main pipeline\\n\\n# ============================================================\\n# STEP 13: MAIN EXPERIMENT LOOP\\n# ============================================================\\ndef main():\\n    import json\\n    from pyswip import Prolog\\n    from datasets import load_dataset\\n    \\n    # Load FOLIO validation split\\n    ds = load_dataset('yale-nlp/FOLIO', split='validation')\\n    examples = list(ds)\\n    \\n    # Smoke test on 5 examples first\\n    test_examples = examples[:5]\\n    full_examples = examples[:150]\\n    \\n    prolog = Prolog()\\n    # Assert meta-interpreter\\n    for line in META_INTERPRETER_PROLOG.strip().split('\\\\n'):\\n        line = line.strip()\\n        if line and not line.startswith('%'):\\n            try:\\n                if line.endswith('.'):\\n                    list(prolog.query(f'assert(({line.rstrip(\\\".\\\")}))'))  # use consult_string or file\\n            except: pass\\n    # Better: write meta-interpreter to a temp .pl file and consult it\\n    # import tempfile, os\\n    # with tempfile.NamedTemporaryFile(suffix='.pl', mode='w', delete=False) as f:\\n    #     f.write(META_INTERPRETER_PROLOG); tmpfile = f.name\\n    # list(prolog.query(f'consult(\\\"{tmpfile}\\\")'))\\n    \\n    results = []\\n    pred_key_index = {'all_preds': []}\\n    \\n    for i, ex in enumerate(full_examples):\\n        if COST_TRACKER['total'] >= MAX_BUDGET_USD:\\n            print(f'Budget exceeded at example {i}. Stopping.')\\n            break\\n        \\n        ex_id = ex.get('story_id', i)\\n        gold = ex['label']  # 'True', 'False', 'Uncertain'\\n        \\n        # Run all methods\\n        try:\\n            dual_sbfl = run_dual_sbfl_pipeline(ex, prolog, pred_key_index)\\n        except Exception as e:\\n            dual_sbfl = {'predicted_label': 'Uncertain', 'error': str(e)}\\n        \\n        try:\\n            oneshot = run_oneshot_baseline(ex, prolog, pred_key_index)\\n        except Exception as e:\\n            oneshot = {'predicted_label': 'Uncertain', 'error': str(e)}\\n        \\n        cot = run_cot_baseline(ex)\\n        \\n        try:\\n            selfrefine = run_selfrefine_baseline(ex, prolog, pred_key_index)\\n        except Exception as e:\\n            selfrefine = {'predicted_label': 'Uncertain', 'error': str(e)}\\n        \\n        result = {\\n            'id': ex_id,\\n            'gold_label': gold,\\n            'predicted_label': dual_sbfl.get('predicted_label'),\\n            'correct': dual_sbfl.get('predicted_label') == gold,\\n            'num_llm_calls': dual_sbfl.get('num_llm_calls', 0),\\n            'num_repairs_applied': dual_sbfl.get('num_repairs_applied', 0),\\n            'ochiai_scores_top5': dual_sbfl.get('ochiai_scores_top5', []),\\n            'subgoal_harvest_top5': dual_sbfl.get('subgoal_harvest_top5', []),\\n            'cumulative_cost_usd': COST_TRACKER['total'],\\n            'baseline_results': {\\n                'oneshot': {'predicted': oneshot.get('predicted_label'), 'correct': oneshot.get('predicted_label') == gold},\\n                'cot': {'predicted': cot.get('predicted_label'), 'correct': cot.get('predicted_label') == gold},\\n                'selfrefine': {'predicted': selfrefine.get('predicted_label'), 'correct': selfrefine.get('predicted_label') == gold},\\n                'flat_sbfl': {}  # fill in if budget permits\\n            }\\n        }\\n        results.append(result)\\n        \\n        # Log progress every 10 examples\\n        if (i+1) % 10 == 0:\\n            acc = sum(r['correct'] for r in results) / len(results)\\n            print(f'[{i+1}/{len(full_examples)}] Dual-SBFL acc={acc:.3f}, cost=${COST_TRACKER[\\\"total\\\"]:.2f}')\\n    \\n    # Compute aggregate metrics\\n    def accuracy(preds, golds): return sum(p==g for p,g in zip(preds,golds))/max(len(golds),1)\\n    \\n    summary = {\\n        'n_examples': len(results),\\n        'total_cost_usd': COST_TRACKER['total'],\\n        'dual_sbfl_accuracy': accuracy([r['predicted_label'] for r in results], [r['gold_label'] for r in results]),\\n        'oneshot_accuracy': accuracy([r['baseline_results']['oneshot']['predicted'] for r in results], [r['gold_label'] for r in results]),\\n        'cot_accuracy': accuracy([r['baseline_results']['cot']['predicted'] for r in results], [r['gold_label'] for r in results]),\\n        'selfrefine_accuracy': accuracy([r['baseline_results']['selfrefine']['predicted'] for r in results], [r['gold_label'] for r in results]),\\n        'avg_repairs_per_example': sum(r['num_repairs_applied'] for r in results) / max(len(results),1),\\n    }\\n    \\n    output = {'summary': summary, 'results': results}\\n    with open('method_out.json', 'w') as f:\\n        json.dump(output, f, indent=2)\\n    print('Done. Results saved to method_out.json')\\n    print(json.dumps(summary, indent=2))\\n\\nif __name__ == '__main__':\\n    main()\\n\\n# ============================================================\\n# CRITICAL IMPLEMENTATION NOTES FOR EXECUTOR\\n# ============================================================\\n# 1. SWI-Prolog must be installed: `apt-get install -y swi-prolog`\\n#    Then: `uv pip install pyswip datasets requests tqdm scipy numpy`\\n# 2. pyswip Prolog class is NOT thread-safe. Use a single instance throughout.\\n# 3. The meta-interpreter MUST be loaded via a .pl file (consult) not assertz.\\n#    Write META_INTERPRETER_PROLOG to /tmp/meta_interp.pl, then:\\n#    list(prolog.query('consult(\\\"/tmp/meta_interp.pl\\\")'))\\n# 4. Between examples, ABOLISH all user predicates. Track predicate names.\\n#    Do NOT use 'abolish' on built-in predicates (true/fail/nl/write etc.).\\n# 5. The FOLIO 'label' field may be 'True'/'False'/'Uncertain' or\\n#    'true'/'false'/'unknown' depending on version. Normalize on load.\\n# 6. The 'premises' field may be a list (use '\\\\n'.join()) or a single string.\\n#    The 'conclusion-FOL' field contains FOL syntax (may include ∀, ∃, →);\\n#    the LLM must translate these to Prolog goals.\\n# 7. OpenRouter API: use model 'meta-llama/llama-3.1-8b-instruct' for cost.\\n#    Budget alert: at 15 oracle queries + 3 repair rounds × 5 items per example,\\n#    worst case ~25 LLM calls per example × 150 examples = 3750 calls.\\n#    At ~$0.00005/call for llama-3.1-8b: ~$0.19 total — well within $8 budget.\\n#    Track costs: prompt_tokens * input_price + completion_tokens * output_price.\\n# 8. FALLBACK for pyswip failures: if pyswip cannot be installed or SWI-Prolog\\n#    is not available, implement a PURE PYTHON Prolog subset (see fallback plan).\\n# 9. Smoke test first: run 5 examples, verify KB extraction produces valid Prolog,\\n#    verify meta-interpreter loads, verify oracle query generation is sensible.\\n# 10. LLM-generated Prolog may contain syntax errors. Wrap every assertz in try/except.\\n#     Log failed assertions. If >50% of clauses fail, flag as extraction failure.\\n# 11. For the flat-SBFL ablation: reuse the same infrastructure but set\\n#     all coverage outcomes to binary (accessed=1, not_accessed=0), skip subgoal harvesting.\\n\",\n  \"fallback_plan\": \"## Fallback 1: pyswip Installation Fails\\nIf SWI-Prolog or pyswip cannot be installed on the target machine:\\n- Implement a minimal pure-Python Prolog interpreter supporting:\\n  - Ground facts: `fact(a).`\\n  - Simple rules: `head(X) :- body(X).`\\n  - Backward-chaining resolution with occurs-check\\n  - depth-limited DFS (depth <= 10)\\n- Python packages: none required beyond stdlib\\n- This covers 80% of FOLIO KB patterns (simple ground facts + chain rules)\\n- The meta-interpreter becomes Python-native, simpler to instrument for coverage tracking\\n\\n## Fallback 2: FOLIO Dataset Access Issues\\nIf `load_dataset('yale-nlp/FOLIO')` fails (auth required or unavailable):\\n- Try `load_dataset('yale-nlp/FOLIO', use_auth_token=True)` with HF_TOKEN env var\\n- Alternatively: download from GitHub raw files:\\n  `https://raw.githubusercontent.com/Yale-LILY/FOLIO/main/data/v0.0/folio-validation.jsonl`\\n  Fetch with requests and parse jsonl manually.\\n- Fields in each line: story_id, premises (list), premises-FOL (list), conclusion, conclusion-FOL, label\\n\\n## Fallback 3: LLM-to-Prolog Conversion Quality Too Low\\nIf >40% of extracted KBs have 0 valid clauses (all fail assertz):\\n- Switch to a more structured extraction prompt: provide a fixed predicate vocabulary\\n  derived from the FOLIO FOL annotations (extract unique predicate names from premises-FOL)\\n- Use the FOL annotations as hints: 'The KB should use predicates: {fol_predicates}'\\n- If that still fails: use the premises-FOL directly, converting FOL syntax to Prolog\\n  (quantifiers → Prolog variables, → becomes :-, ¬ becomes \\\\+)\\n\\n## Fallback 4: Budget Exceeded Early\\nIf $8 budget is consumed before 150 examples:\\n- Report results on however many examples were processed\\n- Reduce to 50 examples with all methods, skip selfrefine baseline (most expensive)\\n- Switch from llama-3.1-8b to a cheaper model if available on OpenRouter\\n  (check /api/v1/models for models with input price < $0.05/M tokens)\\n\\n## Fallback 5: Meta-Interpreter Too Slow or Buggy\\nIf the Prolog meta-interpreter causes timeouts or incorrect results:\\n- Replace meta-interpreter with exception-based coverage: use pyswip's native query\\n  with a timeout, catch PrologError, and infer coverage from which predicates\\n  appear in the error message\\n- Simpler coverage approximation: for each predicate P in KB, run `P(...)` directly\\n  with fresh variables; mark as accessible if it returns any results; mark as\\n  inaccessible (= unify_failed) if it throws or returns []\\n- This loses stratification but retains Ochiai computation\\n\\n## Fallback 6: Conclusion Evaluation Unreliable\\nIf LLM-to-Prolog goal conversion for conclusion-FOL fails frequently:\\n- Use the FOLIO FOL annotations more directly: the conclusion-FOL is often simple\\n  atomic predicates like 'Engaged(bonnie)' — convert with regex:\\n  `re.sub(r'([A-Z][a-zA-Z]*)\\\\(([^)]+)\\\\)', lambda m: f'{m.group(1).lower()}({m.group(2).lower()})', fol)`\\n- As last resort: use LLM-only evaluation (ask LLM if conclusion follows from premises,\\n  classify as True/False/Uncertain from response) — no Prolog needed, but loses\\n  symbolic grounding. Report separately as 'LLM-judge' baseline.\",\n  \"testing_plan\": \"## Phase 0: Environment Smoke Test (5 minutes)\\n1. Install dependencies: `apt-get install -y swi-prolog && uv pip install pyswip datasets requests`\\n2. Test pyswip: `python -c \\\"from pyswip import Prolog; p=Prolog(); p.assertz('happy(john)'); print(list(p.query('happy(X)')))\\\"`\\n   Expected: `[{'X': 'john'}]`\\n3. Test FOLIO load: `python -c \\\"from datasets import load_dataset; ds=load_dataset('yale-nlp/FOLIO',split='validation'); print(ds[0].keys()); print(ds[0]['label'])\\\"`\\n   Expected: keys include 'premises', 'conclusion', 'conclusion-FOL', 'label'\\n4. Test OpenRouter: simple LLM call with `meta-llama/llama-3.1-8b-instruct`, verify cost tracking works\\n5. Load meta-interpreter .pl file via consult, run `solve(true, 5)`, verify returns `[{}]`\\n\\n## Phase 1: Single Example Dry Run (10 minutes)\\n1. Take FOLIO example index 0\\n2. Run extraction prompt → verify output contains valid Prolog clauses (check `.` endings)\\n3. Assert clauses into Prolog, run a trivial query, verify no exception\\n4. Run oracle query generation → verify 10+ questions generated\\n5. Convert 3 questions to Prolog goals manually, run through meta-interpreter\\n6. Verify coverage_log/3 and failed_subgoal/1 are populated after failing query\\n7. Compute Ochiai scores manually on this example, check values in [0,1]\\n8. Run one repair round, check new clauses are valid Prolog\\n9. Run conclusion evaluation, check output is 'True'/'False'/'Uncertain'\\n10. Log: total cost for this example, verify < $0.05\\n\\n## Phase 2: 5-Example Validation (15 minutes)\\n1. Run all 4 methods (dual-SBFL, oneshot, CoT, selfrefine) on first 5 FOLIO examples\\n2. Check: no crashes, all methods return valid labels\\n3. Check: dual-SBFL accuracy >= oneshot (even on 5 examples, shouldn't be worse)\\n4. Check: cost per example < $0.20 average (5 × $0.20 = $1.00 max for smoke test)\\n5. Inspect ochiai_scores_top5 and subgoal_harvest_top5 for sanity:\\n   - Are the suspicious predicates actually in failing queries?\\n   - Are missing subgoals actually absent from KB?\\n6. Confirm method_out.json is written with correct schema\\n\\n## Phase 3: 50-Example Run (1-2 hours)\\n1. Run all methods on first 50 examples\\n2. Check for runtime errors after each batch of 10\\n3. Monitor cost: should be < $2 for 50 examples\\n4. Check accuracy distribution: label breakdown (True/False/Uncertain) should match FOLIO distribution\\n5. Compute preliminary accuracy numbers; if dual-SBFL < oneshot by >10%, investigate\\n   (likely issue: KB extraction quality too low, or conclusion evaluation broken)\\n6. Check avg_repairs_per_example: should be > 0 (repairs are happening)\\n\\n## Phase 4: Full 150-Example Run (2-4 hours)\\n1. Run remaining examples (50-150)\\n2. Monitor budget: stop if > $7 cumulative\\n3. Compute final summary statistics\\n4. Validate output schema: all required fields present in every result\\n5. Run aii-json validation if schema available\\n\\n## Key Sanity Checks\\n- Label distribution in predictions should roughly match gold: ~40% True, ~30% False, ~30% Uncertain (approx FOLIO distribution)\\n- If all predictions are 'Uncertain', conclusion evaluation is broken\\n- If dual-SBFL and oneshot have identical predictions on all examples, repair is not applying\\n- Ochiai scores should vary per example (not all zero and not all one)\\n- Cost tracking: print cumulative cost every 10 examples\"\n}\n", "structuredPatch": [], "originalFile": null, "userModified": false}
```

### [21] ASSISTANT · 2026-06-07 04:58:37 UTC

```
The plan has been written to `.terminal_claude_agent_struct_out.json`. Here's a summary of what it covers:

**Dual-Signal SBFL Pipeline Plan:**

- **Setup**: SWI-Prolog + pyswip, FOLIO dataset from HuggingFace (yale-nlp/FOLIO validation split), OpenRouter with `meta-llama/llama-3.1-8b-instruct` for cost efficiency (~$0.19 total for 150 examples at worst case).

- **Core pipeline per example**: (1) KB extraction, (2) oracle query generation + answers, (3) NL→Prolog goal conversion, (4) meta-interpreter with stratified coverage tracking, (5) Ochiai computation for direct-fault predicates + sub-goal frequency for missing predicates, (6) 3-round repair loop targeting top-5 items.

- **Key implementation detail**: The Prolog meta-interpreter is written to a `.pl` file and loaded via `consult()` — not assertz'd line-by-line — to avoid syntax issues. It logs `coverage_log/3` and `failed_subgoal/1` facts for post-hoc analysis.

- **4 baselines**: one-shot extraction, CoT, self-refine with global pass-rate feedback, and flat-SBFL ablation (binary coverage, no sub-goal harvesting).

- **Compute**: `cpu_heavy` profile (4 vCPUs, 32GB RAM) — no GPU needed for this pipeline.

- **Fallbacks**: Pure-Python Prolog interpreter if pyswip fails; GitHub raw file download if HuggingFace auth fails; exception-based coverage approximation if meta-interpreter is unreliable.
```
