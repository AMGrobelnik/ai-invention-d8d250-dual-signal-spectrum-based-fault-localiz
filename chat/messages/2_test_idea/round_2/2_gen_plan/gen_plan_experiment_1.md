# gen_plan_experiment_1 — test_idea

> Phase: `invention_loop` · round 2 · `gen_plan`
> Run: `run_ITc1Qruy7fap` — Text To Logic Pipeline
>
> Full, verbatim transcript of this agent task — every system/user prompt, assistant response, thinking block, tool call and tool result — in the order they occurred. Nothing truncated.

## Task: `gen_plan_experiment_1` (terminal_claude_agent, claude-sonnet-4-6)

### [1] CONFIG · 2026-06-07 06:18:40 UTC

```
model: claude-sonnet-4-6 | effort: low | permission: bypassPermissions | cwd: /ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_2/gen_plan/gen_plan_experiment_1
```

### [2] ASSISTANT · 2026-06-07 06:18:50 UTC

```
I'll read the dependency dataset structure and then write the plan.
```

### [3] TOOL CALL — Read · 2026-06-07 06:18:50 UTC

```
/ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_art/gen_art_dataset_1/mini_data_out.json
```

### [4] TOOL RESULT — Read · 2026-06-07 06:18:50 UTC

```
{"type": "text", "file": {"filePath": "/ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_art/gen_art_dataset_1/mini_data_out.json", "content": "{\n  \"metadata\": {\n    \"description\": \"FOLIO + ProofWriter-NL dataset for Dual-Signal SBFL evaluation\",\n    \"folio_source\": \"HannaAbiAkl/FOLIO-KR (mirrors yale-nlp/FOLIO with additional KR notations)\",\n    \"proofwriter_source\": \"tasksource/ruletaker config=depth-3ext-NatLang\",\n    \"total_examples\": 1504\n  },\n  \"datasets\": [\n    {\n      \"dataset\": \"folio\",\n      \"examples\": [\n        {\n          \"input\": \"Premises: Tyga is a rapper. Rappers release rap albums. Tyga released the Well Done 3 album. Rappers are not opera singers.\\nConclusion: Well Done 3 is worth listening to.\",\n          \"output\": \"Unknown\",\n          \"metadata_fold\": \"train\",\n          \"metadata_split\": \"train\",\n          \"metadata_is_calibration_doc\": false,\n          \"metadata_gold_label\": \"Unknown\",\n          \"metadata_premises_text\": \"Tyga is a rapper. Rappers release rap albums. Tyga released the Well Done 3 album. Rappers are not opera singers.\",\n          \"metadata_conclusion\": \"Well Done 3 is worth listening to.\",\n          \"metadata_gold_fol_premises\": \"[\\\"IsRapper(tyga)\\\", \\\"\\\\u2200x \\\\u2200y ((IsRapper(x) \\\\u2227 ReleasedAlbum(x, y)) \\\\u2192 IsRapAlbum(y))\\\", \\\"ReleasedAlbum(tyga, wellDone3)\\\", \\\"\\\\u2200x (IsRapper(x) \\\\u2192 \\\\u00acIsOperaSinger(x))\\\"]\",\n          \"metadata_gold_fol_conclusion\": \"IsWorthListening(wellDone3)\",\n          \"metadata_story_id\": \"36\",\n          \"metadata_example_id\": \"106\",\n          \"metadata_task_type\": \"classification\",\n          \"metadata_fold_name\": \"folio\"\n        },\n        {\n          \"input\": \"Premises: All people in this tech company who are consistent and enjoy sticking to their regular routines do not like surprises. People in this tech company who wear the same flannel shirts every day are consistent and enjoy sticking to their regular routines. People in this tech company who do not like shopping for clothes wear the same flannel shirts every day. Old people living in stable homes do not like surprises. People in this tech company who have very high energy and are impulsive like surprises. Mike works in this tech company. If Mike is not a person who wears the same flannel shirts every day, has very high energy, and is impulsive, then Mike either is very consistent and enjoys sticking to his regular routines or does not like surprises.\\nConclusion: Mike is an old person living in a stable home.\",\n          \"output\": \"Unknown\",\n          \"metadata_fold\": \"train\",\n          \"metadata_split\": \"train\",\n          \"metadata_is_calibration_doc\": false,\n          \"metadata_gold_label\": \"Unknown\",\n          \"metadata_premises_text\": \"All people in this tech company who are consistent and enjoy sticking to their regular routines do not like surprises. People in this tech company who wear the same flannel shirts every day are consistent and enjoy sticking to their regular routines. People in this tech company who do not like shopping for clothes wear the same flannel shirts every day. Old people living in stable homes do not like surprises. People in this tech company who have very high energy and are impulsive like surprises. Mike works in this tech company. If Mike is not a person who wears the same flannel shirts every day, has very high energy, and is impulsive, then Mike either is very consistent and enjoys sticking to his regular routines or does not like surprises.\",\n          \"metadata_conclusion\": \"Mike is an old person living in a stable home.\",\n          \"metadata_gold_fol_premises\": \"[\\\"\\\\u2200x (InThisTechCompany(x) \\\\u2227 Consistent(x) \\\\u2227 StickTo(x, theirRegularRoutine) \\\\u2192 \\\\u00acLike(x, surprise))\\\", \\\"\\\\u2200x (InThisTechCompany(x) \\\\u2227 \\\\u2203y (flannelShirt(y) \\\\u2227 WearEveryday(x, y)) \\\\u2192 Consistent(x) \\\\u2227 StickTo(x, theirRegularRoutine))\\\", \\\"\\\\u2200x (InThisTechCompany(x) \\\\u2227 \\\\u00acLikeShoppingFor(x, clothes) \\\\u2192 \\\\u2203y (flannelShirt(y) \\\\u2227 WearEveryday(x, y)))\\\", \\\"\\\\u2200x (InThisTechCompany(x) \\\\u2227 Old(x) \\\\u2227 LiveIn(x, stableHome) \\\\u2192 \\\\u00acLike(x, surprise))\\\", \\\"\\\\u2200x (InThisTechCompany(x) \\\\u2227 Have(x, highEnergy) \\\\u2227 Impulsive(x) \\\\u2192 \\\\u00acLike(x, surprise))\\\", \\\"InThisTechCompany(mike)\\\", \\\"\\\\u00ac(\\\\u2203y (flannelShirt(y) \\\\u2227 WearEveryday(x, y)) \\\\u2227 Have(mike, highEnergy) \\\\u2227 Impulsive(mike)) \\\\u2192 (Consistent(mike) \\\\u2227 StickTo(mike, theirRegularRoutine)) \\\\u2295 \\\\u00acLike(mike, surprise)\\\"]\",\n          \"metadata_gold_fol_conclusion\": \"Old(mike) ∧ LiveIn(mike, stableHome)\",\n          \"metadata_story_id\": \"376\",\n          \"metadata_example_id\": \"1002\",\n          \"metadata_task_type\": \"classification\",\n          \"metadata_fold_name\": \"folio\"\n        },\n        {\n          \"input\": \"Premises: No trick-shot artist in Yale's varsity team struggles with half court shots. Everyone on Yale's varsity team is someone who struggles with half court shots or who successfully shoots a high percentage of 3-pointers. Everyone on Yale's varsity team who successfully shoots a high percentage of 3-pointers is solid at shooting 2-pointers. No one on Yale's varsity team who is solid at shooting 2-pointers is bad at mid-range shots. Jack is on Yale's varsity team, and he is either a trick-shot artist or he successfully shoots a high percentage of 3-pointers.\\nConclusion: Jack is solid at shooting 2-pointers or bad at mid-range shots.\",\n          \"output\": \"True\",\n          \"metadata_fold\": \"train\",\n          \"metadata_split\": \"train\",\n          \"metadata_is_calibration_doc\": false,\n          \"metadata_gold_label\": \"True\",\n          \"metadata_premises_text\": \"No trick-shot artist in Yale's varsity team struggles with half court shots. Everyone on Yale's varsity team is someone who struggles with half court shots or who successfully shoots a high percentage of 3-pointers. Everyone on Yale's varsity team who successfully shoots a high percentage of 3-pointers is solid at shooting 2-pointers. No one on Yale's varsity team who is solid at shooting 2-pointers is bad at mid-range shots. Jack is on Yale's varsity team, and he is either a trick-shot artist or he successfully shoots a high percentage of 3-pointers.\",\n          \"metadata_conclusion\": \"Jack is solid at shooting 2-pointers or bad at mid-range shots.\",\n          \"metadata_gold_fol_premises\": \"[\\\"\\\\u2200x ((In(x, yaleSVarsityTeam) \\\\u2227 TrickShotArtist(x)) \\\\u2192 \\\\u00acStruggleAt(x, halfCourtShot))\\\", \\\"\\\\u2200x (In(x, yaleSVarsityTeam) \\\\u2192 (StruggleAt(x, halfCourtShot) \\\\u2228 GoodAt(x, threes)))\\\", \\\"\\\\u2200x ((In(x, yaleSVarsityTeam) \\\\u2227 GoodAt(x, threes)) \\\\u2192 GoodAt(x, twos))\\\", \\\"\\\\u2200x ((In(x, yaleSVarsityTeam) \\\\u2227 GoodAt(x, twos)) \\\\u2192 BadAt(x, midRangeShot))\\\", \\\"In(jack, yaleSVarsityTeam) \\\\u2227 (TrickShotArtist(jack) \\\\u2295  GoodAt(jack, threes))\\\"]\",\n          \"metadata_gold_fol_conclusion\": \"GoodAt(jack, twos) ∨ BadAt(jack, midRangeShot)\",\n          \"metadata_story_id\": \"408\",\n          \"metadata_example_id\": \"1135\",\n          \"metadata_task_type\": \"classification\",\n          \"metadata_fold_name\": \"folio\"\n        }\n      ]\n    },\n    {\n      \"dataset\": \"proofwriter_ruletaker_depth3\",\n      \"examples\": [\n        {\n          \"input\": \"Premises: The bear chases the rabbit. The bear is blue. The bear is green. The cat is cold. The rabbit chases the cat. The rabbit is nice. The rabbit visits the bear. If someone chases the cat and the cat visits the rabbit then they visit the cat. If someone visits the cat and the cat is cold then they chase the rabbit. If someone chases the rabbit then the rabbit visits the cat. If someone chases the cat and they chase the rabbit then the cat chases the bear. If someone eats the cat and they chase the bear then the bear eats the cat. If the cat chases the bear then the bear is blue.\\nConclusion: The cat does not chase the rabbit.\",\n          \"output\": \"True\",\n          \"metadata_fold\": \"test\",\n          \"metadata_split\": \"test\",\n          \"metadata_is_calibration_doc\": false,\n          \"metadata_gold_label\": \"True\",\n          \"metadata_premises_text\": \"The bear chases the rabbit. The bear is blue. The bear is green. The cat is cold. The rabbit chases the cat. The rabbit is nice. The rabbit visits the bear. If someone chases the cat and the cat visits the rabbit then they visit the cat. If someone visits the cat and the cat is cold then they chase the rabbit. If someone chases the rabbit then the rabbit visits the cat. If someone chases the cat and they chase the rabbit then the cat chases the bear. If someone eats the cat and they chase the bear then the bear eats the cat. If the cat chases the bear then the bear is blue.\",\n          \"metadata_conclusion\": \"The cat does not chase the rabbit.\",\n          \"metadata_gold_fol_premises\": \"[]\",\n          \"metadata_gold_fol_conclusion\": \"\",\n          \"metadata_story_id\": \"\",\n          \"metadata_example_id\": \"0\",\n          \"metadata_task_type\": \"classification\",\n          \"metadata_fold_name\": \"proofwriter\"\n        },\n        {\n          \"input\": \"Premises: Bob is white. Dave is quiet. Fiona is furry. Harry is quiet. If Bob is rough then Bob is furry. All kind things are blue. If something is furry then it is rough. All rough, furry things are young. If something is young then it is kind. Rough, furry things are kind.\\nConclusion: Dave is not white.\",\n          \"output\": \"True\",\n          \"metadata_fold\": \"test\",\n          \"metadata_split\": \"test\",\n          \"metadata_is_calibration_doc\": false,\n          \"metadata_gold_label\": \"True\",\n          \"metadata_premises_text\": \"Bob is white. Dave is quiet. Fiona is furry. Harry is quiet. If Bob is rough then Bob is furry. All kind things are blue. If something is furry then it is rough. All rough, furry things are young. If something is young then it is kind. Rough, furry things are kind.\",\n          \"metadata_conclusion\": \"Dave is not white.\",\n          \"metadata_gold_fol_premises\": \"[]\",\n          \"metadata_gold_fol_conclusion\": \"\",\n          \"metadata_story_id\": \"\",\n          \"metadata_example_id\": \"1\",\n          \"metadata_task_type\": \"classification\",\n          \"metadata_fold_name\": \"proofwriter\"\n        },\n        {\n          \"input\": \"Premises: Anne is white. Charlie is quiet. Gary is young. Harry is blue. If something is smart and white then it is blue. All blue things are quiet. All furry, green things are blue. If something is smart then it is furry. If Anne is white then Anne is smart. Young, green things are blue. If Charlie is green and Charlie is young then Charlie is quiet. If something is blue then it is green.\\nConclusion: Charlie is quiet.\",\n          \"output\": \"True\",\n          \"metadata_fold\": \"test\",\n          \"metadata_split\": \"test\",\n          \"metadata_is_calibration_doc\": false,\n          \"metadata_gold_label\": \"True\",\n          \"metadata_premises_text\": \"Anne is white. Charlie is quiet. Gary is young. Harry is blue. If something is smart and white then it is blue. All blue things are quiet. All furry, green things are blue. If something is smart then it is furry. If Anne is white then Anne is smart. Young, green things are blue. If Charlie is green and Charlie is young then Charlie is quiet. If something is blue then it is green.\",\n          \"metadata_conclusion\": \"Charlie is quiet.\",\n          \"metadata_gold_fol_premises\": \"[]\",\n          \"metadata_gold_fol_conclusion\": \"\",\n          \"metadata_story_id\": \"\",\n          \"metadata_example_id\": \"2\",\n          \"metadata_task_type\": \"classification\",\n          \"metadata_fold_name\": \"proofwriter\"\n        }\n      ]\n    }\n  ]\n}", "numLines": 116, "startLine": 1, "totalLines": 116}}
```

### [5] ASSISTANT · 2026-06-07 06:21:50 UTC

```
Now I have enough context to write the plan.
```

### [6] TOOL CALL — Write · 2026-06-07 06:21:50 UTC

````
File: /ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_2/gen_plan/gen_plan_experiment_1/.terminal_claude_agent_struct_out.json

{
  "title": "Dual-Signal SBFL: Perturbation Calibration, Stronger Oracle, and Hop-Depth Completeness Analysis",
  "summary": "Three sub-experiments using ProofWriter and FOLIO data from gen_art_dataset_1: (A) perturbation-based oracle calibration with injected synthetic errors on 10 docs, (B) GPT-4o-mini oracle vs Haiku oracle quality comparison on 40 examples, (C) KB structural completeness by hop depth (1/2/3-hop) with SBFL repair on 1-hop examples. All LLM calls via OpenRouter; budget cap $6.",
  "runpod_compute_profile": "cpu_heavy",
  "implementation_pseudocode": "## Setup and Dependencies\n\n```\npip install pyswip openai requests scipy numpy\n# pyswip requires SWI-Prolog installed: apt-get install swi-prolog\n# All LLM calls go through OpenRouter using openai-compatible client\n```\n\n## Data Loading\n\n```python\nDATASET_PATH = '/ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_art/gen_art_dataset_1/full_data_out.json'\n\ndef load_proofwriter_examples(path):\n    data = json.load(open(path))\n    proofwriter = [ds for ds in data['datasets'] if ds['dataset'] == 'proofwriter_ruletaker_depth3'][0]\n    return proofwriter['examples']  # 300 examples\n\n# ProofWriter examples don't have hop-depth annotations in the dataset.\n# Proxy hop-depth from rule chain length:\n# Parse premises: count conditional rules (\"If ... then ...\"); count transitive steps needed.\n# Simple heuristic: examples with <=3 rules = 1-hop, 4-6 rules = 2-hop, 7+ rules = 3-hop.\n# Alternatively: use 'depth-3ext-NatLang' config label and split by index thirds for stratification.\n# Better approach: detect hop depth by trying to find shortest proof chain in meta-interpreter.\ndef estimate_hop_depth(premises_text):\n    rules = [s for s in premises_text.split('.') if s.strip().lower().startswith('if')]\n    n_rules = len(rules)\n    if n_rules <= 2: return 1\n    elif n_rules <= 5: return 2\n    else: return 3\n```\n\n## OpenRouter LLM Client\n\n```python\nimport openai\nclient = openai.OpenAI(\n    api_key=os.environ['OPENROUTER_API_KEY'],\n    base_url='https://openrouter.ai/api/v1'\n)\nHAIKU_MODEL = 'anthropic/claude-haiku-3-5'  # cheap extractor/repair\nGPT4O_MINI_MODEL = 'openai/gpt-4o-mini'  # stronger oracle\n\n# Track cumulative cost\ncumulative_cost_usd = 0.0\nBUDGET_CAP = 6.0\ndef check_budget():\n    if cumulative_cost_usd > BUDGET_CAP:\n        raise RuntimeError(f'Budget cap ${BUDGET_CAP} reached at ${cumulative_cost_usd:.3f}')\n\n# Cost estimation: Claude Haiku ~$0.25/M input + $1.25/M output; GPT-4o-mini ~$0.15/M input + $0.60/M output\n# Track via response.usage.prompt_tokens and completion_tokens\ndef estimate_cost(model, prompt_tokens, completion_tokens):\n    if 'haiku' in model:\n        return prompt_tokens * 0.25e-6 + completion_tokens * 1.25e-6\n    elif 'gpt-4o-mini' in model:\n        return prompt_tokens * 0.15e-6 + completion_tokens * 0.60e-6\n    return 0\n```\n\n## Core Components\n\n### 1. KB Extractor (LLM Call 1 — Haiku)\n\n```python\nKB_EXTRACTION_PROMPT = '''\nYou are a logic expert. Convert the following natural language premises into SWI-Prolog facts and rules.\nOutput ONLY valid Prolog clauses, one per line. No explanations.\nUse lowercase atoms for constants. Use snake_case for predicate names.\nFor rules \"If X then Y\", use: head :- body.\nFor universal rules \"All X are Y\", use: y(X) :- x(X).\nFor negations \"X is not Y\", use: \\+ y(X) in body, or represent as not_y(X) fact.\n\nPremises:\n{premises}\n\nOutput Prolog KB:\n'''\n\ndef extract_kb(premises_text, model=HAIKU_MODEL):\n    # Returns list of Prolog clause strings\n    response = client.chat.completions.create(\n        model=model,\n        messages=[{'role': 'user', 'content': KB_EXTRACTION_PROMPT.format(premises=premises_text)}],\n        max_tokens=800, temperature=0.0\n    )\n    # parse response into list of clauses\n    clauses = [line.strip() for line in response.choices[0].message.content.split('\\n') \n               if line.strip() and not line.strip().startswith('%')]\n    return clauses, response.usage\n```\n\n### 2. Prolog Meta-Interpreter with Stratified Coverage\n\n```python\n# SWI-Prolog via pyswip\nfrom pyswip import Prolog\n\ndef load_kb_to_prolog(clauses):\n    prolog = Prolog()\n    for clause in clauses:\n        try:\n            prolog.assertz(clause.rstrip('.'))\n        except Exception:\n            pass  # skip malformed clauses; log them\n    return prolog\n\ndef run_query_with_trace(prolog, query_str):\n    \"\"\"\n    Returns:\n      - success: bool\n      - accessed_predicates: dict {pred_name: 'direct_fail'|'subgoal_fail'|'success'}\n      - failed_subgoals: list of predicate names that were attempted but never resolved\n    \"\"\"\n    # Use Prolog meta-interpreter to capture trace\n    # Inject a tracing meta-interpreter into SWI-Prolog\n    # See: https://www.swi-prolog.org/pldoc/man?section=metainterp\n    # \n    # Strategy: use SWI-Prolog's built-in trace/1 or implement a simple meta-interpreter\n    # Simple approach: use catch/3 + call/1 with assertz to record accessed predicates\n    #\n    # Inject meta-interpreter clauses once at setup:\n    META_INTERPRETER = '''\n    solve(true) :- !.\n    solve((A,B)) :- !, solve(A), solve(B).\n    solve(Goal) :-\n        functor(Goal, Name, Arity),\n        assert(accessed(Name/Arity)),\n        (clause(Goal, Body) ->\n            (solve(Body) -> assert(resolved(Name/Arity)) ; assert(subgoal_failed(Name/Arity)), fail)\n        ;\n            assert(unif_failed(Name/Arity)), fail\n        ).\n    '''\n    # Load meta-interpreter; clear state; run; collect results\n    # For pyswip, use prolog.query() and assertz/retract patterns\n    pass  # see detailed implementation note below\n\n# IMPLEMENTATION NOTE for executor:\n# pyswip runs SWI-Prolog in-process. Use prolog.assertz() to add meta-interpreter clauses.\n# Between queries, retract all dynamic facts (accessed/1, resolved/1, etc.) to reset state.\n# Run: list(prolog.query(f'solve({query_str})')) to attempt proof.\n# Then query: list(prolog.query('accessed(X)')) to get accessed predicates.\n# Then query: list(prolog.query('unif_failed(X)')) for direct-fail predicates.\n# Then query: list(prolog.query('subgoal_failed(X)')) for indirect-fail predicates.\n# This gives the stratified coverage spectrum per query.\n```\n\n### 3. Oracle Generator (LLM Call 2)\n\n```python\nORACLE_GEN_PROMPT = '''\nGiven these natural language premises and a target conclusion, generate {n_queries} yes/no reasoning questions.\nEach question should test a specific logical relationship that can be verified from the premises.\nHalf should be answerable TRUE, half FALSE from the premises.\nOutput JSON: [{{\"query\": \"...\", \"expected\": true/false}}, ...]\n\nPremises: {premises}\nConclusion: {conclusion}\n'''\n\ndef generate_oracle_queries(premises_text, conclusion, n_queries=10, model=HAIKU_MODEL):\n    # Returns list of {query_nl: str, expected: bool}\n    response = client.chat.completions.create(\n        model=model,\n        messages=[{'role': 'user', 'content': ORACLE_GEN_PROMPT.format(\n            premises=premises_text, conclusion=conclusion, n_queries=n_queries)}],\n        max_tokens=600, temperature=0.3\n    )\n    queries = json.loads(response.choices[0].message.content)\n    return queries, response.usage\n\nNL_TO_PROLOG_QUERY_PROMPT = '''\nConvert this yes/no question to a SWI-Prolog query (just the goal, no :-). \nUse the predicate names from this KB:\n{kb_clauses}\n\nQuestion: {question}\nIf expected answer is True, write a goal that should SUCCEED.\nIf expected answer is False, write a goal that should FAIL (prefix with \\+).\nOutput only the Prolog goal.\n'''\n\ndef nl_query_to_prolog(nl_query, expected, kb_clauses, model=HAIKU_MODEL):\n    # Returns prolog goal string\n    response = client.chat.completions.create(\n        model=model,\n        messages=[{'role': 'user', 'content': NL_TO_PROLOG_QUERY_PROMPT.format(\n            kb_clauses='\\n'.join(kb_clauses), question=nl_query)}],\n        max_tokens=100, temperature=0.0\n    )\n    return response.choices[0].message.content.strip(), response.usage\n```\n\n### 4. Ochiai Suspiciousness + Sub-Goal Harvesting\n\n```python\nimport numpy as np\nfrom scipy.stats import spearmanr\n\ndef compute_ochiai(coverage_matrix):\n    \"\"\"\n    coverage_matrix: dict {pred: {qid: {'accessed': bool, 'unified': bool, 'direct_fail': bool}}}\n    Returns dict {pred: ochiai_score}\n    \"\"\"\n    # Standard Ochiai: suspiciousness(p) = ef / sqrt((ef+nf) * (ef+ep))\n    # ef = queries that FAIL and access p; nf = queries that fail and DON'T access p\n    # ep = queries that PASS and access p\n    scores = {}\n    pass_ids = {qid for qid, result in query_results.items() if result['success']}\n    fail_ids = {qid for qid, result in query_results.items() if not result['success']}\n    for pred, coverage in coverage_matrix.items():\n        ef = sum(1 for qid in fail_ids if coverage.get(qid, {}).get('accessed', False))\n        ep = sum(1 for qid in pass_ids if coverage.get(qid, {}).get('accessed', False))\n        nf = sum(1 for qid in fail_ids if not coverage.get(qid, {}).get('accessed', False))\n        denom = np.sqrt((ef + nf) * (ef + ep))\n        scores[pred] = ef / denom if denom > 0 else 0.0\n    return scores\n\ndef harvest_failed_subgoals(failed_subgoal_counts):\n    \"\"\"Returns dict {pred: frequency_count} for predicates never resolved.\"\"\"\n    # Already tracked during meta-interpreter runs\n    return dict(sorted(failed_subgoal_counts.items(), key=lambda x: -x[1]))\n\ndef build_repair_agenda(ochiai_scores, missing_pred_scores, K=5):\n    \"\"\"\n    Merge Ochiai scores (erroneous predicates) and missing predicate scores.\n    Normalize each to [0,1] then rank by combined score.\n    Returns top-K items as [{'type': 'erroneous'|'missing', 'pred': str, 'score': float}]\n    \"\"\"\n    max_o = max(ochiai_scores.values(), default=1)\n    max_m = max(missing_pred_scores.values(), default=1)\n    combined = {}\n    for p, s in ochiai_scores.items():\n        combined[p] = {'score': s/max_o, 'type': 'erroneous'}\n    for p, s in missing_pred_scores.items():\n        if p not in combined:\n            combined[p] = {'score': s/max_m, 'type': 'missing'}\n        else:\n            combined[p]['score'] = max(combined[p]['score'], s/max_m)\n    ranked = sorted(combined.items(), key=lambda x: -x[1]['score'])\n    return [{'pred': p, **v} for p, v in ranked[:K]]\n```\n\n### 5. Repair (LLM Call 3 — Haiku)\n\n```python\nREPAIR_PROMPT = '''\nYou are debugging a Prolog knowledge base extracted from natural language.\nThe predicate '{pred}' has been flagged as suspicious (type: {error_type}).\n\nOriginal premises:\n{premises}\n\nCurrent KB clauses:\n{kb_clauses}\n\nFor type 'erroneous': replace or fix the clause for '{pred}'.\nFor type 'missing': add a new clause for '{pred}' that the premises imply.\n\nOutput ONLY the corrected or new Prolog clause(s), one per line.\n'''\n\ndef repair_predicate(pred, error_type, premises_text, kb_clauses, model=HAIKU_MODEL):\n    response = client.chat.completions.create(\n        model=model,\n        messages=[{'role': 'user', 'content': REPAIR_PROMPT.format(\n            pred=pred, error_type=error_type,\n            premises=premises_text, kb_clauses='\\n'.join(kb_clauses))}],\n        max_tokens=300, temperature=0.0\n    )\n    new_clauses = [line.strip() for line in response.choices[0].message.content.split('\\n')\n                   if line.strip() and not line.strip().startswith('%')]\n    return new_clauses, response.usage\n```\n\n## Sub-experiment A — Perturbation Oracle Calibration (10 docs)\n\n```python\nimport random\n\ndef inject_synthetic_errors(clauses, k=5, seed=42):\n    \"\"\"\n    Inject k synthetic errors into KB clauses. Record ground-truth perturbed predicates.\n    Error types:\n      - delete_fact: remove a unit clause (fact)\n      - rename_predicate: change predicate name in one clause to a nonsense variant\n      - alter_argument: change an atom argument in one fact\n    Returns: (perturbed_clauses, ground_truth_perturbed_preds: list of pred names)\n    \"\"\"\n    rng = random.Random(seed)\n    facts = [c for c in clauses if ':-' not in c]\n    rules = [c for c in clauses if ':-' in c]\n    perturbed = list(clauses)\n    gt_preds = []\n    errors_done = 0\n    \n    # Attempt k errors with mix of types\n    error_types = rng.choices(['delete_fact', 'rename_predicate', 'alter_argument'], k=k)\n    for etype in error_types:\n        if etype == 'delete_fact' and facts:\n            target = rng.choice(facts)\n            perturbed.remove(target)\n            pred_name = target.split('(')[0].strip()\n            gt_preds.append(pred_name)\n            facts.remove(target)\n        elif etype == 'rename_predicate' and perturbed:\n            idx = rng.randint(0, len(perturbed)-1)\n            old = perturbed[idx]\n            # Find first predicate name and rename it with '_broken' suffix\n            import re\n            match = re.match(r'([a-z_][a-z0-9_]*)', old)\n            if match:\n                orig_name = match.group(1)\n                perturbed[idx] = old.replace(orig_name, orig_name + '_broken', 1)\n                gt_preds.append(orig_name)\n        elif etype == 'alter_argument' and facts:\n            target = rng.choice(facts)\n            idx = perturbed.index(target)\n            # Replace first atom argument with 'wrong_value'\n            altered = re.sub(r'\\(([a-z_][a-z0-9_]*)', '(wrong_value', target, count=1)\n            perturbed[idx] = altered\n            gt_preds.append(target.split('(')[0].strip())\n    return perturbed, list(set(gt_preds))\n\ndef run_sub_experiment_A(examples, n_docs=10):\n    \"\"\"\n    For 10 ProofWriter docs:\n    1. Extract KB via Haiku\n    2. Inject 5 synthetic errors\n    3. Generate 10 oracle queries via Haiku\n    4. Run meta-interpreter on perturbed KB for each query\n    5. Compute Ochiai scores\n    6. Compute Spearman rho vs ground-truth perturbed predicates\n       (GT ranking: perturbed preds at rank 1..k, others at rank k+1)\n    7. Report mean rho\n    \"\"\"\n    docs = examples[:n_docs]  # first 10 ProofWriter examples\n    per_doc_rho = []\n    \n    for i, ex in enumerate(docs):\n        check_budget()\n        premises_text = ex['metadata_premises_text']\n        conclusion = ex['metadata_conclusion']\n        \n        # Step 1: Extract KB\n        clauses, usage = extract_kb(premises_text)\n        update_cost(HAIKU_MODEL, usage)\n        \n        # Step 2: Inject errors\n        perturbed_clauses, gt_perturbed_preds = inject_synthetic_errors(clauses, k=5, seed=i)\n        \n        # Step 3: Generate oracle queries (on perturbed KB)\n        oracle_queries, usage = generate_oracle_queries(premises_text, conclusion, n_queries=10)\n        update_cost(HAIKU_MODEL, usage)\n        \n        # Step 4-5: Run meta-interpreter, compute Ochiai\n        ochiai_scores, missing_scores = run_sbfl_pipeline(\n            perturbed_clauses, oracle_queries, premises_text)\n        \n        # Step 6: Compute Spearman rho\n        # All predicates in original clauses as candidates\n        all_preds = list(set(\n            c.split('(')[0].strip().split(':-')[0].strip() for c in clauses if c.strip()\n        ))\n        # Ground-truth ranking: gt_perturbed_preds get rank 1 (most suspicious), others get n+1\n        gt_ranks = {p: (1 if p in gt_perturbed_preds else len(gt_perturbed_preds)+1)\n                    for p in all_preds}\n        ochiai_ranks = {p: ochiai_scores.get(p, 0) for p in all_preds}\n        \n        # Spearman between ochiai score and (inverted) gt rank\n        shared = [p for p in all_preds if p in ochiai_scores or p in gt_perturbed_preds]\n        if len(shared) < 3:\n            per_doc_rho.append(0.0)  # not enough data\n            continue\n        gt_vec = [-gt_ranks[p] for p in shared]  # negative so higher = more suspicious\n        ochiai_vec = [ochiai_scores.get(p, 0) for p in shared]\n        rho, _ = spearmanr(ochiai_vec, gt_vec)\n        per_doc_rho.append(float(rho) if not np.isnan(rho) else 0.0)\n    \n    return {\n        'mean_rho': float(np.mean(per_doc_rho)),\n        'per_doc_rho': per_doc_rho\n    }\n```\n\n## Sub-experiment B — Stronger Model Oracle (40 examples)\n\n```python\ndef run_sub_experiment_B(examples, n_examples=40, calibration_n=5):\n    \"\"\"\n    40 ProofWriter examples:\n    - Extract KB with Haiku\n    - Run dual-SBFL pipeline twice: once with Haiku oracle, once with GPT-4o-mini oracle\n    - Measure accuracy (correct True/False prediction after repair) for both\n    - Run perturbation calibration (protocol from A) on 5-doc subset for each oracle\n    - Report {haiku: {accuracy, calibration_rho}, gpt4omini: {accuracy, calibration_rho}}\n    \"\"\"\n    # Select 40 examples; ensure cost budget allows ~40 * (4 LLM calls) * avg_tokens\n    # Budget estimate: 40 docs * 4 calls * 500 tokens avg * ($0.25/M for Haiku) ≈ $0.02 — very cheap\n    # GPT-4o-mini oracle: 40 * 10 queries * 300 tokens * ($0.15/M) ≈ $0.002 — negligible\n    docs = examples[:n_examples]\n    results = {'haiku': {'correct': 0, 'total': 0}, 'gpt4omini': {'correct': 0, 'total': 0}}\n    \n    for model_name, oracle_model in [('haiku', HAIKU_MODEL), ('gpt4omini', GPT4O_MINI_MODEL)]:\n        for ex in docs:\n            check_budget()\n            premises_text = ex['metadata_premises_text']\n            conclusion = ex['metadata_conclusion']\n            gold_label = ex['output']  # 'True' or 'False'\n            \n            # Extract KB with Haiku (same for both conditions)\n            clauses, usage = extract_kb(premises_text, model=HAIKU_MODEL)\n            update_cost(HAIKU_MODEL, usage)\n            \n            # Generate oracle queries with varying model\n            oracle_queries, usage = generate_oracle_queries(\n                premises_text, conclusion, n_queries=10, model=oracle_model)\n            update_cost(oracle_model, usage)\n            \n            # Run dual-SBFL repair (up to 3 rounds, K=5)\n            final_clauses = run_dual_sbfl_repair(\n                clauses, oracle_queries, premises_text, rounds=3, K=5)\n            \n            # Evaluate: translate conclusion to Prolog query, run on final KB\n            pred = evaluate_on_kb(final_clauses, conclusion, premises_text)\n            if pred == gold_label:\n                results[model_name]['correct'] += 1\n            results[model_name]['total'] += 1\n    \n    # Calibration rho on 5-doc subset for each oracle model\n    calibration_results = {}\n    for model_name, oracle_model in [('haiku', HAIKU_MODEL), ('gpt4omini', GPT4O_MINI_MODEL)]:\n        rho_results = run_perturbation_calibration_with_model(\n            docs[:calibration_n], oracle_model=oracle_model)\n        calibration_results[model_name] = rho_results\n    \n    return {\n        'haiku': {\n            'accuracy': results['haiku']['correct'] / results['haiku']['total'],\n            'calibration_rho': calibration_results['haiku']['mean_rho']\n        },\n        'gpt4omini': {\n            'accuracy': results['gpt4omini']['correct'] / results['gpt4omini']['total'],\n            'calibration_rho': calibration_results['gpt4omini']['mean_rho']\n        }\n    }\n```\n\n## Sub-experiment C — KB Completeness by Hop Depth\n\n```python\ndef run_sub_experiment_C(examples, n_per_depth=30):\n    \"\"\"\n    Stratify ProofWriter examples by hop depth (1/2/3 using rule-count heuristic).\n    For each depth:\n      - Extract KB with Haiku\n      - Run meta-interpreter on the gold conclusion query\n      - Check if proof terminates or stalls on missing predicate\n        (completeness = proof terminates at success or provable failure, not incomplete-trace)\n      - For 1-hop examples specifically: also run dual-SBFL repair and measure accuracy\n    \"\"\"\n    # Stratify\n    by_depth = {1: [], 2: [], 3: []}\n    for ex in examples:\n        depth = estimate_hop_depth(ex['metadata_premises_text'])\n        by_depth[depth].append(ex)\n    \n    results_by_depth = {}\n    for depth in [1, 2, 3]:\n        check_budget()\n        depth_docs = by_depth[depth][:n_per_depth]\n        completeness_count = 0\n        sbfl_correct = 0\n        sbfl_total = 0\n        \n        for ex in depth_docs:\n            premises_text = ex['metadata_premises_text']\n            conclusion = ex['metadata_conclusion']\n            gold_label = ex['output']\n            \n            # Extract KB\n            clauses, usage = extract_kb(premises_text)\n            update_cost(HAIKU_MODEL, usage)\n            \n            # Load to Prolog and test conclusion\n            prolog = load_kb_to_prolog(clauses)\n            conclusion_query, usage = nl_query_to_prolog_simple(conclusion, clauses)\n            update_cost(HAIKU_MODEL, usage)\n            \n            # Run meta-interpreter; check if proof completes (no 'subgoal_failed' at leaf with unknown pred)\n            success, accessed, failed_subgoals = run_query_with_trace(prolog, conclusion_query)\n            is_complete = len([sg for sg in failed_subgoals if sg not in accessed]) == 0\n            # is_complete: no subgoal failed because of an entirely missing predicate\n            if is_complete:\n                completeness_count += 1\n            \n            # For 1-hop only: also run SBFL repair\n            if depth == 1:\n                oracle_queries, usage = generate_oracle_queries(premises_text, conclusion)\n                update_cost(HAIKU_MODEL, usage)\n                final_clauses = run_dual_sbfl_repair(clauses, oracle_queries, premises_text)\n                pred = evaluate_on_kb(final_clauses, conclusion, premises_text)\n                if pred == gold_label:\n                    sbfl_correct += 1\n                sbfl_total += 1\n        \n        entry = {'completeness_rate': completeness_count / max(len(depth_docs), 1)}\n        if depth == 1:\n            entry['sbfl_accuracy'] = sbfl_correct / max(sbfl_total, 1)\n        results_by_depth[str(depth)] = entry\n    \n    return {'by_depth': results_by_depth}\n```\n\n## Main Pipeline Helper: run_dual_sbfl_repair\n\n```python\ndef run_dual_sbfl_repair(clauses, oracle_queries, premises_text, rounds=3, K=5):\n    \"\"\"\n    Run SBFL-guided repair for `rounds` rounds, targeting K predicates per round.\n    Returns final KB clauses.\n    \"\"\"\n    current_clauses = list(clauses)\n    for round_idx in range(rounds):\n        check_budget()\n        # Convert NL oracle queries to Prolog\n        prolog_queries = []\n        for q in oracle_queries:\n            pg, usage = nl_query_to_prolog(q['query'], q['expected'], current_clauses)\n            update_cost(HAIKU_MODEL, usage)\n            prolog_queries.append({'goal': pg, 'expected': q['expected']})\n        \n        # Run meta-interpreter on all queries\n        prolog = load_kb_to_prolog(current_clauses)\n        coverage_matrix = {}\n        failed_subgoal_counts = {}\n        query_results = {}\n        for qid, pq in enumerate(prolog_queries):\n            success, accessed, failed_subgoals = run_query_with_trace(prolog, pq['goal'])\n            query_results[qid] = {'success': success, 'expected': pq['expected']}\n            for pred, status in accessed.items():\n                if pred not in coverage_matrix:\n                    coverage_matrix[pred] = {}\n                coverage_matrix[pred][qid] = status\n            for sg in failed_subgoals:\n                failed_subgoal_counts[sg] = failed_subgoal_counts.get(sg, 0) + 1\n        \n        # Compute Ochiai scores\n        ochiai_scores = compute_ochiai(coverage_matrix, query_results)\n        missing_scores = harvest_failed_subgoals(failed_subgoal_counts)\n        \n        # Build repair agenda\n        agenda = build_repair_agenda(ochiai_scores, missing_scores, K=K)\n        \n        # Repair top-K items\n        for item in agenda:\n            check_budget()\n            new_clauses, usage = repair_predicate(\n                item['pred'], item['type'], premises_text, current_clauses)\n            update_cost(HAIKU_MODEL, usage)\n            # Replace or add clauses\n            if item['type'] == 'erroneous':\n                # Remove old clauses for this predicate\n                current_clauses = [c for c in current_clauses \n                                   if not c.split('(')[0].strip() == item['pred']]\n            current_clauses.extend(new_clauses)\n    \n    return current_clauses\n```\n\n## Evaluation Helper\n\n```python\nEVAL_PROMPT = '''\nGiven this Prolog KB and conclusion, does the conclusion logically follow from the KB?\nKB:\n{kb}\n\nConclusion: {conclusion}\n\nAnswer only True, False, or Unknown.\n'''\n\ndef evaluate_on_kb(clauses, conclusion, premises_text):\n    \"\"\"\n    Try to evaluate via Prolog meta-interpreter first.\n    Fall back to LLM judge if Prolog query conversion fails.\n    Returns 'True', 'False', or 'Unknown'.\n    \"\"\"\n    # Attempt Prolog-based evaluation\n    try:\n        conclusion_query, usage = nl_query_to_prolog_simple(conclusion, clauses)\n        update_cost(HAIKU_MODEL, usage)\n        prolog = load_kb_to_prolog(clauses)\n        results = list(prolog.query(conclusion_query))\n        if results:  # query succeeded\n            return 'True'\n        else:\n            # Check if negation holds\n            neg_results = list(prolog.query(f'\\\\+ {conclusion_query}'))\n            return 'False' if neg_results else 'Unknown'\n    except Exception:\n        # Fallback: LLM judge\n        response = client.chat.completions.create(\n            model=HAIKU_MODEL,\n            messages=[{'role': 'user', 'content': EVAL_PROMPT.format(\n                kb='\\n'.join(clauses), conclusion=conclusion)}],\n            max_tokens=10, temperature=0.0\n        )\n        return response.choices[0].message.content.strip()\n```\n\n## Main Script Orchestration\n\n```python\ndef main():\n    # Load data\n    data = json.load(open(DATASET_PATH))\n    proofwriter = next(ds for ds in data['datasets'] if 'proofwriter' in ds['dataset'])\n    examples = proofwriter['examples']  # 300 examples\n    \n    print(f'Loaded {len(examples)} ProofWriter examples')\n    \n    # Sub-experiment A: 10 docs, perturbation calibration\n    print('Running Sub-experiment A: Perturbation Oracle Calibration...')\n    result_A = run_sub_experiment_A(examples, n_docs=10)\n    print(f'  Mean Spearman rho: {result_A[\"mean_rho\"]:.3f}')\n    print(f'  Budget used: ${cumulative_cost_usd:.3f}')\n    \n    # Sub-experiment B: 40 docs, stronger oracle\n    print('Running Sub-experiment B: Stronger Model Oracle...')\n    result_B = run_sub_experiment_B(examples, n_examples=40, calibration_n=5)\n    print(f'  Haiku accuracy: {result_B[\"haiku\"][\"accuracy\"]:.3f}')\n    print(f'  GPT-4o-mini accuracy: {result_B[\"gpt4omini\"][\"accuracy\"]:.3f}')\n    print(f'  Budget used: ${cumulative_cost_usd:.3f}')\n    \n    # Sub-experiment C: 30 per depth, completeness analysis\n    print('Running Sub-experiment C: KB Completeness by Hop Depth...')\n    result_C = run_sub_experiment_C(examples, n_per_depth=30)\n    for depth, stats in result_C['by_depth'].items():\n        print(f'  Depth {depth}: completeness={stats[\"completeness_rate\"]:.3f}', end='')\n        if 'sbfl_accuracy' in stats:\n            print(f', sbfl_accuracy={stats[\"sbfl_accuracy\"]:.3f}', end='')\n        print()\n    \n    # Write output\n    output = {\n        'perturbation_calibration': result_A,\n        'stronger_model': result_B,\n        'hop_completeness': result_C\n    }\n    with open('method_out.json', 'w') as f:\n        json.dump(output, f, indent=2)\n    print(f'Written method_out.json. Total cost: ${cumulative_cost_usd:.4f}')\n\nif __name__ == '__main__':\n    main()\n```\n\n## SWI-Prolog Installation Note\n\n```bash\n# Before running the script, install SWI-Prolog:\napt-get install -y swi-prolog\n# Verify:\nswipl --version\n# Install pyswip:\npip install pyswip\n# Test:\npython -c \"from pyswip import Prolog; p = Prolog(); p.assertz('fact(a)'); print(list(p.query('fact(X)')))\"\n```\n\n## pyswip Meta-Interpreter: Concrete Implementation\n\n```python\n# The meta-interpreter needs to be loaded into every Prolog instance.\n# Use a module-level setup that loads the meta-interpreter before KB clauses.\n\nMETA_INTERP_CLAUSES = [\n    ':- dynamic accessed_pred/1, resolved_pred/1, unif_failed_pred/1, subgoal_failed_pred/1',\n    'reset_trace :- retractall(accessed_pred(_)), retractall(resolved_pred(_)), retractall(unif_failed_pred(_)), retractall(subgoal_failed_pred(_))',\n    'solve(true) :- !',\n    'solve((A,B)) :- !, solve(A), solve(B)',\n    'solve((A;B)) :- !, (solve(A) ; solve(B))',\n    'solve(\\\\+(A)) :- !, \\\\+ solve(A)',\n    'solve(Goal) :- functor(Goal, Name, Arity), assert(accessed_pred(Name/Arity)), (clause(Goal, Body) -> (solve(Body) -> assert(resolved_pred(Name/Arity)) ; assert(subgoal_failed_pred(Name/Arity)), fail) ; assert(unif_failed_pred(Name/Arity)), fail)'\n]\n# Load via prolog.assertz() for each clause\n# Reset via prolog.query('reset_trace') before each query\n# Collect trace via prolog.query('accessed_pred(X)') etc.\n```\n",
  "fallback_plan": "## Fallback Scenarios\n\n**If pyswip/SWI-Prolog installation fails:**\nFall back to a pure-Python minimal Prolog interpreter. Use the `kanren` package (pip install kanren) for logic unification, or implement a simple backward-chaining interpreter over Datalog-style facts/rules without full SWI-Prolog. The meta-interpreter trace collection becomes a wrapper around the recursive solver. This loses full Prolog semantics (no cut, no arithmetic) but handles the ProofWriter/FOLIO-style rules which are essentially Datalog.\n\n**If LLM KB extraction produces unparseable Prolog:**\nAdd a post-processing step: strip markdown fences, remove lines that don't match `predicate_name(...)` pattern, lowercase all identifiers. If >50% of clauses are malformed, fall back to a rule-template extraction where the LLM fills structured templates (\"FACT: entity(value)\", \"RULE: head(X) :- body(X)\").\n\n**If oracle query generation produces malformed JSON:**\nAdd json.loads with exception handling; retry once with explicit JSON schema in the prompt. If still failing, fall back to a simple oracle: generate 5 paraphrases of the premise facts as positive queries and 5 negations as negative queries, using simple string templates.\n\n**If budget approaches $5 before all sub-experiments complete:**\nScale down: Sub-experiment A → 5 docs instead of 10. Sub-experiment B → 20 examples instead of 40. Sub-experiment C → 15 per depth instead of 30. Report partial results clearly in method_out.json with a 'partial_run' flag.\n\n**If Spearman rho is NaN (too few predicates):**\nFor docs with <3 predicates, skip rho computation and report null for that doc. Mean rho is computed only over docs with valid rho. If <5 valid docs in Sub-experiment A, report mean_rho=null with explanation.\n\n**If ProofWriter examples don't stratify well by hop depth (all map to same bucket):**\nFall back to: split examples into thirds by index (first 100 = 1-hop proxy, middle 100 = 2-hop, last 100 = 3-hop) since depth-3ext examples are generally ordered by complexity in the dataset. Alternatively, use the gold conclusion complexity (count of conditions in the conclusion) as depth proxy.",
  "testing_plan": "## Testing Plan\n\n### Quick Sanity Tests (run these first, should complete in <5 minutes)\n\n**Test 1: SWI-Prolog + pyswip integration**\n```python\nfrom pyswip import Prolog\np = Prolog()\np.assertz('animal(cat)')\np.assertz('animal(dog)')\np.assertz('likes(X) :- animal(X)')\nresults = list(p.query('likes(X)'))\nassert len(results) == 2, f'Expected 2 results, got {results}'\nprint('pyswip OK')\n```\n\n**Test 2: Meta-interpreter trace on one simple example**\n```python\n# Use first ProofWriter example\n# Extract KB, load to Prolog, run one oracle query\n# Check that accessed_pred, unif_failed_pred, subgoal_failed_pred are populated\n# Expected: at least one predicate in accessed_pred\n```\n\n**Test 3: Oracle query generation on one example**\n```python\n# Call generate_oracle_queries on example 0\n# Verify response is parseable JSON list with 'query' and 'expected' fields\n# Verify n_queries = 10\n```\n\n**Test 4: Ochiai computation correctness**\n```python\n# Manually construct a coverage matrix:\n# pred_A: accessed in 3 failing queries, 1 passing query\n# pred_B: accessed only in 1 failing query\n# pred_C: not accessed\n# Expected: ochiai(pred_A) > ochiai(pred_B) > ochiai(pred_C) = 0\nscores = compute_ochiai(manual_matrix, manual_query_results)\nassert scores['pred_A'] > scores['pred_B']\nassert scores['pred_C'] == 0\nprint('Ochiai correctness OK')\n```\n\n**Test 5: Repair prompt on one predicate**\n```python\n# Use example 0, extract KB, inject one error, call repair_predicate\n# Verify response contains at least one Prolog clause (contains '(')\n```\n\n**Test 6: Cost tracking**\n```python\n# After 3 LLM calls, verify cumulative_cost_usd > 0\n# Verify check_budget() doesn't raise when cost < $6\n```\n\n### Confirmation Signals Before Full Run\n\n1. **Sub-experiment A mini-run**: Run on 2 docs only. Expected: rho values in [-1, 1], not NaN. Should complete in <3 minutes.\n2. **Sub-experiment B mini-run**: Run on 5 examples with both oracle models. Expected: accuracy values in [0, 1]. Should complete in <5 minutes.\n3. **Sub-experiment C mini-run**: Run on 5 examples per depth. Expected: completeness_rate in [0, 1].\n\n### Full Scale Triggers\n- All 3 mini-runs produce valid (non-NaN, non-error) outputs → proceed to full scale\n- Budget after mini-runs < $1.00 → safe to proceed\n- Prolog meta-interpreter produces non-empty trace for >80% of queries → stratified coverage working\n\n### Expected Output Ranges\n- perturbation_calibration.mean_rho: should be in [0.3, 0.9] (hypothesis: ≥0.6; lower is also interesting)\n- stronger_model.gpt4omini.accuracy vs .haiku.accuracy: expect gpt4omini ≥ haiku\n- hop_completeness.by_depth: expect completeness_rate decreases as depth increases (1-hop > 2-hop > 3-hop)\n- sbfl_accuracy (1-hop only): expect >0.5 (above random for binary classification)\n\n### Failure Detection\n- If >30% of KB extractions produce 0 valid Prolog clauses → KB extraction prompt needs fixing\n- If meta-interpreter hangs (>10s per query) → add timeout via signal.alarm or subprocess with timeout\n- If oracle JSON parse fails >50% → switch to template-based oracle generation fallback"
}
````

### [7] TOOL RESULT — Write · 2026-06-07 06:21:50 UTC

````
{"type": "create", "filePath": "/ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_2/gen_plan/gen_plan_experiment_1/.terminal_claude_agent_struct_out.json", "content": "{\n  \"title\": \"Dual-Signal SBFL: Perturbation Calibration, Stronger Oracle, and Hop-Depth Completeness Analysis\",\n  \"summary\": \"Three sub-experiments using ProofWriter and FOLIO data from gen_art_dataset_1: (A) perturbation-based oracle calibration with injected synthetic errors on 10 docs, (B) GPT-4o-mini oracle vs Haiku oracle quality comparison on 40 examples, (C) KB structural completeness by hop depth (1/2/3-hop) with SBFL repair on 1-hop examples. All LLM calls via OpenRouter; budget cap $6.\",\n  \"runpod_compute_profile\": \"cpu_heavy\",\n  \"implementation_pseudocode\": \"## Setup and Dependencies\\n\\n```\\npip install pyswip openai requests scipy numpy\\n# pyswip requires SWI-Prolog installed: apt-get install swi-prolog\\n# All LLM calls go through OpenRouter using openai-compatible client\\n```\\n\\n## Data Loading\\n\\n```python\\nDATASET_PATH = '/ai-inventor/aii_data/runs/run_ITc1Qruy7fap/3_invention_loop/iter_1/gen_art/gen_art_dataset_1/full_data_out.json'\\n\\ndef load_proofwriter_examples(path):\\n    data = json.load(open(path))\\n    proofwriter = [ds for ds in data['datasets'] if ds['dataset'] == 'proofwriter_ruletaker_depth3'][0]\\n    return proofwriter['examples']  # 300 examples\\n\\n# ProofWriter examples don't have hop-depth annotations in the dataset.\\n# Proxy hop-depth from rule chain length:\\n# Parse premises: count conditional rules (\\\"If ... then ...\\\"); count transitive steps needed.\\n# Simple heuristic: examples with <=3 rules = 1-hop, 4-6 rules = 2-hop, 7+ rules = 3-hop.\\n# Alternatively: use 'depth-3ext-NatLang' config label and split by index thirds for stratification.\\n# Better approach: detect hop depth by trying to find shortest proof chain in meta-interpreter.\\ndef estimate_hop_depth(premises_text):\\n    rules = [s for s in premises_text.split('.') if s.strip().lower().startswith('if')]\\n    n_rules = len(rules)\\n    if n_rules <= 2: return 1\\n    elif n_rules <= 5: return 2\\n    else: return 3\\n```\\n\\n## OpenRouter LLM Client\\n\\n```python\\nimport openai\\nclient = openai.OpenAI(\\n    api_key=os.environ['OPENROUTER_API_KEY'],\\n    base_url='https://openrouter.ai/api/v1'\\n)\\nHAIKU_MODEL = 'anthropic/claude-haiku-3-5'  # cheap extractor/repair\\nGPT4O_MINI_MODEL = 'openai/gpt-4o-mini'  # stronger oracle\\n\\n# Track cumulative cost\\ncumulative_cost_usd = 0.0\\nBUDGET_CAP = 6.0\\ndef check_budget():\\n    if cumulative_cost_usd > BUDGET_CAP:\\n        raise RuntimeError(f'Budget cap ${BUDGET_CAP} reached at ${cumulative_cost_usd:.3f}')\\n\\n# Cost estimation: Claude Haiku ~$0.25/M input + $1.25/M output; GPT-4o-mini ~$0.15/M input + $0.60/M output\\n# Track via response.usage.prompt_tokens and completion_tokens\\ndef estimate_cost(model, prompt_tokens, completion_tokens):\\n    if 'haiku' in model:\\n        return prompt_tokens * 0.25e-6 + completion_tokens * 1.25e-6\\n    elif 'gpt-4o-mini' in model:\\n        return prompt_tokens * 0.15e-6 + completion_tokens * 0.60e-6\\n    return 0\\n```\\n\\n## Core Components\\n\\n### 1. KB Extractor (LLM Call 1 — Haiku)\\n\\n```python\\nKB_EXTRACTION_PROMPT = '''\\nYou are a logic expert. Convert the following natural language premises into SWI-Prolog facts and rules.\\nOutput ONLY valid Prolog clauses, one per line. No explanations.\\nUse lowercase atoms for constants. Use snake_case for predicate names.\\nFor rules \\\"If X then Y\\\", use: head :- body.\\nFor universal rules \\\"All X are Y\\\", use: y(X) :- x(X).\\nFor negations \\\"X is not Y\\\", use: \\\\+ y(X) in body, or represent as not_y(X) fact.\\n\\nPremises:\\n{premises}\\n\\nOutput Prolog KB:\\n'''\\n\\ndef extract_kb(premises_text, model=HAIKU_MODEL):\\n    # Returns list of Prolog clause strings\\n    response = client.chat.completions.create(\\n        model=model,\\n        messages=[{'role': 'user', 'content': KB_EXTRACTION_PROMPT.format(premises=premises_text)}],\\n        max_tokens=800, temperature=0.0\\n    )\\n    # parse response into list of clauses\\n    clauses = [line.strip() for line in response.choices[0].message.content.split('\\\\n') \\n               if line.strip() and not line.strip().startswith('%')]\\n    return clauses, response.usage\\n```\\n\\n### 2. Prolog Meta-Interpreter with Stratified Coverage\\n\\n```python\\n# SWI-Prolog via pyswip\\nfrom pyswip import Prolog\\n\\ndef load_kb_to_prolog(clauses):\\n    prolog = Prolog()\\n    for clause in clauses:\\n        try:\\n            prolog.assertz(clause.rstrip('.'))\\n        except Exception:\\n            pass  # skip malformed clauses; log them\\n    return prolog\\n\\ndef run_query_with_trace(prolog, query_str):\\n    \\\"\\\"\\\"\\n    Returns:\\n      - success: bool\\n      - accessed_predicates: dict {pred_name: 'direct_fail'|'subgoal_fail'|'success'}\\n      - failed_subgoals: list of predicate names that were attempted but never resolved\\n    \\\"\\\"\\\"\\n    # Use Prolog meta-interpreter to capture trace\\n    # Inject a tracing meta-interpreter into SWI-Prolog\\n    # See: https://www.swi-prolog.org/pldoc/man?section=metainterp\\n    # \\n    # Strategy: use SWI-Prolog's built-in trace/1 or implement a simple meta-interpreter\\n    # Simple approach: use catch/3 + call/1 with assertz to record accessed predicates\\n    #\\n    # Inject meta-interpreter clauses once at setup:\\n    META_INTERPRETER = '''\\n    solve(true) :- !.\\n    solve((A,B)) :- !, solve(A), solve(B).\\n    solve(Goal) :-\\n        functor(Goal, Name, Arity),\\n        assert(accessed(Name/Arity)),\\n        (clause(Goal, Body) ->\\n            (solve(Body) -> assert(resolved(Name/Arity)) ; assert(subgoal_failed(Name/Arity)), fail)\\n        ;\\n            assert(unif_failed(Name/Arity)), fail\\n        ).\\n    '''\\n    # Load meta-interpreter; clear state; run; collect results\\n    # For pyswip, use prolog.query() and assertz/retract patterns\\n    pass  # see detailed implementation note below\\n\\n# IMPLEMENTATION NOTE for executor:\\n# pyswip runs SWI-Prolog in-process. Use prolog.assertz() to add meta-interpreter clauses.\\n# Between queries, retract all dynamic facts (accessed/1, resolved/1, etc.) to reset state.\\n# Run: list(prolog.query(f'solve({query_str})')) to attempt proof.\\n# Then query: list(prolog.query('accessed(X)')) to get accessed predicates.\\n# Then query: list(prolog.query('unif_failed(X)')) for direct-fail predicates.\\n# Then query: list(prolog.query('subgoal_failed(X)')) for indirect-fail predicates.\\n# This gives the stratified coverage spectrum per query.\\n```\\n\\n### 3. Oracle Generator (LLM Call 2)\\n\\n```python\\nORACLE_GEN_PROMPT = '''\\nGiven these natural language premises and a target conclusion, generate {n_queries} yes/no reasoning questions.\\nEach question should test a specific logical relationship that can be verified from the premises.\\nHalf should be answerable TRUE, half FALSE from the premises.\\nOutput JSON: [{{\\\"query\\\": \\\"...\\\", \\\"expected\\\": true/false}}, ...]\\n\\nPremises: {premises}\\nConclusion: {conclusion}\\n'''\\n\\ndef generate_oracle_queries(premises_text, conclusion, n_queries=10, model=HAIKU_MODEL):\\n    # Returns list of {query_nl: str, expected: bool}\\n    response = client.chat.completions.create(\\n        model=model,\\n        messages=[{'role': 'user', 'content': ORACLE_GEN_PROMPT.format(\\n            premises=premises_text, conclusion=conclusion, n_queries=n_queries)}],\\n        max_tokens=600, temperature=0.3\\n    )\\n    queries = json.loads(response.choices[0].message.content)\\n    return queries, response.usage\\n\\nNL_TO_PROLOG_QUERY_PROMPT = '''\\nConvert this yes/no question to a SWI-Prolog query (just the goal, no :-). \\nUse the predicate names from this KB:\\n{kb_clauses}\\n\\nQuestion: {question}\\nIf expected answer is True, write a goal that should SUCCEED.\\nIf expected answer is False, write a goal that should FAIL (prefix with \\\\+).\\nOutput only the Prolog goal.\\n'''\\n\\ndef nl_query_to_prolog(nl_query, expected, kb_clauses, model=HAIKU_MODEL):\\n    # Returns prolog goal string\\n    response = client.chat.completions.create(\\n        model=model,\\n        messages=[{'role': 'user', 'content': NL_TO_PROLOG_QUERY_PROMPT.format(\\n            kb_clauses='\\\\n'.join(kb_clauses), question=nl_query)}],\\n        max_tokens=100, temperature=0.0\\n    )\\n    return response.choices[0].message.content.strip(), response.usage\\n```\\n\\n### 4. Ochiai Suspiciousness + Sub-Goal Harvesting\\n\\n```python\\nimport numpy as np\\nfrom scipy.stats import spearmanr\\n\\ndef compute_ochiai(coverage_matrix):\\n    \\\"\\\"\\\"\\n    coverage_matrix: dict {pred: {qid: {'accessed': bool, 'unified': bool, 'direct_fail': bool}}}\\n    Returns dict {pred: ochiai_score}\\n    \\\"\\\"\\\"\\n    # Standard Ochiai: suspiciousness(p) = ef / sqrt((ef+nf) * (ef+ep))\\n    # ef = queries that FAIL and access p; nf = queries that fail and DON'T access p\\n    # ep = queries that PASS and access p\\n    scores = {}\\n    pass_ids = {qid for qid, result in query_results.items() if result['success']}\\n    fail_ids = {qid for qid, result in query_results.items() if not result['success']}\\n    for pred, coverage in coverage_matrix.items():\\n        ef = sum(1 for qid in fail_ids if coverage.get(qid, {}).get('accessed', False))\\n        ep = sum(1 for qid in pass_ids if coverage.get(qid, {}).get('accessed', False))\\n        nf = sum(1 for qid in fail_ids if not coverage.get(qid, {}).get('accessed', False))\\n        denom = np.sqrt((ef + nf) * (ef + ep))\\n        scores[pred] = ef / denom if denom > 0 else 0.0\\n    return scores\\n\\ndef harvest_failed_subgoals(failed_subgoal_counts):\\n    \\\"\\\"\\\"Returns dict {pred: frequency_count} for predicates never resolved.\\\"\\\"\\\"\\n    # Already tracked during meta-interpreter runs\\n    return dict(sorted(failed_subgoal_counts.items(), key=lambda x: -x[1]))\\n\\ndef build_repair_agenda(ochiai_scores, missing_pred_scores, K=5):\\n    \\\"\\\"\\\"\\n    Merge Ochiai scores (erroneous predicates) and missing predicate scores.\\n    Normalize each to [0,1] then rank by combined score.\\n    Returns top-K items as [{'type': 'erroneous'|'missing', 'pred': str, 'score': float}]\\n    \\\"\\\"\\\"\\n    max_o = max(ochiai_scores.values(), default=1)\\n    max_m = max(missing_pred_scores.values(), default=1)\\n    combined = {}\\n    for p, s in ochiai_scores.items():\\n        combined[p] = {'score': s/max_o, 'type': 'erroneous'}\\n    for p, s in missing_pred_scores.items():\\n        if p not in combined:\\n            combined[p] = {'score': s/max_m, 'type': 'missing'}\\n        else:\\n            combined[p]['score'] = max(combined[p]['score'], s/max_m)\\n    ranked = sorted(combined.items(), key=lambda x: -x[1]['score'])\\n    return [{'pred': p, **v} for p, v in ranked[:K]]\\n```\\n\\n### 5. Repair (LLM Call 3 — Haiku)\\n\\n```python\\nREPAIR_PROMPT = '''\\nYou are debugging a Prolog knowledge base extracted from natural language.\\nThe predicate '{pred}' has been flagged as suspicious (type: {error_type}).\\n\\nOriginal premises:\\n{premises}\\n\\nCurrent KB clauses:\\n{kb_clauses}\\n\\nFor type 'erroneous': replace or fix the clause for '{pred}'.\\nFor type 'missing': add a new clause for '{pred}' that the premises imply.\\n\\nOutput ONLY the corrected or new Prolog clause(s), one per line.\\n'''\\n\\ndef repair_predicate(pred, error_type, premises_text, kb_clauses, model=HAIKU_MODEL):\\n    response = client.chat.completions.create(\\n        model=model,\\n        messages=[{'role': 'user', 'content': REPAIR_PROMPT.format(\\n            pred=pred, error_type=error_type,\\n            premises=premises_text, kb_clauses='\\\\n'.join(kb_clauses))}],\\n        max_tokens=300, temperature=0.0\\n    )\\n    new_clauses = [line.strip() for line in response.choices[0].message.content.split('\\\\n')\\n                   if line.strip() and not line.strip().startswith('%')]\\n    return new_clauses, response.usage\\n```\\n\\n## Sub-experiment A — Perturbation Oracle Calibration (10 docs)\\n\\n```python\\nimport random\\n\\ndef inject_synthetic_errors(clauses, k=5, seed=42):\\n    \\\"\\\"\\\"\\n    Inject k synthetic errors into KB clauses. Record ground-truth perturbed predicates.\\n    Error types:\\n      - delete_fact: remove a unit clause (fact)\\n      - rename_predicate: change predicate name in one clause to a nonsense variant\\n      - alter_argument: change an atom argument in one fact\\n    Returns: (perturbed_clauses, ground_truth_perturbed_preds: list of pred names)\\n    \\\"\\\"\\\"\\n    rng = random.Random(seed)\\n    facts = [c for c in clauses if ':-' not in c]\\n    rules = [c for c in clauses if ':-' in c]\\n    perturbed = list(clauses)\\n    gt_preds = []\\n    errors_done = 0\\n    \\n    # Attempt k errors with mix of types\\n    error_types = rng.choices(['delete_fact', 'rename_predicate', 'alter_argument'], k=k)\\n    for etype in error_types:\\n        if etype == 'delete_fact' and facts:\\n            target = rng.choice(facts)\\n            perturbed.remove(target)\\n            pred_name = target.split('(')[0].strip()\\n            gt_preds.append(pred_name)\\n            facts.remove(target)\\n        elif etype == 'rename_predicate' and perturbed:\\n            idx = rng.randint(0, len(perturbed)-1)\\n            old = perturbed[idx]\\n            # Find first predicate name and rename it with '_broken' suffix\\n            import re\\n            match = re.match(r'([a-z_][a-z0-9_]*)', old)\\n            if match:\\n                orig_name = match.group(1)\\n                perturbed[idx] = old.replace(orig_name, orig_name + '_broken', 1)\\n                gt_preds.append(orig_name)\\n        elif etype == 'alter_argument' and facts:\\n            target = rng.choice(facts)\\n            idx = perturbed.index(target)\\n            # Replace first atom argument with 'wrong_value'\\n            altered = re.sub(r'\\\\(([a-z_][a-z0-9_]*)', '(wrong_value', target, count=1)\\n            perturbed[idx] = altered\\n            gt_preds.append(target.split('(')[0].strip())\\n    return perturbed, list(set(gt_preds))\\n\\ndef run_sub_experiment_A(examples, n_docs=10):\\n    \\\"\\\"\\\"\\n    For 10 ProofWriter docs:\\n    1. Extract KB via Haiku\\n    2. Inject 5 synthetic errors\\n    3. Generate 10 oracle queries via Haiku\\n    4. Run meta-interpreter on perturbed KB for each query\\n    5. Compute Ochiai scores\\n    6. Compute Spearman rho vs ground-truth perturbed predicates\\n       (GT ranking: perturbed preds at rank 1..k, others at rank k+1)\\n    7. Report mean rho\\n    \\\"\\\"\\\"\\n    docs = examples[:n_docs]  # first 10 ProofWriter examples\\n    per_doc_rho = []\\n    \\n    for i, ex in enumerate(docs):\\n        check_budget()\\n        premises_text = ex['metadata_premises_text']\\n        conclusion = ex['metadata_conclusion']\\n        \\n        # Step 1: Extract KB\\n        clauses, usage = extract_kb(premises_text)\\n        update_cost(HAIKU_MODEL, usage)\\n        \\n        # Step 2: Inject errors\\n        perturbed_clauses, gt_perturbed_preds = inject_synthetic_errors(clauses, k=5, seed=i)\\n        \\n        # Step 3: Generate oracle queries (on perturbed KB)\\n        oracle_queries, usage = generate_oracle_queries(premises_text, conclusion, n_queries=10)\\n        update_cost(HAIKU_MODEL, usage)\\n        \\n        # Step 4-5: Run meta-interpreter, compute Ochiai\\n        ochiai_scores, missing_scores = run_sbfl_pipeline(\\n            perturbed_clauses, oracle_queries, premises_text)\\n        \\n        # Step 6: Compute Spearman rho\\n        # All predicates in original clauses as candidates\\n        all_preds = list(set(\\n            c.split('(')[0].strip().split(':-')[0].strip() for c in clauses if c.strip()\\n        ))\\n        # Ground-truth ranking: gt_perturbed_preds get rank 1 (most suspicious), others get n+1\\n        gt_ranks = {p: (1 if p in gt_perturbed_preds else len(gt_perturbed_preds)+1)\\n                    for p in all_preds}\\n        ochiai_ranks = {p: ochiai_scores.get(p, 0) for p in all_preds}\\n        \\n        # Spearman between ochiai score and (inverted) gt rank\\n        shared = [p for p in all_preds if p in ochiai_scores or p in gt_perturbed_preds]\\n        if len(shared) < 3:\\n            per_doc_rho.append(0.0)  # not enough data\\n            continue\\n        gt_vec = [-gt_ranks[p] for p in shared]  # negative so higher = more suspicious\\n        ochiai_vec = [ochiai_scores.get(p, 0) for p in shared]\\n        rho, _ = spearmanr(ochiai_vec, gt_vec)\\n        per_doc_rho.append(float(rho) if not np.isnan(rho) else 0.0)\\n    \\n    return {\\n        'mean_rho': float(np.mean(per_doc_rho)),\\n        'per_doc_rho': per_doc_rho\\n    }\\n```\\n\\n## Sub-experiment B — Stronger Model Oracle (40 examples)\\n\\n```python\\ndef run_sub_experiment_B(examples, n_examples=40, calibration_n=5):\\n    \\\"\\\"\\\"\\n    40 ProofWriter examples:\\n    - Extract KB with Haiku\\n    - Run dual-SBFL pipeline twice: once with Haiku oracle, once with GPT-4o-mini oracle\\n    - Measure accuracy (correct True/False prediction after repair) for both\\n    - Run perturbation calibration (protocol from A) on 5-doc subset for each oracle\\n    - Report {haiku: {accuracy, calibration_rho}, gpt4omini: {accuracy, calibration_rho}}\\n    \\\"\\\"\\\"\\n    # Select 40 examples; ensure cost budget allows ~40 * (4 LLM calls) * avg_tokens\\n    # Budget estimate: 40 docs * 4 calls * 500 tokens avg * ($0.25/M for Haiku) ≈ $0.02 — very cheap\\n    # GPT-4o-mini oracle: 40 * 10 queries * 300 tokens * ($0.15/M) ≈ $0.002 — negligible\\n    docs = examples[:n_examples]\\n    results = {'haiku': {'correct': 0, 'total': 0}, 'gpt4omini': {'correct': 0, 'total': 0}}\\n    \\n    for model_name, oracle_model in [('haiku', HAIKU_MODEL), ('gpt4omini', GPT4O_MINI_MODEL)]:\\n        for ex in docs:\\n            check_budget()\\n            premises_text = ex['metadata_premises_text']\\n            conclusion = ex['metadata_conclusion']\\n            gold_label = ex['output']  # 'True' or 'False'\\n            \\n            # Extract KB with Haiku (same for both conditions)\\n            clauses, usage = extract_kb(premises_text, model=HAIKU_MODEL)\\n            update_cost(HAIKU_MODEL, usage)\\n            \\n            # Generate oracle queries with varying model\\n            oracle_queries, usage = generate_oracle_queries(\\n                premises_text, conclusion, n_queries=10, model=oracle_model)\\n            update_cost(oracle_model, usage)\\n            \\n            # Run dual-SBFL repair (up to 3 rounds, K=5)\\n            final_clauses = run_dual_sbfl_repair(\\n                clauses, oracle_queries, premises_text, rounds=3, K=5)\\n            \\n            # Evaluate: translate conclusion to Prolog query, run on final KB\\n            pred = evaluate_on_kb(final_clauses, conclusion, premises_text)\\n            if pred == gold_label:\\n                results[model_name]['correct'] += 1\\n            results[model_name]['total'] += 1\\n    \\n    # Calibration rho on 5-doc subset for each oracle model\\n    calibration_results = {}\\n    for model_name, oracle_model in [('haiku', HAIKU_MODEL), ('gpt4omini', GPT4O_MINI_MODEL)]:\\n        rho_results = run_perturbation_calibration_with_model(\\n            docs[:calibration_n], oracle_model=oracle_model)\\n        calibration_results[model_name] = rho_results\\n    \\n    return {\\n        'haiku': {\\n            'accuracy': results['haiku']['correct'] / results['haiku']['total'],\\n            'calibration_rho': calibration_results['haiku']['mean_rho']\\n        },\\n        'gpt4omini': {\\n            'accuracy': results['gpt4omini']['correct'] / results['gpt4omini']['total'],\\n            'calibration_rho': calibration_results['gpt4omini']['mean_rho']\\n        }\\n    }\\n```\\n\\n## Sub-experiment C — KB Completeness by Hop Depth\\n\\n```python\\ndef run_sub_experiment_C(examples, n_per_depth=30):\\n    \\\"\\\"\\\"\\n    Stratify ProofWriter examples by hop depth (1/2/3 using rule-count heuristic).\\n    For each depth:\\n      - Extract KB with Haiku\\n      - Run meta-interpreter on the gold conclusion query\\n      - Check if proof terminates or stalls on missing predicate\\n        (completeness = proof terminates at success or provable failure, not incomplete-trace)\\n      - For 1-hop examples specifically: also run dual-SBFL repair and measure accuracy\\n    \\\"\\\"\\\"\\n    # Stratify\\n    by_depth = {1: [], 2: [], 3: []}\\n    for ex in examples:\\n        depth = estimate_hop_depth(ex['metadata_premises_text'])\\n        by_depth[depth].append(ex)\\n    \\n    results_by_depth = {}\\n    for depth in [1, 2, 3]:\\n        check_budget()\\n        depth_docs = by_depth[depth][:n_per_depth]\\n        completeness_count = 0\\n        sbfl_correct = 0\\n        sbfl_total = 0\\n        \\n        for ex in depth_docs:\\n            premises_text = ex['metadata_premises_text']\\n            conclusion = ex['metadata_conclusion']\\n            gold_label = ex['output']\\n            \\n            # Extract KB\\n            clauses, usage = extract_kb(premises_text)\\n            update_cost(HAIKU_MODEL, usage)\\n            \\n            # Load to Prolog and test conclusion\\n            prolog = load_kb_to_prolog(clauses)\\n            conclusion_query, usage = nl_query_to_prolog_simple(conclusion, clauses)\\n            update_cost(HAIKU_MODEL, usage)\\n            \\n            # Run meta-interpreter; check if proof completes (no 'subgoal_failed' at leaf with unknown pred)\\n            success, accessed, failed_subgoals = run_query_with_trace(prolog, conclusion_query)\\n            is_complete = len([sg for sg in failed_subgoals if sg not in accessed]) == 0\\n            # is_complete: no subgoal failed because of an entirely missing predicate\\n            if is_complete:\\n                completeness_count += 1\\n            \\n            # For 1-hop only: also run SBFL repair\\n            if depth == 1:\\n                oracle_queries, usage = generate_oracle_queries(premises_text, conclusion)\\n                update_cost(HAIKU_MODEL, usage)\\n                final_clauses = run_dual_sbfl_repair(clauses, oracle_queries, premises_text)\\n                pred = evaluate_on_kb(final_clauses, conclusion, premises_text)\\n                if pred == gold_label:\\n                    sbfl_correct += 1\\n                sbfl_total += 1\\n        \\n        entry = {'completeness_rate': completeness_count / max(len(depth_docs), 1)}\\n        if depth == 1:\\n            entry['sbfl_accuracy'] = sbfl_correct / max(sbfl_total, 1)\\n        results_by_depth[str(depth)] = entry\\n    \\n    return {'by_depth': results_by_depth}\\n```\\n\\n## Main Pipeline Helper: run_dual_sbfl_repair\\n\\n```python\\ndef run_dual_sbfl_repair(clauses, oracle_queries, premises_text, rounds=3, K=5):\\n    \\\"\\\"\\\"\\n    Run SBFL-guided repair for `rounds` rounds, targeting K predicates per round.\\n    Returns final KB clauses.\\n    \\\"\\\"\\\"\\n    current_clauses = list(clauses)\\n    for round_idx in range(rounds):\\n        check_budget()\\n        # Convert NL oracle queries to Prolog\\n        prolog_queries = []\\n        for q in oracle_queries:\\n            pg, usage = nl_query_to_prolog(q['query'], q['expected'], current_clauses)\\n            update_cost(HAIKU_MODEL, usage)\\n            prolog_queries.append({'goal': pg, 'expected': q['expected']})\\n        \\n        # Run meta-interpreter on all queries\\n        prolog = load_kb_to_prolog(current_clauses)\\n        coverage_matrix = {}\\n        failed_subgoal_counts = {}\\n        query_results = {}\\n        for qid, pq in enumerate(prolog_queries):\\n            success, accessed, failed_subgoals = run_query_with_trace(prolog, pq['goal'])\\n            query_results[qid] = {'success': success, 'expected': pq['expected']}\\n            for pred, status in accessed.items():\\n                if pred not in coverage_matrix:\\n                    coverage_matrix[pred] = {}\\n                coverage_matrix[pred][qid] = status\\n            for sg in failed_subgoals:\\n                failed_subgoal_counts[sg] = failed_subgoal_counts.get(sg, 0) + 1\\n        \\n        # Compute Ochiai scores\\n        ochiai_scores = compute_ochiai(coverage_matrix, query_results)\\n        missing_scores = harvest_failed_subgoals(failed_subgoal_counts)\\n        \\n        # Build repair agenda\\n        agenda = build_repair_agenda(ochiai_scores, missing_scores, K=K)\\n        \\n        # Repair top-K items\\n        for item in agenda:\\n            check_budget()\\n            new_clauses, usage = repair_predicate(\\n                item['pred'], item['type'], premises_text, current_clauses)\\n            update_cost(HAIKU_MODEL, usage)\\n            # Replace or add clauses\\n            if item['type'] == 'erroneous':\\n                # Remove old clauses for this predicate\\n                current_clauses = [c for c in current_clauses \\n                                   if not c.split('(')[0].strip() == item['pred']]\\n            current_clauses.extend(new_clauses)\\n    \\n    return current_clauses\\n```\\n\\n## Evaluation Helper\\n\\n```python\\nEVAL_PROMPT = '''\\nGiven this Prolog KB and conclusion, does the conclusion logically follow from the KB?\\nKB:\\n{kb}\\n\\nConclusion: {conclusion}\\n\\nAnswer only True, False, or Unknown.\\n'''\\n\\ndef evaluate_on_kb(clauses, conclusion, premises_text):\\n    \\\"\\\"\\\"\\n    Try to evaluate via Prolog meta-interpreter first.\\n    Fall back to LLM judge if Prolog query conversion fails.\\n    Returns 'True', 'False', or 'Unknown'.\\n    \\\"\\\"\\\"\\n    # Attempt Prolog-based evaluation\\n    try:\\n        conclusion_query, usage = nl_query_to_prolog_simple(conclusion, clauses)\\n        update_cost(HAIKU_MODEL, usage)\\n        prolog = load_kb_to_prolog(clauses)\\n        results = list(prolog.query(conclusion_query))\\n        if results:  # query succeeded\\n            return 'True'\\n        else:\\n            # Check if negation holds\\n            neg_results = list(prolog.query(f'\\\\\\\\+ {conclusion_query}'))\\n            return 'False' if neg_results else 'Unknown'\\n    except Exception:\\n        # Fallback: LLM judge\\n        response = client.chat.completions.create(\\n            model=HAIKU_MODEL,\\n            messages=[{'role': 'user', 'content': EVAL_PROMPT.format(\\n                kb='\\\\n'.join(clauses), conclusion=conclusion)}],\\n            max_tokens=10, temperature=0.0\\n        )\\n        return response.choices[0].message.content.strip()\\n```\\n\\n## Main Script Orchestration\\n\\n```python\\ndef main():\\n    # Load data\\n    data = json.load(open(DATASET_PATH))\\n    proofwriter = next(ds for ds in data['datasets'] if 'proofwriter' in ds['dataset'])\\n    examples = proofwriter['examples']  # 300 examples\\n    \\n    print(f'Loaded {len(examples)} ProofWriter examples')\\n    \\n    # Sub-experiment A: 10 docs, perturbation calibration\\n    print('Running Sub-experiment A: Perturbation Oracle Calibration...')\\n    result_A = run_sub_experiment_A(examples, n_docs=10)\\n    print(f'  Mean Spearman rho: {result_A[\\\"mean_rho\\\"]:.3f}')\\n    print(f'  Budget used: ${cumulative_cost_usd:.3f}')\\n    \\n    # Sub-experiment B: 40 docs, stronger oracle\\n    print('Running Sub-experiment B: Stronger Model Oracle...')\\n    result_B = run_sub_experiment_B(examples, n_examples=40, calibration_n=5)\\n    print(f'  Haiku accuracy: {result_B[\\\"haiku\\\"][\\\"accuracy\\\"]:.3f}')\\n    print(f'  GPT-4o-mini accuracy: {result_B[\\\"gpt4omini\\\"][\\\"accuracy\\\"]:.3f}')\\n    print(f'  Budget used: ${cumulative_cost_usd:.3f}')\\n    \\n    # Sub-experiment C: 30 per depth, completeness analysis\\n    print('Running Sub-experiment C: KB Completeness by Hop Depth...')\\n    result_C = run_sub_experiment_C(examples, n_per_depth=30)\\n    for depth, stats in result_C['by_depth'].items():\\n        print(f'  Depth {depth}: completeness={stats[\\\"completeness_rate\\\"]:.3f}', end='')\\n        if 'sbfl_accuracy' in stats:\\n            print(f', sbfl_accuracy={stats[\\\"sbfl_accuracy\\\"]:.3f}', end='')\\n        print()\\n    \\n    # Write output\\n    output = {\\n        'perturbation_calibration': result_A,\\n        'stronger_model': result_B,\\n        'hop_completeness': result_C\\n    }\\n    with open('method_out.json', 'w') as f:\\n        json.dump(output, f, indent=2)\\n    print(f'Written method_out.json. Total cost: ${cumulative_cost_usd:.4f}')\\n\\nif __name__ == '__main__':\\n    main()\\n```\\n\\n## SWI-Prolog Installation Note\\n\\n```bash\\n# Before running the script, install SWI-Prolog:\\napt-get install -y swi-prolog\\n# Verify:\\nswipl --version\\n# Install pyswip:\\npip install pyswip\\n# Test:\\npython -c \\\"from pyswip import Prolog; p = Prolog(); p.assertz('fact(a)'); print(list(p.query('fact(X)')))\\\"\\n```\\n\\n## pyswip Meta-Interpreter: Concrete Implementation\\n\\n```python\\n# The meta-interpreter needs to be loaded into every Prolog instance.\\n# Use a module-level setup that loads the meta-interpreter before KB clauses.\\n\\nMETA_INTERP_CLAUSES = [\\n    ':- dynamic accessed_pred/1, resolved_pred/1, unif_failed_pred/1, subgoal_failed_pred/1',\\n    'reset_trace :- retractall(accessed_pred(_)), retractall(resolved_pred(_)), retractall(unif_failed_pred(_)), retractall(subgoal_failed_pred(_))',\\n    'solve(true) :- !',\\n    'solve((A,B)) :- !, solve(A), solve(B)',\\n    'solve((A;B)) :- !, (solve(A) ; solve(B))',\\n    'solve(\\\\\\\\+(A)) :- !, \\\\\\\\+ solve(A)',\\n    'solve(Goal) :- functor(Goal, Name, Arity), assert(accessed_pred(Name/Arity)), (clause(Goal, Body) -> (solve(Body) -> assert(resolved_pred(Name/Arity)) ; assert(subgoal_failed_pred(Name/Arity)), fail) ; assert(unif_failed_pred(Name/Arity)), fail)'\\n]\\n# Load via prolog.assertz() for each clause\\n# Reset via prolog.query('reset_trace') before each query\\n# Collect trace via prolog.query('accessed_pred(X)') etc.\\n```\\n\",\n  \"fallback_plan\": \"## Fallback Scenarios\\n\\n**If pyswip/SWI-Prolog installation fails:**\\nFall back to a pure-Python minimal Prolog interpreter. Use the `kanren` package (pip install kanren) for logic unification, or implement a simple backward-chaining interpreter over Datalog-style facts/rules without full SWI-Prolog. The meta-interpreter trace collection becomes a wrapper around the recursive solver. This loses full Prolog semantics (no cut, no arithmetic) but handles the ProofWriter/FOLIO-style rules which are essentially Datalog.\\n\\n**If LLM KB extraction produces unparseable Prolog:**\\nAdd a post-processing step: strip markdown fences, remove lines that don't match `predicate_name(...)` pattern, lowercase all identifiers. If >50% of clauses are malformed, fall back to a rule-template extraction where the LLM fills structured templates (\\\"FACT: entity(value)\\\", \\\"RULE: head(X) :- body(X)\\\").\\n\\n**If oracle query generation produces malformed JSON:**\\nAdd json.loads with exception handling; retry once with explicit JSON schema in the prompt. If still failing, fall back to a simple oracle: generate 5 paraphrases of the premise facts as positive queries and 5 negations as negative queries, using simple string templates.\\n\\n**If budget approaches $5 before all sub-experiments complete:**\\nScale down: Sub-experiment A → 5 docs instead of 10. Sub-experiment B → 20 examples instead of 40. Sub-experiment C → 15 per depth instead of 30. Report partial results clearly in method_out.json with a 'partial_run' flag.\\n\\n**If Spearman rho is NaN (too few predicates):**\\nFor docs with <3 predicates, skip rho computation and report null for that doc. Mean rho is computed only over docs with valid rho. If <5 valid docs in Sub-experiment A, report mean_rho=null with explanation.\\n\\n**If ProofWriter examples don't stratify well by hop depth (all map to same bucket):**\\nFall back to: split examples into thirds by index (first 100 = 1-hop proxy, middle 100 = 2-hop, last 100 = 3-hop) since depth-3ext examples are generally ordered by complexity in the dataset. Alternatively, use the gold conclusion complexity (count of conditions in the conclusion) as depth proxy.\",\n  \"testing_plan\": \"## Testing Plan\\n\\n### Quick Sanity Tests (run these first, should complete in <5 minutes)\\n\\n**Test 1: SWI-Prolog + pyswip integration**\\n```python\\nfrom pyswip import Prolog\\np = Prolog()\\np.assertz('animal(cat)')\\np.assertz('animal(dog)')\\np.assertz('likes(X) :- animal(X)')\\nresults = list(p.query('likes(X)'))\\nassert len(results) == 2, f'Expected 2 results, got {results}'\\nprint('pyswip OK')\\n```\\n\\n**Test 2: Meta-interpreter trace on one simple example**\\n```python\\n# Use first ProofWriter example\\n# Extract KB, load to Prolog, run one oracle query\\n# Check that accessed_pred, unif_failed_pred, subgoal_failed_pred are populated\\n# Expected: at least one predicate in accessed_pred\\n```\\n\\n**Test 3: Oracle query generation on one example**\\n```python\\n# Call generate_oracle_queries on example 0\\n# Verify response is parseable JSON list with 'query' and 'expected' fields\\n# Verify n_queries = 10\\n```\\n\\n**Test 4: Ochiai computation correctness**\\n```python\\n# Manually construct a coverage matrix:\\n# pred_A: accessed in 3 failing queries, 1 passing query\\n# pred_B: accessed only in 1 failing query\\n# pred_C: not accessed\\n# Expected: ochiai(pred_A) > ochiai(pred_B) > ochiai(pred_C) = 0\\nscores = compute_ochiai(manual_matrix, manual_query_results)\\nassert scores['pred_A'] > scores['pred_B']\\nassert scores['pred_C'] == 0\\nprint('Ochiai correctness OK')\\n```\\n\\n**Test 5: Repair prompt on one predicate**\\n```python\\n# Use example 0, extract KB, inject one error, call repair_predicate\\n# Verify response contains at least one Prolog clause (contains '(')\\n```\\n\\n**Test 6: Cost tracking**\\n```python\\n# After 3 LLM calls, verify cumulative_cost_usd > 0\\n# Verify check_budget() doesn't raise when cost < $6\\n```\\n\\n### Confirmation Signals Before Full Run\\n\\n1. **Sub-experiment A mini-run**: Run on 2 docs only. Expected: rho values in [-1, 1], not NaN. Should complete in <3 minutes.\\n2. **Sub-experiment B mini-run**: Run on 5 examples with both oracle models. Expected: accuracy values in [0, 1]. Should complete in <5 minutes.\\n3. **Sub-experiment C mini-run**: Run on 5 examples per depth. Expected: completeness_rate in [0, 1].\\n\\n### Full Scale Triggers\\n- All 3 mini-runs produce valid (non-NaN, non-error) outputs → proceed to full scale\\n- Budget after mini-runs < $1.00 → safe to proceed\\n- Prolog meta-interpreter produces non-empty trace for >80% of queries → stratified coverage working\\n\\n### Expected Output Ranges\\n- perturbation_calibration.mean_rho: should be in [0.3, 0.9] (hypothesis: ≥0.6; lower is also interesting)\\n- stronger_model.gpt4omini.accuracy vs .haiku.accuracy: expect gpt4omini ≥ haiku\\n- hop_completeness.by_depth: expect completeness_rate decreases as depth increases (1-hop > 2-hop > 3-hop)\\n- sbfl_accuracy (1-hop only): expect >0.5 (above random for binary classification)\\n\\n### Failure Detection\\n- If >30% of KB extractions produce 0 valid Prolog clauses → KB extraction prompt needs fixing\\n- If meta-interpreter hangs (>10s per query) → add timeout via signal.alarm or subprocess with timeout\\n- If oracle JSON parse fails >50% → switch to template-based oracle generation fallback\"\n}\n", "structuredPatch": [], "originalFile": null, "userModified": false}
````

### [8] ASSISTANT · 2026-06-07 06:21:54 UTC

```
Plan written to `.terminal_claude_agent_struct_out.json`. The plan covers three sub-experiments using the ProofWriter data from gen_art_dataset_1: (A) perturbation-based oracle calibration with injected synthetic errors measuring Spearman rho, (B) GPT-4o-mini vs Haiku oracle quality comparison on 40 examples, and (C) KB structural completeness stratified by hop depth with SBFL repair on 1-hop examples. All LLM calls go through OpenRouter with a $6 hard budget cap and cost tracking after every call.
```
