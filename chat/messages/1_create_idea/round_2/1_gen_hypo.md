# gen_hypo_1 — create_idea

> Phase: `hypo_loop` · round 2 · `gen_hypo`
> Run: `run_ITc1Qruy7fap` — Text To Logic Pipeline
>
> Full, verbatim transcript of this agent task — every system/user prompt, assistant response, thinking block, tool call and tool result — in the order they occurred. Nothing truncated.

## Task: `gen_hypo_1` (terminal_claude_agent, claude-sonnet-4-6)

### [1] CONFIG · 2026-06-07 04:46:58 UTC

```
model: claude-sonnet-4-6 | effort: low | permission: bypassPermissions | cwd: /ai-inventor/aii_data/runs/run_ITc1Qruy7fap/iter_2/gen_hypo/claude_agent
```

### [2] ASSISTANT · 2026-06-07 04:47:22 UTC

```
I'll revise the previous hypothesis to address all the reviewer's critiques. Let me do a quick search on the missing related works first, then write the revised hypothesis.
```

### [3] TOOL CALL — Skill · 2026-06-07 04:47:22 UTC

```
aii-web-tools
```

### [4] TOOL RESULT — Skill · 2026-06-07 04:47:22 UTC

```
{"success": true, "commandName": "aii-web-tools"}
```

### [5] TOOL CALL — Bash · 2026-06-07 04:47:38 UTC

```
Search for missing related works and benchmark:
export SKILL_DIR="/ai-inventor/.claude/skills/aii-web-tools"
export PY="$SKILL_DIR/../.ability_client_venv/bin/python"
$PY "$SKILL_DIR/scripts/aii_fast_web_search.py" --query "Logic-LM Pan 2023 NeurIPS LLM symbolic solver reasoning" --max-results 5 &
$PY "$SKILL_DIR/scripts/aii_fast_web_search.py" --query "DSPy ASP answer set programming iterative feedback LLM 2025" --max-results 5 &
$PY "$SKILL_DIR/scripts/aii_fast_web_search.py" --query "FOLIO natural language first order logic benchmark dataset" --max-results 5 &
wait
```

### [6] TOOL RESULT — Bash · 2026-06-07 04:47:38 UTC

```
{"stdout": "Search: Logic-LM Pan 2023 NeurIPS LLM symbolic solver reasoning\nFound: 5 results\n\n1. Logic-LM: Empowering Large Language Models with Symbolic ...\n   https://arxiv.org/abs/2305.12295\n   This paper introduces a novel framework, Logic-LM, which integrates LLMs with symbolic solvers to improve logical problem-solving....\n\n2. teacherpeterpan/Logic-LLM: The project page for \"LOGIC-LM ...\n   https://github.com/teacherpeterpan/logic-llm\n   This paper introduces a novel framework, Logic-LM, which integrates LLMs with symbolic solvers to improve logical problem-solving. Our method first utilizes ......\n\n3. Enhancing Reasoning Capabilities of LLMs via Principled Synthetic ...\n   https://neurips.cc/virtual/2024/poster/93733\n   Large language models (LLMs) are capable of solving a wide range of tasks, yet they have struggled with reasoning....\n\n4. [PDF] A Survey on LLM Symbolic Reasoning - TechRxiv\n   https://www.techrxiv.org/doi/pdf/10.36227/techrxiv.176538331.19733376/v1\n   Recent works couple LLMs with symbolic solvers to ensure the faithfulness of reasoning and enable verifiable inference beyond textual chains. e.g.,. Logic-LM ( ......\n\n5. Papers Explained: Logic-LM Empowering Large Language Models ...\n   https://medium.com/@mittal.chinmay/papers-explained-logic-lm-empowering-large-language-models-with-symbolic-solvers-for-faithful-57afd0f7f039\n   Logic-LM is a novel framework to combine LLMs and Symbolic Solvers for faithful Logical Reasoning. Instead of making the LLM solve the reasoning ......\n\nSearch: FOLIO natural language first order logic benchmark dataset\nFound: 5 results\n\n1. FOLIO: Natural Language Reasoning with First-Order Logic\n   https://aclanthology.org/2024.emnlp-main.1229/\n   We present FOLIO, a human-annotated, logically complex and diverse dataset for reasoning in natural language (NL), equipped with first-order logic (FOL) ......\n\n2. Yale-LILY/FOLIO - GitHub\n   https://github.com/Yale-LILY/FOLIO\n   FOLIO is an expert-written, open-domain, logically complex and diverse dataset for natural language reasoning with first-order logic....\n\n3. FOLIO: Natural Language Reasoning with First-Order Logic - arXiv\n   https://arxiv.org/abs/2209.00840\n   FOLIO consists of 1,430 examples (unique conclusions), each paired with one of 487 sets of premises used to deductively reason for the validity ......\n\n4. [PDF] FOLIO: Natural Language Reasoning with First-Order Logic\n   https://www.semanticscholar.org/paper/FOLIO%3A-Natural-Language-Reasoning-with-First-Order-Han-Schoelkopf/5581bf85386737bd3378eec68189759a05280bea\n   This work presents FOLIO, a human-annotated, logically complex and diverse dataset for reasoning in natural language (NL), equipped with first-order logic ......\n\n5. FOLIO: Natural Language Reasoning with First-Order Logic\n   https://huggingface.co/papers/2209.00840\n   FOLIO is a dataset for reasoning in natural language with first-order logic annotations, evaluated for its effectiveness in FOL reasoning ......\n\nSearch: DSPy ASP answer set programming iterative feedback LLM 2025\nFound: 5 results\n\n1. DSPy-based neural-symbolic pipeline to enhance spatial reasoning ...\n   https://www.sciencedirect.com/science/article/pii/S0893608025009025\n   We present a neural-symbolic framework that integrates LLMs with Answer Set Programming (ASP) through an iterative feedback loop, enabling precise and reliable ......\n\n2. Dspy-based Neural-Symbolic Pipeline to Enhance Spatial ... - arXiv\n   https://arxiv.org/html/2411.18564v2\n   This paper presents a novel neural-symbolic framework that enhances LLMs' spatial reasoning abilities through iterative feedback between LLMs and Answer Set ......\n\n3. Dspy-based Neural-Symbolic Pipeline to Enhance Spatial ...\n   https://ui.adsabs.harvard.edu/abs/arXiv:2411.18564\n   This paper presents a novel neural-symbolic framework that enhances LLMs' spatial reasoning abilities through iterative feedback between LLMs and Answer Set ......\n\n4. How LLM Optimization Frameworks Like TEXTGRAD and DSPy Are ...\n   https://medium.com/@adnanmasood/beyond-prompt-engineering-how-llm-optimization-frameworks-like-textgrad-and-dspy-are-building-the-6790d3bf0b34\n   An in-depth review of test-time iterative refinement versus compile-time declarative programming for enhancing AI system performance....\n\n5. dspy.RLM\n   https://dspy.ai/api/modules/RLM/\n   The LLM writes Python code to examine data, call sub-LLMs for semantic analysis, and build up answers iteratively. The default interpreter is PythonInterpreter ......\n\n[1]   Done                    $PY \"$SKILL_DIR/scripts/aii_fast_web_search.py\" --query \"Logic-LM Pan 2023 NeurIPS LLM symbolic solver reasoning\" --max-results 5\n[2]-  Done                    $PY \"$SKILL_DIR/scripts/aii_fast_web_search.py\" --query \"DSPy ASP answer set programming iterative feedback LLM 2025\" --max-results 5\n[3]+  Done                    $PY \"$SKILL_DIR/scripts/aii_fast_web_search.py\" --query \"FOLIO natural language first order logic benchmark dataset\" --max-results 5", "stderr": "", "interrupted": false, "isImage": false, "noOutputExpected": false}
```

### [7] ASSISTANT · 2026-06-07 04:49:30 UTC

```
Good. Now I have the context needed to write the revised hypothesis. The reviewer's critiques are clear and I can address them all:

1. **Oracle circularity** → Use LLM oracle for SBFL localization only; use FOLIO/ProofWriter human-annotated queries for final evaluation
2. **Missing predicate blindness** → Add "failed sub-goal harvesting" from SLD traces 
3. **Coverage semantics** → Stratify by unification depth (direct vs indirect fault)
4. **Benchmark mismatch** → Replace CLUTRR/RuleTaker with FOLIO + ProofWriter NL; report bootstrap CIs
5. **Fuzzy unification** → Clarify as optional module with explicit call budget
6. **Missing related work** → Add Logic-LM and DSPy-ASP
7. **Statistical rigor** → Cohen's h, bootstrap CIs, multi-seed
```

### [8] TOOL CALL — Write · 2026-06-07 04:49:30 UTC

```
File: /ai-inventor/aii_data/runs/run_ITc1Qruy7fap/iter_2/gen_hypo/claude_agent/.terminal_claude_agent_struct_out.json

{
  "title": "Dual-Signal SBFL for Neuro-Symbolic NL-to-FOL Extraction: Ochiai Scores on Accessed Predicates Combined with Failed Sub-Goal Harvesting for Missing Clauses",
  "hypothesis": "Applying a dual-signal variant of Spectrum-Based Fault Localization (SBFL) to LLM-extracted Prolog knowledge bases — combining (a) Ochiai-coefficient suspiciousness on accessed predicates with (b) failed sub-goal harvesting for missing predicates — significantly improves multi-hop reasoning accuracy and reduces hallucination rates compared to one-shot and generic iterative neuro-symbolic pipelines. Specifically: (1) an LLM extracts an initial Prolog KB from text; (2) the same LLM generates a self-consistent 'localization oracle' of positive and negative queries used exclusively for SBFL-guided repair targeting — NOT for final evaluation; (3) Prolog proof attempts record a stratified coverage spectrum distinguishing predicates that fail to unify directly (direct-fault candidates) from predicates whose sub-goals fail downstream (indirect-fault candidates), plus unsatisfied sub-goals that never appear in the KB (missing-predicate candidates); (4) Ochiai suspiciousness scores over direct-fault candidates and sub-goal frequency counts over missing-predicate candidates are combined into a unified ranked repair agenda; (5) the LLM repairs top-K items per round. Final accuracy is measured exclusively on FOLIO and ProofWriter-NL benchmarks with human-annotated FOL ground truth, decoupling the localization oracle from evaluation, thereby eliminating circularity. The SLD-resolution trace with stratified fault labels serves as the human-auditable reasoning graph, and each repaired predicate is linked to the source text span that triggered it.",
  "motivation": "Current neuro-symbolic pipelines translate text to FOL in a single LLM pass, then invoke a symbolic reasoner without structured feedback about which extracted predicates are wrong or absent. Generic iterative refinement (self-refine) re-prompts the LLM with vague global feedback, wasting API calls and often re-hallucinating the same errors. Two structural problems block progress: (1) knowing WHERE the errors are (localization), and (2) knowing WHAT TYPE of error is present — an erroneous predicate that was extracted but wrongly represented, or a missing predicate that was never extracted at all. Standard SBFL from software testing addresses (1) for accessed predicates but is completely blind to (2) — missing predicates receive zero coverage and zero Ochiai score, yet NL-to-KB extraction systematically drops peripheral facts. The proposed dual-signal extension fills this gap by harvesting unsatisfied sub-goals from failed SLD traces as independent evidence of missing clauses, creating a repair agenda that covers both error types. A secondary structural problem in prior work is oracle circularity: if the same LLM generates both the KB and the test queries, a coherent-but-wrong KB may pass a hallucinated oracle. We resolve this by separating roles: the LLM oracle is used only for SBFL localization (which requires only relative pass/fail signal, not ground truth), while human-annotated benchmarks (FOLIO, ProofWriter-NL) provide the final evaluation signal. This design yields a fully self-grounding repair mechanism (no external supervision needed at repair time) with rigorous evaluation, producing hallucination-reduced neuro-symbolic systems with fully auditable proof traces.",
  "assumptions": [
    "The LLM-generated localization oracle provides sufficient relative pass/fail discrimination to rank predicates by suspiciousness even if it does not constitute ground truth — calibrated by measuring Spearman correlation between LLM-oracle-based Ochiai rankings and human-oracle-based rankings on a 20-document calibration set before full experiments.",
    "Unsatisfied sub-goals at the deepest point of failed SLD-resolution traces reliably identify missing predicates: sub-goals appearing in multiple failing proofs but never successfully resolved are candidate absent facts that the LLM failed to extract from the source text.",
    "Stratifying coverage by unification outcome (direct unification failure vs. successful unification with failing sub-goals) improves Ochiai precision over flat coverage, because predicates that fail to unify at all are more likely to be directly erroneous than predicates whose sub-goals fail downstream.",
    "LLMs given a suspicious predicate or unsatisfied sub-goal, the surrounding KB context, and the relevant source text span can produce correct repairs or additions at substantially higher rate than full KB re-generation, within a budget of 3 repair rounds and K=5 targets per round.",
    "FOLIO (human-annotated NL-to-FOL with expert-written premises and conclusions) and ProofWriter-NL (NL-surface variants of ProofWriter) provide sufficient benchmark signal to compare this pipeline against RAG, chain-of-thought, and generic self-refine baselines on multi-hop deduction accuracy and atomic fact precision/recall."
  ],
  "investigation_approach": "**Scope (stated explicitly):** The pipeline targets short professionally-written documents of ≤3000 characters, yielding ≤100 predicates per KB. At this scale, the coverage matrix is at most 100×30 (predicates × oracle queries), requiring negligible compute. The approach becomes computationally prohibitive above ~500 predicates due to quadratic growth in SLD-resolution depth and coverage tracking. This scope restriction is an explicit design choice, not a limitation to hide.\n\n**Phase 0 — Oracle Calibration (20 documents, before main experiments):** For each document, generate (a) LLM-oracle queries and (b) human-annotated queries. Compute Spearman correlation between Ochiai rankings derived from each oracle. If ρ < 0.6, the LLM oracle is unreliable for localization and the method is abandoned. If ρ ≥ 0.6, report ρ as a confidence bound on the localization signal. This calibration is a reported result, not a hidden pre-filter.\n\n**Phase 1 — Pipeline Construction:** Implement in Python with SWI-Prolog via pyswip. The extractor (LLM call 1) maps a 3000-character document to a Prolog KB. The localization oracle generator (LLM call 2) produces 20–30 yes/no queries for SBFL use only. A Prolog meta-interpreter runs each query and records: (i) for each predicate, whether it was accessed and whether it unified successfully (direct unification failure) or its sub-goals failed (indirect fault); (ii) the deepest unsatisfied sub-goal for each failing query. From this: Ochiai scores are computed over direct-fault predicates; missing-predicate scores are frequency counts of unsatisfied sub-goals across failing queries. Both ranked lists are merged into a unified repair agenda (top-K items). LLM call 3 receives each suspicious item with its source text span and returns a corrected predicate or a new predicate to add. The pipeline iterates up to 3 rounds. Optionally, an LLM-as-fuzzy-unifier module (LLM call 4, optional) is triggered when Prolog unification fails on surface-form variants; this is an explicit additional module with its own per-document call budget (max 5 calls), not part of the core SBFL mechanism.\n\n**Phase 2 — Baselines:** (a) one-shot LLM→FOL with no feedback; (b) standard RAG (sentence retrieval + LLM answer); (c) chain-of-thought prompting; (d) generic self-refine (LLM re-generates KB with only global pass-rate as feedback); (e) SBFL with flat (non-stratified) coverage as an ablation to test whether stratification improves precision.\n\n**Phase 3 — Evaluation on Human-Annotated Benchmarks:** Primary: FOLIO dataset (1,430 NL-to-FOL examples with expert-annotated FOL premises and conclusions). Secondary: ProofWriter-NL (NL-surface variants requiring multi-hop deduction). Metrics: (i) atomic fact precision and recall against human-annotated ground-truth predicates; (ii) multi-hop deduction accuracy (Prolog query answering vs. gold answers at 2-hop and 3-hop depths separately); (iii) hallucination rate (predicates in KB not grounded in source text, verified by human annotation on 150-document custom corpus); (iv) LLM API calls per document. All accuracy comparisons reported with 95% bootstrap confidence intervals (10,000 resamples) and Cohen's h effect size for proportions. Results run across 5 random seeds and 3 document splits.\n\n**Phase 4 — Analysis and Auditable Traces:** Ablate oracle quality (human vs. LLM-generated queries), stratification (flat vs. stratified coverage), repair budget (K=1,3,5 and 1–3 rounds), and suspiciousness metric (Ochiai vs. random vs. recency). Each repair round trace is output as a graph linking source text span → extracted predicate → Ochiai/sub-goal score → repair action, providing a human-auditable path from document to KB correction.",
  "success_criteria": "**Confirmation:** The dual-signal SBFL pipeline achieves statistically significant improvement (95% bootstrap CI excludes zero, Cohen's h ≥ 0.2) over one-shot extraction and generic self-refine on FOLIO multi-hop deduction accuracy; hallucination rate (unsupported predicates per KB) is reduced by ≥20% vs. one-shot extraction with human-verified annotation on the 150-document corpus; and total LLM calls per document is fewer than generic self-refine at matched accuracy. Separately: 3-hop deduction accuracy improvement exceeds 2-hop improvement, consistent with the prediction that SBFL advantage grows with reasoning depth.\n\n**Disconfirmation:** If dual-signal SBFL-targeted repair performs no better than randomly-selected predicate repair (matched for LLM call budget), the core SBFL mechanism provides no benefit. If Ochiai scores over direct-fault predicates alone (without sub-goal harvesting) match dual-signal performance, the missing-predicate component adds no value. If oracle calibration (Phase 0) shows ρ < 0.6, the approach is invalidated before main experiments.\n\n**Partial confirmation:** If SBFL reduces hallucination without improving multi-hop accuracy, the repair mechanism improves extraction fidelity but the KB structure (predicate schema design) is the downstream bottleneck. If sub-goal harvesting alone accounts for most gains without Ochiai, the missing-predicate signal dominates and the method should be reframed as sub-goal-driven iterative completion.",
  "related_works": [
    "Logic-LM (Pan et al., NeurIPS 2023): Integrates LLMs with symbolic solvers (Pyke, Z3) for logical reasoning. The LLM translates NL problems to formal representations; a symbolic solver executes them; solver error messages are fed back to the LLM for repair. Fundamentally differs from the proposed work in that feedback is generic solver error strings, not a principled ranked suspiciousness signal derived from coverage spectra. Logic-LM does not distinguish between erroneous accessed predicates and entirely missing predicates, and does not produce a stratified fault characterization. The proposed Ochiai+sub-goal dual signal is the key mechanistic advance over Logic-LM-style generic error feedback.",
    "DSPy-ASP (Dspy-based Neural-Symbolic Pipeline, ScienceDirect 2025): Combines LLMs with Answer Set Programming via an iterative feedback loop, using ASP solver outputs to guide LLM re-generation. Structurally similar to the proposed pipeline but uses global solver feedback rather than per-predicate suspiciousness scores. Does not address missing predicates separately from erroneous ones, and does not provide a ranked localization signal. The proposed work adds a principled statistical ranking mechanism (SBFL) that differentiates targeted repair from generic re-generation.",
    "AlloyFLhy (Wang et al., 2020): Applies SBFL and mutation-based fault localization to manually-authored Alloy specifications (first-order relational logic) with manually-crafted test formulas. The proposed work adapts the SBFL mechanism to automatically LLM-extracted NL-to-FOL knowledge bases, where the test oracle is LLM-generated and used only for localization (not evaluation), and where missing predicates require a complementary sub-goal harvesting mechanism absent in AlloyFLhy.",
    "LINC (Olausson et al., 2023): Translates NL reasoning to FOL via LLM and executes with a symbolic solver in one shot, without structured feedback. The proposed work adds a SBFL-guided iterative repair loop with stratified fault detection.",
    "Self-Refine (Madaan et al., 2023): Generic iterative LLM refinement where the LLM provides natural language critique as feedback. The proposed work replaces vague NL critique with a formal ranked suspiciousness score derived from symbolic proof coverage, providing a more principled and targeted repair signal.",
    "FOLIO (Han et al., 2022/2024, EMNLP): Human-annotated NL-to-FOL dataset with expert-written premises and FOL-annotated conclusions. Used in the proposed work as the primary evaluation benchmark to provide human-ground-truth evaluation decoupled from the LLM-generated localization oracle, directly resolving the oracle circularity problem.",
    "Inductive Learning of Logical Theories with LLMs (Souza et al., AAAI-25): Combines LLMs with Prolog-based feedback for logic theory induction on synthetic ILP tasks, using global accuracy as feedback rather than per-predicate suspiciousness. Targets rule induction from example facts, not NL document understanding."
  ],
  "inspiration": "The primary inspiration is a Level 3 (methodological) cross-field transfer: Spectrum-Based Fault Localization (SBFL) from software engineering — specifically the Ochiai coefficient used in Tarantula/Ochiai SBFL systems — is imported into the NL-to-FOL extraction domain. The transplant maps program statements→extracted Prolog predicates, test suite→LLM-generated localization oracle queries, test pass/fail→Prolog proof success/failure, and execution coverage→SLD-resolution trace. AlloyFLhy demonstrated this transfer works for manually-authored declarative logic specs; the proposed work extends it to automatically extracted NL-to-FOL KBs and introduces two mechanistic novelties absent in software SBFL: (1) stratified coverage tracking (direct unification failure vs. sub-goal failure) motivated by the semantically richer failure modes in Prolog vs. imperative code; and (2) failed sub-goal harvesting for missing predicates, which has no analog in software SBFL because missing statements are a code smell, not a bug — whereas missing extracted facts are the dominant failure mode in NL-to-KB extraction. The secondary inspiration is from metamorphic testing in software engineering: the test oracle is derived from the source document itself (not external ground truth), making the pipeline self-grounding. The key insight borrowed from calibrated oracle design in medical testing (sensitivity/specificity calibration before deployment) is to first measure oracle reliability on a calibration set before using it as the primary signal — this directly addresses the circularity critique by empirically quantifying how much the LLM oracle can be trusted for localization.",
  "terms": [
    {
      "term": "Dual-Signal SBFL",
      "definition": "An extension of Spectrum-Based Fault Localization that combines two complementary signals: (1) Ochiai suspiciousness scores over predicates accessed during proof attempts (for erroneous predicates), and (2) frequency counts of unsatisfied sub-goals in failed SLD traces (for missing predicates). Standard SBFL only computes signal (1), which is blind to predicates absent from the KB."
    },
    {
      "term": "Stratified Coverage Spectrum",
      "definition": "A coverage matrix that distinguishes three outcomes for each (predicate, query) pair: (a) predicate not accessed, (b) predicate accessed and unified successfully but sub-goals failed (indirect fault candidate), (c) predicate accessed but failed to unify (direct fault candidate). This stratification improves Ochiai precision over flat binary coverage by separating predicates that are directly erroneous from those that are collateral victims of downstream errors."
    },
    {
      "term": "Failed Sub-Goal Harvesting",
      "definition": "The process of recording the deepest unsatisfied sub-goal in each failed SLD-resolution proof. Sub-goals that appear frequently across multiple failing proofs but never successfully resolve are treated as evidence of missing predicates — facts or rules the LLM failed to extract from the source document. These are ranked by frequency and added to the repair agenda alongside Ochiai-ranked accessed predicates."
    },
    {
      "term": "Localization Oracle",
      "definition": "An LLM-generated set of yes/no reasoning queries used exclusively to drive SBFL-based repair targeting — not for final evaluation. Its sole role is to provide relative pass/fail signal to compute Ochiai scores and harvest sub-goals. Final accuracy is measured against independent human-annotated benchmarks (FOLIO, ProofWriter-NL), decoupling localization from evaluation and eliminating oracle circularity."
    },
    {
      "term": "Oracle Calibration",
      "definition": "A pre-experimental study measuring the Spearman correlation between Ochiai predicate rankings derived from LLM-generated oracle queries vs. human-annotated oracle queries on 20 held-out documents. A correlation ρ ≥ 0.6 validates that the LLM oracle provides sufficient relative ranking signal for localization; ρ < 0.6 constitutes a pre-registered invalidation criterion for the approach."
    },
    {
      "term": "SLD-Resolution Trace (Stratified)",
      "definition": "The step-by-step record of SWI-Prolog proof attempts, augmented with per-step unification outcome labels (unified / failed-to-unify / sub-goals-failed). This stratified trace serves as the human-auditable reasoning graph, with each predicate repair linked back to the source text span from which it should have been extracted."
    },
    {
      "term": "Fuzzy Unification Module (Optional)",
      "definition": "An optional pipeline component (LLM call 4, max 5 calls per document) that is triggered when Prolog unification fails due to surface-form variation (e.g., 'agreement' vs. 'contract'). The LLM is queried to determine semantic equivalence given the source text context. This module is explicitly separate from the core SBFL mechanism and is ablated independently."
    }
  ],
  "summary": "We propose a dual-signal extension of Spectrum-Based Fault Localization for repairing LLM-extracted Prolog knowledge bases: Ochiai suspiciousness scores target erroneous accessed predicates, while failed sub-goal harvesting from SLD traces targets missing predicates — the dominant error type invisible to standard SBFL. Oracle circularity is resolved by using the LLM-generated oracle exclusively for repair localization while evaluating final accuracy on human-annotated FOLIO and ProofWriter-NL benchmarks, with oracle reliability pre-validated via a Spearman-correlation calibration study."
}
```

### [9] TOOL RESULT — Write · 2026-06-07 04:49:30 UTC

```
{"type": "create", "filePath": "/ai-inventor/aii_data/runs/run_ITc1Qruy7fap/iter_2/gen_hypo/claude_agent/.terminal_claude_agent_struct_out.json", "content": "{\n  \"title\": \"Dual-Signal SBFL for Neuro-Symbolic NL-to-FOL Extraction: Ochiai Scores on Accessed Predicates Combined with Failed Sub-Goal Harvesting for Missing Clauses\",\n  \"hypothesis\": \"Applying a dual-signal variant of Spectrum-Based Fault Localization (SBFL) to LLM-extracted Prolog knowledge bases — combining (a) Ochiai-coefficient suspiciousness on accessed predicates with (b) failed sub-goal harvesting for missing predicates — significantly improves multi-hop reasoning accuracy and reduces hallucination rates compared to one-shot and generic iterative neuro-symbolic pipelines. Specifically: (1) an LLM extracts an initial Prolog KB from text; (2) the same LLM generates a self-consistent 'localization oracle' of positive and negative queries used exclusively for SBFL-guided repair targeting — NOT for final evaluation; (3) Prolog proof attempts record a stratified coverage spectrum distinguishing predicates that fail to unify directly (direct-fault candidates) from predicates whose sub-goals fail downstream (indirect-fault candidates), plus unsatisfied sub-goals that never appear in the KB (missing-predicate candidates); (4) Ochiai suspiciousness scores over direct-fault candidates and sub-goal frequency counts over missing-predicate candidates are combined into a unified ranked repair agenda; (5) the LLM repairs top-K items per round. Final accuracy is measured exclusively on FOLIO and ProofWriter-NL benchmarks with human-annotated FOL ground truth, decoupling the localization oracle from evaluation, thereby eliminating circularity. The SLD-resolution trace with stratified fault labels serves as the human-auditable reasoning graph, and each repaired predicate is linked to the source text span that triggered it.\",\n  \"motivation\": \"Current neuro-symbolic pipelines translate text to FOL in a single LLM pass, then invoke a symbolic reasoner without structured feedback about which extracted predicates are wrong or absent. Generic iterative refinement (self-refine) re-prompts the LLM with vague global feedback, wasting API calls and often re-hallucinating the same errors. Two structural problems block progress: (1) knowing WHERE the errors are (localization), and (2) knowing WHAT TYPE of error is present — an erroneous predicate that was extracted but wrongly represented, or a missing predicate that was never extracted at all. Standard SBFL from software testing addresses (1) for accessed predicates but is completely blind to (2) — missing predicates receive zero coverage and zero Ochiai score, yet NL-to-KB extraction systematically drops peripheral facts. The proposed dual-signal extension fills this gap by harvesting unsatisfied sub-goals from failed SLD traces as independent evidence of missing clauses, creating a repair agenda that covers both error types. A secondary structural problem in prior work is oracle circularity: if the same LLM generates both the KB and the test queries, a coherent-but-wrong KB may pass a hallucinated oracle. We resolve this by separating roles: the LLM oracle is used only for SBFL localization (which requires only relative pass/fail signal, not ground truth), while human-annotated benchmarks (FOLIO, ProofWriter-NL) provide the final evaluation signal. This design yields a fully self-grounding repair mechanism (no external supervision needed at repair time) with rigorous evaluation, producing hallucination-reduced neuro-symbolic systems with fully auditable proof traces.\",\n  \"assumptions\": [\n    \"The LLM-generated localization oracle provides sufficient relative pass/fail discrimination to rank predicates by suspiciousness even if it does not constitute ground truth — calibrated by measuring Spearman correlation between LLM-oracle-based Ochiai rankings and human-oracle-based rankings on a 20-document calibration set before full experiments.\",\n    \"Unsatisfied sub-goals at the deepest point of failed SLD-resolution traces reliably identify missing predicates: sub-goals appearing in multiple failing proofs but never successfully resolved are candidate absent facts that the LLM failed to extract from the source text.\",\n    \"Stratifying coverage by unification outcome (direct unification failure vs. successful unification with failing sub-goals) improves Ochiai precision over flat coverage, because predicates that fail to unify at all are more likely to be directly erroneous than predicates whose sub-goals fail downstream.\",\n    \"LLMs given a suspicious predicate or unsatisfied sub-goal, the surrounding KB context, and the relevant source text span can produce correct repairs or additions at substantially higher rate than full KB re-generation, within a budget of 3 repair rounds and K=5 targets per round.\",\n    \"FOLIO (human-annotated NL-to-FOL with expert-written premises and conclusions) and ProofWriter-NL (NL-surface variants of ProofWriter) provide sufficient benchmark signal to compare this pipeline against RAG, chain-of-thought, and generic self-refine baselines on multi-hop deduction accuracy and atomic fact precision/recall.\"\n  ],\n  \"investigation_approach\": \"**Scope (stated explicitly):** The pipeline targets short professionally-written documents of ≤3000 characters, yielding ≤100 predicates per KB. At this scale, the coverage matrix is at most 100×30 (predicates × oracle queries), requiring negligible compute. The approach becomes computationally prohibitive above ~500 predicates due to quadratic growth in SLD-resolution depth and coverage tracking. This scope restriction is an explicit design choice, not a limitation to hide.\\n\\n**Phase 0 — Oracle Calibration (20 documents, before main experiments):** For each document, generate (a) LLM-oracle queries and (b) human-annotated queries. Compute Spearman correlation between Ochiai rankings derived from each oracle. If ρ < 0.6, the LLM oracle is unreliable for localization and the method is abandoned. If ρ ≥ 0.6, report ρ as a confidence bound on the localization signal. This calibration is a reported result, not a hidden pre-filter.\\n\\n**Phase 1 — Pipeline Construction:** Implement in Python with SWI-Prolog via pyswip. The extractor (LLM call 1) maps a 3000-character document to a Prolog KB. The localization oracle generator (LLM call 2) produces 20–30 yes/no queries for SBFL use only. A Prolog meta-interpreter runs each query and records: (i) for each predicate, whether it was accessed and whether it unified successfully (direct unification failure) or its sub-goals failed (indirect fault); (ii) the deepest unsatisfied sub-goal for each failing query. From this: Ochiai scores are computed over direct-fault predicates; missing-predicate scores are frequency counts of unsatisfied sub-goals across failing queries. Both ranked lists are merged into a unified repair agenda (top-K items). LLM call 3 receives each suspicious item with its source text span and returns a corrected predicate or a new predicate to add. The pipeline iterates up to 3 rounds. Optionally, an LLM-as-fuzzy-unifier module (LLM call 4, optional) is triggered when Prolog unification fails on surface-form variants; this is an explicit additional module with its own per-document call budget (max 5 calls), not part of the core SBFL mechanism.\\n\\n**Phase 2 — Baselines:** (a) one-shot LLM→FOL with no feedback; (b) standard RAG (sentence retrieval + LLM answer); (c) chain-of-thought prompting; (d) generic self-refine (LLM re-generates KB with only global pass-rate as feedback); (e) SBFL with flat (non-stratified) coverage as an ablation to test whether stratification improves precision.\\n\\n**Phase 3 — Evaluation on Human-Annotated Benchmarks:** Primary: FOLIO dataset (1,430 NL-to-FOL examples with expert-annotated FOL premises and conclusions). Secondary: ProofWriter-NL (NL-surface variants requiring multi-hop deduction). Metrics: (i) atomic fact precision and recall against human-annotated ground-truth predicates; (ii) multi-hop deduction accuracy (Prolog query answering vs. gold answers at 2-hop and 3-hop depths separately); (iii) hallucination rate (predicates in KB not grounded in source text, verified by human annotation on 150-document custom corpus); (iv) LLM API calls per document. All accuracy comparisons reported with 95% bootstrap confidence intervals (10,000 resamples) and Cohen's h effect size for proportions. Results run across 5 random seeds and 3 document splits.\\n\\n**Phase 4 — Analysis and Auditable Traces:** Ablate oracle quality (human vs. LLM-generated queries), stratification (flat vs. stratified coverage), repair budget (K=1,3,5 and 1–3 rounds), and suspiciousness metric (Ochiai vs. random vs. recency). Each repair round trace is output as a graph linking source text span → extracted predicate → Ochiai/sub-goal score → repair action, providing a human-auditable path from document to KB correction.\",\n  \"success_criteria\": \"**Confirmation:** The dual-signal SBFL pipeline achieves statistically significant improvement (95% bootstrap CI excludes zero, Cohen's h ≥ 0.2) over one-shot extraction and generic self-refine on FOLIO multi-hop deduction accuracy; hallucination rate (unsupported predicates per KB) is reduced by ≥20% vs. one-shot extraction with human-verified annotation on the 150-document corpus; and total LLM calls per document is fewer than generic self-refine at matched accuracy. Separately: 3-hop deduction accuracy improvement exceeds 2-hop improvement, consistent with the prediction that SBFL advantage grows with reasoning depth.\\n\\n**Disconfirmation:** If dual-signal SBFL-targeted repair performs no better than randomly-selected predicate repair (matched for LLM call budget), the core SBFL mechanism provides no benefit. If Ochiai scores over direct-fault predicates alone (without sub-goal harvesting) match dual-signal performance, the missing-predicate component adds no value. If oracle calibration (Phase 0) shows ρ < 0.6, the approach is invalidated before main experiments.\\n\\n**Partial confirmation:** If SBFL reduces hallucination without improving multi-hop accuracy, the repair mechanism improves extraction fidelity but the KB structure (predicate schema design) is the downstream bottleneck. If sub-goal harvesting alone accounts for most gains without Ochiai, the missing-predicate signal dominates and the method should be reframed as sub-goal-driven iterative completion.\",\n  \"related_works\": [\n    \"Logic-LM (Pan et al., NeurIPS 2023): Integrates LLMs with symbolic solvers (Pyke, Z3) for logical reasoning. The LLM translates NL problems to formal representations; a symbolic solver executes them; solver error messages are fed back to the LLM for repair. Fundamentally differs from the proposed work in that feedback is generic solver error strings, not a principled ranked suspiciousness signal derived from coverage spectra. Logic-LM does not distinguish between erroneous accessed predicates and entirely missing predicates, and does not produce a stratified fault characterization. The proposed Ochiai+sub-goal dual signal is the key mechanistic advance over Logic-LM-style generic error feedback.\",\n    \"DSPy-ASP (Dspy-based Neural-Symbolic Pipeline, ScienceDirect 2025): Combines LLMs with Answer Set Programming via an iterative feedback loop, using ASP solver outputs to guide LLM re-generation. Structurally similar to the proposed pipeline but uses global solver feedback rather than per-predicate suspiciousness scores. Does not address missing predicates separately from erroneous ones, and does not provide a ranked localization signal. The proposed work adds a principled statistical ranking mechanism (SBFL) that differentiates targeted repair from generic re-generation.\",\n    \"AlloyFLhy (Wang et al., 2020): Applies SBFL and mutation-based fault localization to manually-authored Alloy specifications (first-order relational logic) with manually-crafted test formulas. The proposed work adapts the SBFL mechanism to automatically LLM-extracted NL-to-FOL knowledge bases, where the test oracle is LLM-generated and used only for localization (not evaluation), and where missing predicates require a complementary sub-goal harvesting mechanism absent in AlloyFLhy.\",\n    \"LINC (Olausson et al., 2023): Translates NL reasoning to FOL via LLM and executes with a symbolic solver in one shot, without structured feedback. The proposed work adds a SBFL-guided iterative repair loop with stratified fault detection.\",\n    \"Self-Refine (Madaan et al., 2023): Generic iterative LLM refinement where the LLM provides natural language critique as feedback. The proposed work replaces vague NL critique with a formal ranked suspiciousness score derived from symbolic proof coverage, providing a more principled and targeted repair signal.\",\n    \"FOLIO (Han et al., 2022/2024, EMNLP): Human-annotated NL-to-FOL dataset with expert-written premises and FOL-annotated conclusions. Used in the proposed work as the primary evaluation benchmark to provide human-ground-truth evaluation decoupled from the LLM-generated localization oracle, directly resolving the oracle circularity problem.\",\n    \"Inductive Learning of Logical Theories with LLMs (Souza et al., AAAI-25): Combines LLMs with Prolog-based feedback for logic theory induction on synthetic ILP tasks, using global accuracy as feedback rather than per-predicate suspiciousness. Targets rule induction from example facts, not NL document understanding.\"\n  ],\n  \"inspiration\": \"The primary inspiration is a Level 3 (methodological) cross-field transfer: Spectrum-Based Fault Localization (SBFL) from software engineering — specifically the Ochiai coefficient used in Tarantula/Ochiai SBFL systems — is imported into the NL-to-FOL extraction domain. The transplant maps program statements→extracted Prolog predicates, test suite→LLM-generated localization oracle queries, test pass/fail→Prolog proof success/failure, and execution coverage→SLD-resolution trace. AlloyFLhy demonstrated this transfer works for manually-authored declarative logic specs; the proposed work extends it to automatically extracted NL-to-FOL KBs and introduces two mechanistic novelties absent in software SBFL: (1) stratified coverage tracking (direct unification failure vs. sub-goal failure) motivated by the semantically richer failure modes in Prolog vs. imperative code; and (2) failed sub-goal harvesting for missing predicates, which has no analog in software SBFL because missing statements are a code smell, not a bug — whereas missing extracted facts are the dominant failure mode in NL-to-KB extraction. The secondary inspiration is from metamorphic testing in software engineering: the test oracle is derived from the source document itself (not external ground truth), making the pipeline self-grounding. The key insight borrowed from calibrated oracle design in medical testing (sensitivity/specificity calibration before deployment) is to first measure oracle reliability on a calibration set before using it as the primary signal — this directly addresses the circularity critique by empirically quantifying how much the LLM oracle can be trusted for localization.\",\n  \"terms\": [\n    {\n      \"term\": \"Dual-Signal SBFL\",\n      \"definition\": \"An extension of Spectrum-Based Fault Localization that combines two complementary signals: (1) Ochiai suspiciousness scores over predicates accessed during proof attempts (for erroneous predicates), and (2) frequency counts of unsatisfied sub-goals in failed SLD traces (for missing predicates). Standard SBFL only computes signal (1), which is blind to predicates absent from the KB.\"\n    },\n    {\n      \"term\": \"Stratified Coverage Spectrum\",\n      \"definition\": \"A coverage matrix that distinguishes three outcomes for each (predicate, query) pair: (a) predicate not accessed, (b) predicate accessed and unified successfully but sub-goals failed (indirect fault candidate), (c) predicate accessed but failed to unify (direct fault candidate). This stratification improves Ochiai precision over flat binary coverage by separating predicates that are directly erroneous from those that are collateral victims of downstream errors.\"\n    },\n    {\n      \"term\": \"Failed Sub-Goal Harvesting\",\n      \"definition\": \"The process of recording the deepest unsatisfied sub-goal in each failed SLD-resolution proof. Sub-goals that appear frequently across multiple failing proofs but never successfully resolve are treated as evidence of missing predicates — facts or rules the LLM failed to extract from the source document. These are ranked by frequency and added to the repair agenda alongside Ochiai-ranked accessed predicates.\"\n    },\n    {\n      \"term\": \"Localization Oracle\",\n      \"definition\": \"An LLM-generated set of yes/no reasoning queries used exclusively to drive SBFL-based repair targeting — not for final evaluation. Its sole role is to provide relative pass/fail signal to compute Ochiai scores and harvest sub-goals. Final accuracy is measured against independent human-annotated benchmarks (FOLIO, ProofWriter-NL), decoupling localization from evaluation and eliminating oracle circularity.\"\n    },\n    {\n      \"term\": \"Oracle Calibration\",\n      \"definition\": \"A pre-experimental study measuring the Spearman correlation between Ochiai predicate rankings derived from LLM-generated oracle queries vs. human-annotated oracle queries on 20 held-out documents. A correlation ρ ≥ 0.6 validates that the LLM oracle provides sufficient relative ranking signal for localization; ρ < 0.6 constitutes a pre-registered invalidation criterion for the approach.\"\n    },\n    {\n      \"term\": \"SLD-Resolution Trace (Stratified)\",\n      \"definition\": \"The step-by-step record of SWI-Prolog proof attempts, augmented with per-step unification outcome labels (unified / failed-to-unify / sub-goals-failed). This stratified trace serves as the human-auditable reasoning graph, with each predicate repair linked back to the source text span from which it should have been extracted.\"\n    },\n    {\n      \"term\": \"Fuzzy Unification Module (Optional)\",\n      \"definition\": \"An optional pipeline component (LLM call 4, max 5 calls per document) that is triggered when Prolog unification fails due to surface-form variation (e.g., 'agreement' vs. 'contract'). The LLM is queried to determine semantic equivalence given the source text context. This module is explicitly separate from the core SBFL mechanism and is ablated independently.\"\n    }\n  ],\n  \"summary\": \"We propose a dual-signal extension of Spectrum-Based Fault Localization for repairing LLM-extracted Prolog knowledge bases: Ochiai suspiciousness scores target erroneous accessed predicates, while failed sub-goal harvesting from SLD traces targets missing predicates — the dominant error type invisible to standard SBFL. Oracle circularity is resolved by using the LLM-generated oracle exclusively for repair localization while evaluating final accuracy on human-annotated FOLIO and ProofWriter-NL benchmarks, with oracle reliability pre-validated via a Spearman-correlation calibration study.\"\n}\n", "structuredPatch": [], "originalFile": null, "userModified": false}
```

### [10] ASSISTANT · 2026-06-07 04:49:42 UTC

```
The revised hypothesis is written. Here's a summary of how each major critique was addressed:

**Major critiques resolved:**

1. **Oracle circularity** → The LLM oracle is now used exclusively for SBFL *localization* (repair targeting), never for evaluation. Final accuracy is measured only on human-annotated FOLIO and ProofWriter-NL benchmarks. A pre-experimental oracle calibration study (Spearman ρ ≥ 0.6 threshold) is required before main experiments proceed.

2. **SBFL blind to missing predicates** → Added "failed sub-goal harvesting": unsatisfied sub-goals appearing frequently across failing proofs are ranked by frequency as missing-predicate candidates, merged with Ochiai scores into a unified repair agenda.

3. **Coverage semantics differ from software SBFL** → Added stratified coverage spectrum distinguishing direct unification failures from sub-goal failures, improving Ochiai precision and enabling more accurate fault attribution.

4. **Benchmark mismatch** → Replaced CLUTRR/RuleTaker with FOLIO (human-annotated NL-to-FOL) and ProofWriter-NL. All comparisons now report 95% bootstrap CIs, Cohen's h effect sizes, 5 random seeds, and separate 2-hop vs. 3-hop accuracy breakdowns.

**Minor critiques resolved:**

5. **Scope restriction** → Explicitly stated in investigation approach with computational analysis of when the approach becomes prohibitive.

6. **Fuzzy unification** → Defined as an explicit optional module (LLM call 4, max 5 calls/doc) separate from the core SBFL mechanism, ablated independently.

7. **Missing related works** → Logic-LM (Pan et al., NeurIPS 2023) and DSPy-ASP (ScienceDirect 2025) added with precise mechanistic differentiation.

8. **Statistical rigor** → Fixed thresholds replaced with Cohen's h, bootstrap CIs, multi-seed evaluation, and depth-stratified accuracy reporting.
```
